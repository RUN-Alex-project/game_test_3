# v1.43 交付报告：固定冒险者 I

状态：开发完成；本对话开发+SE 自验门禁通过
任务书：docs/未来版本任务书/v1.43_固定冒险者I.md
工程：E:\deepseek-work\TKS3_mod\godot_remake
日期：2026-08-20
输入限制：仅纯文本/代码/JSON/资源路径/节点属性/命令/日志

---

## 1. 结果摘要

v1.43 把 12 名首批固定冒险者接入生产：名册、关系值/历史/送礼、六类好友委托各一条。玩家在卡萨诺城日常任务官对话点「冒险者公告板」打开面板。存档 v21 迁移到 v22，顶层唯一新字段 `expansion_state`，`SAVE_SCHEMA_KEYS` 39 项。不新增卡萨诺地图演员，候补 24 名不进生产名册。

## 2. 生产调用链

日常任务官 → `adventurer_board` → `AdventurerRosterPanel` → `GameState.gift_adventurer` / `accept_commission` / `claim_commission` → RelationshipService / CommissionService 纯函数返回新 expansion → GameState 一次性提交 → `save_game` 写 v22。

战斗击杀走现有 `apply_victory_rewards` 再 `CommissionService.record_kill`。切图 `_apply_current_map` 调 `GameState.note_map_visit`。

UI 不直接写 `GameState.expansion_state`；生产无 `set_relationship_value`。

## 3. 数据与存档

- `data/adventurers.json`：12 人，ID 为 `npc_adv_lin_xia` 等，`source_classification=singleplayer_extension`
- `data/adventurer_commissions.json`：6 条（collect/explore/kill/deliver/pet/campaign）
- `data/relationship_rules.json`：认识 0 / 朋友 10 / 知己 200 / 恋人 500 / 爱人 1000
- `world_seed==0` 写入固定值 `1297043285`，不读运行中 GameState
- 绑定物 = `category == equipment`，不可送礼
- `docs/expansion_data_contract_v22.json` `status=ENABLED`
- `docs/save_schema_registry.json` version=22，39 字段

委托目标：silver_ore / thunder_continent / spider / fruit / pet_level>=2 / owned_territory 非空。

## 4. 测试

- `tests/test_adventurer_registry_scene.tscn`
- `tests/test_adventurer_relationship_scene.tscn`
- `tests/test_adventurer_roster_main_scene.tscn`
- `work/v143/verify_adventurer_negatives.py`：N1-N8 NEG_ALL_PASS
- 基线：`tests/test_manifest.json` 69 场景；`docs/current_test_baseline.json` automated_scenes=69 total_runs=70

## 5. 门禁实测（本对话）

- 专项三场景：全部 PASS，Godot 退出码 0
- 负向：`python work/v143/verify_adventurer_negatives.py` → NEG_ALL_PASS
- 全量：`python tools/run_tests.py` → registered=69 applicable=69 passed=69 failed=0，加主场景冒烟，约 94s
- `python docs/doc_gate.py` → DOC_GATE PASS

未重建 exe/pck，不声称 release gate 通过。未跑 Linux 全量。

## 6. 新增/修改文件（主要）

新增：
- scripts/adventurer_roster_panel.gd
- scripts/adventurer_service.gd / relationship_service.gd / commission_service.gd / expansion_state_service.gd（本版前半成品补齐）
- data/adventurers.json / adventurer_commissions.json / relationship_rules.json
- tests/test_adventurer_*_scene.gd/.tscn
- work/v143/verify_adventurer_negatives.py
- work/v143/history_probe.json

修改：
- scripts/game_state.gd（v22 入口）
- scripts/main_original.gd（公告板入口、切图 note_map_visit、hide 面板）
- docs/save_schema_registry.json
- docs/expansion_data_contract_v22.json
- tests/test_save_integrity_scene.gd
- tests/test_moyu_expansion_registry_scene.gd（合同 ENABLED）
- tests/ui_audit/test_ui_startup_panels_hidden_scene.gd（13 面板）
- tests/ui_audit/test_ui_panel_bounds_scene.gd
- tests/test_manifest.json
- docs/current_test_baseline.json
- README.md / 开发进度.md / docs/综合整改报告.md

## 7. 刻意未做

- 不重建发布包
- 不开 v1.44（日结/邮件/直接交易）
- 不把候补 24 名写进生产
- 不改用户冻结数值
- 不新增卡萨诺地图 NPC

## 8. 未完成/限制

- 工作区仍有父仓库 README 与大量 `.import` 未跟踪文件；本版未 `git add -A`。
- 未提交 git commit（用户未要求）。
