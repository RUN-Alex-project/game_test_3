# v1.42 交付报告：内容证据与扩展数据合同

状态：开发完成，待 PM/SE 独立复验（**未自行宣称通过**）
任务书：docs/未来版本任务书/v1.42_内容证据与数据合同.md
开发执行者：DeepSeek V4 Flash（输入与验收全部为纯文本、源代码、JSON、资源路径、节点属性、命令、运行时状态与日志；无任何非文本附件）

---

## 0. 自查中发现的限制或不确定性（如实列出）

### 0.1 2.2 的实际提取结果、命令、退出码及未确认范围

- **可重跑字节扫描（本项目）**：`timeout 90 python work/v142/extract_swf_text.py "E:/魔域/2.2.swf" "work/v142/text/moyu_2.2_strings.txt" --label "2.2"`
  - 退出码 0，耗时约 3.3s，输出去重唯一字符串：UTF-8 CJK 2978 条 / GBK CJK（二级）64835 条 / ASCII 24046 条。
  - 原始字节扫描不解析 AS2/AS3 动作码语义，故 2.2 的证据级别仅为"字符串确实存在于 SWF 字节中"。
- **FFDec 动作码/p-code 导出（历史审计，TKS3_mod/work/moyu_versions_audit）**：JPEXS v26.2.1 对 2.2 的 AS2 动作翻译报 `SEVERE: Invalid property index: PopItem/DuplicateItem/DeleteActionItem...`，产生约 1.8GB 错误日志，p-code 导出为空。
  - **未确认范围**：2.2 的机制、地图拓扑、角色坐标、交互接线、数值公式均无稳定证据，全部如实不写入 local_version_confirmed；仅在受控文本中有名称/文本 token 时允许名称级证据。
- 2.2/2.3/2.4 内容字符串高度一致（同系列版本增量），绝大多数 token 在三个版本均命中；版本差异以 2.4 为主证据文本登记，命中版本在条目 notes 中记录。

### 0.2 所有 evidence_gap

- 注册表条目级 evidence_gap：**0 条**（没有登记任何"确认有但缺证据"的条目）。
- 证据能力边界（非条目级 gap，登记于 evidence.md 第 7 节与决策文档）：
  1. 字节扫描只证明字符串存在，不证明运行机制（按钮跳转/触发条件/数值公式）。
  2. 地图名称 token 不证明地图拓扑、角色坐标或全部交互（总计划书 2.3 规则）。
  3. 2.2 的 FFDec 动作码/p-code 导出失败 → 2.2 仅字符串级证据，机制级仍为 gap。
  4. 本地有"朋友/好友/邮寄"文本与入口，但不代表存在完整拍卖行或联网玩家经济；拍卖/排行/日结标 singleplayer_extension。
  5. 五个分段挑战（偷袭后路/军团管理员/守卫沼泽/黑衣圣兽师/断舰行动）仅名称/局部 token 确认（local_version_partial），完整规则未确认。
  6. DefineText/DefineEditText 静态字形文本依赖字体映射，字节扫描无法还原；既有 FFDec 审计显示 2.3/2.4 静态文本仅"魔域"标题。

### 0.3 未纳入候选目录的内容及原因

1. 2.2 的机制结论（FFDec 动作码失败 → 只保留字符串级证据）。
2. 答题系统的具体题库（历史/文学/科学问答 token 存在，但完整题库机制未确认，仅登记"答题/声望"系统级 token）。
3. 婚姻/恋爱完整规则（token 表明存在恋人/分手/永久失去朋友，但完整数值/触发/结局未确认；恋爱作为可选文本关系默认方案留 v1.43）。
4. 军团玩家组队玩法（有军团/军衔/爵位/军团管理员 token，但"玩家可加入军团"联网行为原版存在与否未确认；仅登记军衔任务/军团管理员挑战等背景）。
5. 各地"战场/郊区/幻境"子区域（token 大量存在，但仅名称级；留待对应版本实现时选择，避免目录膨胀）。
6. 所有"精英/头目/护卫"怪物系列（token 大量存在，本次仅登记 11 个代表性 Boss，避免与现有 data/monsters.json 重复登记）。
7. 截图/视觉素材（本版本严格不处理任何非文本附件；SWF 内图片不导入、不引用）。

### 0.4 本版本未修改生产代码、数值、场景、存档和发布包的核对结果

git 仓库根为 E:\deepseek-work\TKS3_mod（父仓库）。`git status` 核对：

- **生产目录零修改**：`scripts/`、`scenes/`、`data/`、`project.godot`、`export_presets.cfg`、`artifacts/` 在 `git status` 中均无改动记录。
- 已跟踪修改仅限本版本允许范围：`run_tests.ps1`、`docs/doc_gate.py`、`docs/综合整改报告.md`、`godot_remake/README.md`、`开发进度.md`、`版本改动.md`（均为测试口径/v1.42 状态说明）。
- 父目录 `TKS3_mod/README.md`（`../README.md`）有**先前会话遗留**的未提交改动（项目简介改写，含 v1.42 无关内容），本版本未触碰；将在交付中如实标注。
- 新增文件：`docs/moyu_23_24_content_registry.json`、`docs/moyu_23_24_evidence.md`、`docs/expansion_data_contract_v22.json`、`docs/expansion_content_decisions.md`、`docs/current_test_baseline.json`、`docs/evidence/v142/*`（3 文本 + 3 脚本规范副本）、`tests/test_moyu_expansion_registry_scene.{gd,tscn}`。
- `work/v142/`（可重跑提取脚本 + 提取输出）按父仓库 .gitignore 忽略，不提交；规范证据副本在 `docs/evidence/v142/`。
- 存档保持 v21；`expansion_data_contract_v22.json` 明确 `status: NOT_ENABLED`，未接入 GameState/SAVE_SCHEMA_DEFAULTS；发布包未重建（exe/pck 时间戳与哈希不变）。

---

## 1. 2.2/2.3/2.4 实际反查范围与 2.2 未完成缺口

见第 0.1 节。命令、退出码、耗时、未确认范围已逐项列出；2.2 未完成（机制级）缺口如实登记，未写入任何确认事实。

## 2. 每类内容数量、证据状态数量和版本分配统计

来源：`docs/moyu_23_24_content_registry.json`（registry_version v1.42.1，baseline v1.41）。

| 类别 | 数量 |
|---|---:|
| 候选地图 | 22 |
| 候选任务（主线15/区域支线16/冒险者委托14/分段挑战12/日常周常15） | 72 |
| 首批固定冒险者 | 12 |
| 候补固定冒险者 | 24 |
| 候选物品/材料 | 22 |
| 候选技能/Boss（9 技能 + 11 Boss） | 20 |
| 单机原创系统 | 6 |
| **合计** | **178** |

证据状态：local_version_confirmed 108 / local_version_partial 5 / singleplayer_extension 65 / evidence_gap 0。

版本分配（planned_version）：v1.43×26、v1.44×7、v1.45×2、v1.46×1、v1.47×5、v1.48×18、v1.49×7、v1.50×17、v1.51×15、v1.52×30、v1.53×20、v1.54×3、not_planned×27。全部在《v1.42 以后单机长线扩展任务书》版本路线内。

## 3. v22 数据合同及其未启用声明

`docs/expansion_data_contract_v22.json`：
- `status: NOT_ENABLED`；`namespace: GameState.expansion_state`；`baseline_save_version: v21`；`activation_version: v1.43`。
- 15 个字段（revision/world_seed/day_sequence/adventurers/relationships/mailbox/commission_state/rankings/season/economy/properties/auction/campaign/collections）逐项登记：类型、固定默认值、首个启用版本、迁移策略、拥有服务、是否允许为空、是否参与 deterministic seed。
- 迁移铁律：从固定 schema 默认值构建 DTO，不读取运行中 GameState；加载成功一次性提交；失败内存不变。
- 确定性输入：world_seed + game_day + system_id + entity_id + event_sequence。
- **未接入 GameState、SAVE_SCHEMA_DEFAULTS、存档版本；生产存档保持 v21，不增加任何持久字段。**

## 4. 正向与每项负向的精确错误码

正向验证器（唯一，同时被负向复用）：`res://tests/test_moyu_expansion_registry_scene.tscn`（tests/test_moyu_expansion_registry_scene.gd）。

- **正向**：`godot --headless --path ... --scene res://tests/test_moyu_expansion_registry_scene.tscn --quit-after 300` → `PASS moyu_expansion_registry: ...（registry 全部条目 178.0）`，退出码 0。
- **负向**（work/v142/verify_registry_negatives.py，10 项，每项先断言变异生效，再由同一正向验证器命中精确错误码，try/finally 恢复，无残留）：

| 负向 | 变异 | 精确错误码 |
|---|---|---|
| N1 | confirmed token → 不存在 token | `ERR_TOKEN_MISS` |
| N2 | confirmed source → 不存在文件 | `ERR_SOURCE_MISS` |
| N3 | 复制已有 ID | `ERR_DUP_ID` |
| N4 | evidence_status → 非法值 | `ERR_BAD_EVIDENCE_STATUS` |
| N5 | source_version → 不允许值 | `ERR_BAD_SOURCE_VERSION` |
| N6 | planned_version → 不存在版本 | `ERR_BAD_PLANNED` |
| N7 | 删除 v22 必填字段 | `ERR_CONTRACT_FIELD_MISS` |
| N8 | 候选地图 ID → 现有 maps.json ID | `ERR_MAP_COLLIDE` |
| N9 | 候选任务 ID → 现有 quests.json ID | `ERR_QUEST_COLLIDE` |
| N10 | singleplayer_extension 非法加 token | `ERR_GAP_FABRICATED_TOKEN`（+`ERR_GAP_FABRICATED_SOURCE`） |

- 结果：`NEG_ALL_PASS: 10 项真实变异全部先证明生效、再由正向同一验证器命中精确错误码；无残留；恢复后 PASS`，退出码 0。

## 5. runner 自动化场景数量变动与 doc gate 机械验证方式

- `run_tests.ps1` `$scenes` 数组：65 → **66**（新增 `res://tests/test_moyu_expansion_registry_scene.tscn`）；主场景冒烟固定 1 次；总 RUN 66 → **67**。
- 新增唯一测试基线文件 `docs/current_test_baseline.json`：`{automated_scenes: 66, smoke_scenes: 1, total_runs: 67, updated_in_version: "v1.42"}`。
- `docs/doc_gate.py` 第 6 节重写：**从 `run_tests.ps1` 的 `$scenes` 数组机械读取当前数量**，与基线文件 `automated_scenes` 比对，不再把 65 写成永久常量；当前文档（README/开发进度/综合整改报告）必须含当前口径 `66 个自动化场景 + 主场景冒烟，共 67 RUN` 且不得残留 v1.41 旧口径；历史文档（复验文档/最终交付报告/已知问题）不得被机械改成 67 RUN（旧数量冻结）。
- `work/verify_doc_gate_negatives.py` 同步新口径：N1（旧总数 66 RUN）、N2（缺 66 自动化场景）、N3a（runner 67）、N3b（runner 65）全部被拦截，`NEG_ALL_PASS`。
- `doc_gate.py` 当前：`DOC_GATE PASS: 真实文件 SHA256 双向一致、文件集合双向相等、manifest 内部一致、状态文本一致、无控制字符、数量统一、stale=false`，退出码 0。

## 6. 未改生产代码、玩法、数值、场景、存档和发布包声明

本版本未修改任何生产脚本、玩法、数值、地图、UI、场景、GameState、存档版本（v21 不变）和发布包（exe/pck 不重建、manifest/SHA256 不变）。核对见第 0.4 节。

## 7. 纯文本输入与验收声明

DeepSeek V4 Flash 的开发输入与自动化验收全部为纯文本、源代码、JSON、TSV、本地资源路径、节点属性、命令、运行时状态与日志；本版本未请求、生成、引用或依赖任何截图、图片识别、屏幕录制、视觉附件或非文本上下文。

## 8. 批量工作、复核方式与未闭合缺口

- DeepSeek V4 Flash 实际执行：3 个 SWF 的字节级文本提取（含 2.2 的可终止导出与失败记录）、178 条注册表录入与 token 校验、v22 合同与决策文档编写、专项测试与 10 项真实变异负向、runner/基线/doc_gate 机械数量改造、全量回归与门禁运行。
- 复核方式：所有 confirmed token 由构建器在受控证据文本逐条精确命中后才写入；负向先证明变异生效再由同一验证器命中错误码；全量 runner、冒烟、doc_gate、release gate 独立门禁。
- 未闭合缺口：2.2 机制级、五个 partial 挑战的完整规则、恋爱/军团/答题完整规则（见第 0.2/0.3 节）。

## 9. 性能限制、已知问题、下一版本依赖

- 性能：提取/构建/门禁均在数秒至数分钟内完成；全量 runner 66 场景逐个 headless 运行。
- 已知问题：Markdown 样式警告（MD041/MD060 等）为历史既有，非功能性问题，不阻断。
- 下一版本依赖：v1.43 需从 v21 迁移 v22 并启用 revision/world_seed/adventurers/relationships/commission_state；首批 12 名固定冒险者档案（cohort=first）与 26 条 v1.43 计划任务已在本注册表预留。
