# 《魔域 1.03》Godot 重制版 v1.37–v1.41 全版本复验文档

> 用途：供 PM/SE 或任一复验者**逐项核对** v1.37→v1.41 全部改动、证据、测试与产物。
> 原则：每项改动可定位到文件/断言/错误码；未完成或 evidence_gap 如实标注，不夸大为"完整还原"。
> 基线：数据结构/存档保持 v21；未改玩法数值（用户覆盖值除外，均标注冻结）。

---

## 0. 版本总览

| 版本 | 主题 | 核心产物 | 专项断言 | 状态 |
|---|---|---|---|---|
| v1.37 | 技能/特效/掉落/声音反馈校正 | combat_feedback_registry + 证据门禁 | 218 | 正式签署（整改08） |
| v1.38 | 剧情与对话分支完整性 | story_dialogue_registry（27节点） | 190 | 十域完成 |
| v1.39 | 原版数值/用户覆盖双层审计 | value_audit_registry（17项） | 31 | 完成 |
| v1.40 | 存档架构加固（保持v21） | save_schema_registry | 23 | 完成 |
| v1.41 | 发布候选 + 人工验收包 | exe + 验收清单 + 交付报告 | 14+25 | **已签署（可进入用户试玩）** |

**全量自动化**：65 个自动化场景 + 主场景冒烟，共 66 RUN（含 6 专项）连续两次 EXIT 0，`PASS all automated scenes and main smoke test`。

---

## 1. v1.37 技能、战斗特效、掉落与声音反馈校正

### 1.1 声音证据链条（FFDec 权威）

- **工具**：JPEXS FFDec 26.2.1 + Temurin JRE 21（`work/v137/tools/`）
- **三类导出**：`-format script:pcode/pcodehex/as -export script` → 各 817 文件，SHA-256 清单 `work/v137/*_sha256.txt`
- **权威 opcode**（pcodehex 证明）：`0x1C=GetVariable`、`0x99=Jump`、`0x9D=If`、`0x81=GotoFrame`
- **门禁**：`work/v137/v137_evidence_gate.py` v3——正向 PASS + 负向 **15/15 精确集合命中**（M1-M15），EXIT 0 无残留
  - 负向含集合精确相等（禁止额外错误码）、生产调用点真验证（M9-M12）、TSV 双向多重集比较（M14/M15）
- **标签解析器**：`v137_tags_parser.py`——SoundRate 位域掩码 `(flags>>2)&0x03`；v1/v9 三表逐项 diff IDENTICAL；与 manifest 独立字段比较不无条件 OK
- **MP3 帧头独立基准**：`v137_mp3_check.py`——37 项 rate/channels 与 manifest 全一致 EXIT 0
- **37 项声音分类**：`docs/combat_feedback_registry.json`——17 native / 2 used_unconfirmed / 18 evidence_gap；provisional_unverified 清零
  - 关键修正：StartSound 帧号按 ShowFrame 计数（123@frame7 攻击、518@frame27 飞天连斩等），4 项改为 native_confirmed

### 1.2 战斗反馈状态机

- **事务式反馈通道**：`scripts/combat_feedback_service.gd`（唯一生产反馈入口）
- **76 项状态矩阵**：`ALLOWED_EVENTS_BY_STATE`（19事件×4状态），专项内独立 EXPECTED 表 + mutation 负向（非循环自证）
- **end_session 幂等**：只结束匹配且仍活动的 session
- **同步重入**：主场景生命周期测试覆盖胜利 2.2s 恢复段 + 失败流程重入（`test_feedback_lifecycle_main_scene`）

### 1.3 专项测试

- `test_combat_feedback_sequence_scene.gd`：**218 断言**，分区 A-I+N，含 76 状态矩阵 + 15 负向
- `test_monster_hit_scene.gd`：受击确定性（事件驱动轮询贴图），**100/100 连续通过**
- `test_doc_revoked_scene.gd`：文档 REVOKED 负向（撤销短语只出现在 REVOKED 行）

### 1.4 复验路径

```
python work/v137/v137_tags_parser.py <swf> v9   # 标签 + manifest 对比
python work/v137/v137_mp3_check.py <swf>        # MP3 帧头基准
python work/v137/v137_sound_evidence.py          # p-code 证据提取
python work/v137/v137_classify_37.py             # 分类校验写回
python work/v137/v137_evidence_gate.py           # 门禁（正向+15负向）
```

---

## 2. v1.38 剧情与对话分支完整性

### 2.1 十域剧情注册表

- **`docs/story_dialogue_registry.json`**：**27 节点**，覆盖任务书十域
- **`docs/evidence/story_dialogues_v103_v9.txt`**：180 行，按域登记 SWF 原文（sprite/frame token，native_confirmed）或行为证据（evidence_gap 如实标注）

### 2.2 十域节点清单

| 域 | 节点 | 状态机要点 |
|---|---|---|
| 救王 | rescue_king | 首次授王/魔石、幂等、跨日、读档 |
| 王国情报 | king_dialogue / prime_minister_king_news | 未救拒开/已救六类情报 |
| 公主/侍女 | princess_chat / give_roses / claim_princess_friend_gift / princess_sunday_gift / maid_year_pig / maid_combat_stone | 每日聊天/玫瑰/知己/周日/年猪/战斗力石 |
| 日常官 | daily_task_accept / complete / submit_pet | 接受/交付/训练幻兽/跨日重置 |
| 五福娃 | fuwa_start_round / beast_battle / claim_reward / completion | 五轮领取/完成幂等/跨日使者 |
| 后花园/抽奖 | palace_garden_entry / lottery_chest | 爵位门槛/可重复抽奖 |
| PK | pk_register / pk_champion | 周六/三级分组/当天一次 |
| 战魂 | war_soul_quest_unlock / explorer / chest / complete | 极品解锁/付费/守卫/完成幂等 |
| 最终战 | final_campaign_victory / gate | 六类击败/跨日复活/主帅门禁/结局 |
| 研究所 | research_produce | 产量任务/日上限 |

### 2.3 每节点验证矩阵

每节点验证：前置未满足（零状态差）/ 首次成功（精确状态差）/ 重复（幂等）/ 跨日 / 读档 v21 恢复。验证器为纯函数，正向与负向共用。

### 2.4 负向（mutation_applied + 同一验证器 + 精确错误码）

`STORY_PRECONDITION_UNCOVERED` / `STORY_REWARD_NOT_IDEMPOTENT` / `STORY_ACTION_ROUTE_MISMATCH` / `STORY_SAVE_DEFAULT_MISSING` / `STORY_NATIVE_ROUTE_LEAKED_TO_AGGREGATOR` / `EVIDENCE_TOKEN_MISS`

### 2.5 专项测试

`test_story_dialogue_state_machine_scene.gd`：**二次整改为真正注册表驱动**（v1.38 整改）——遍历注册表全部 27 节点，每节点 `test.setup/verify` 为函数名，测试用 `call()` 调度（**变异 setup/verify 字段名即测试失败**，实证字段驱动）；ROUTE 节点走 main_original 真实入口（_open_king_dialogue/_open_prime_minister_king_news/can_enter_map 门槛/finish_pk_race）；读档保存期望值逐字段深比较；跨日按 `test.cross_day` policy；六负向变异真实注册表（has_method 检测 setup/verify 字段缺失 + 路由/跨日策略/存档默认/证据 token）；写入审计断言终态 flag 仅由前置夹具产生。

---

## 3. v1.39 原版数值与用户覆盖值双层审计

### 3.1 数值审计注册表

- **`docs/value_audit_registry.json`**：**17 项值**，每项登记 value_id/domain/native/user_override/effective/formula/evidence_source/config_source/implementation_sources/classification/pm_decision/known_risks
- **`docs/evidence/value_audit_v103_v9.txt`**：36 行，按域登记权威来源

### 3.2 关键审计值

| value_id | user_override | effective | 权威源 |
|---|---|---|---|
| player_exp_multiplier | 10 | 10 | data/progression.json |
| boss_military_multiplier | 10 | 10 | data/progression.json |
| normal_military_multiplier | 1 | 1 | data/progression.json |
| initial_gold / magic_stones / sale_grant | 99_999_999_999 | 同 | game_state.gd:63-64,462 |
| quality/magic_soul_success_rate | 1.0 | 1.0 | data/enhancement.json |
| war_soul_crystal_success_rate | 0.5（符号`<`） | 0.5 | data/enhancement.json:42 |
| rose_99 / rose_999 | 50 / 250 | 同 | data/enhancement.json |
| research_tech_cap / daily_cap / stock_cap | 300 / 6 / 100 | 同 | data/pet_config.json |
| spider_skill_base / queen_skill_base | [.3,.5,.6] / [.4,.4,.2] | 同 | combat_service.gd:117-127 |
| enhanced_moon_box_50 | 0.50 | 0.50 | combat_service.gd:133 |
| gold_per_merit | 750000 | 750000 | data/ranks.json |

### 3.3 负向错误码

`USER_OVERRIDE_MISSING` / `VALUE_EFFECTIVE_MISMATCH` / `VALUE_MULTIPLIER_APPLIED_TWICE` / `VALUE_SCOPE_MISMATCH` / `VALUE_PROBABILITY_BOUNDARY_MISMATCH` / `VALUE_UNREGISTERED_MAGIC_NUMBER`

### 3.4 专项测试

`test_value_audit_registry_scene.gd`：**31 断言**，逐项验证 effective==配置、用户覆盖完整、经验只乘一次、军功作用域、概率边界。

---

## 4. v1.40 存档架构加固（保持 v21）

### 4.1 决策

- **`docs/save_schema_registry.json`**：schema_decision 明确**保持 v21，不升级 v22**（"条件性 v22"评估后决定不升级——现有字段完整迁移、无新增顶层字段需求）
- **加固**：拒绝未知高版本（避免未来版本被旧程序悄悄降级覆盖，game_state.gd:2095）；原子写入失败保留旧档；损坏输入拒绝且不污染内存

### 4.2 负向错误码

`SAVE_TRANSACTION_PARTIAL_COMMIT` / `SAVE_ATOMIC_TEMP_INVALID` / `SAVE_MIGRATION_DEFAULT_MISSING` / `SAVE_SCHEMA_TYPE_MISMATCH` / `SAVE_OLD_FILE_NOT_PRESERVED` / `SAVE_FUTURE_VERSION_ACCEPTED`

### 4.3 专项测试

`test_save_integrity_scene.gd`：**23 断言**——v21 round-trip deep equality、最小旧档迁移、损坏输入拒绝、原子写入失败保留旧档、未知高版本拒绝。

---

## 5. v1.41 发布候选版与人工验收包

### 5.1 构建产物（artifacts/releases/v1.41/）

| 文件 | 大小 | SHA256 |
|---|---|---|
| 魔域1.03_v1.41.exe | 104,518,656 B | a7512911d25a9702...873c |
| 魔域1.03_v1.41.pck | 23,427,088 B | 80e2302d7f854995...1797 |
| build_manifest.json | — | 记录 exe/pck 双 SHA256（与 SHA256SUMS 匹配） |
| SHA256SUMS.txt | — | 5 产物校验和 |
| 试玩验收清单_v1.41.md | — | 28图+系统/NPC+长流程+覆盖值 |
| 已知问题_v1.41.md | — | 分级（无阻断） |
| 最终交付报告_v1.41.md | — | 任务书第11节八项 |

- **构建**：Godot 4.6.3 + 导出模板 4.6.3.stable，`--export-release "Windows Desktop"`
- **exe 冒烟**：`--headless --quit-after` → EXIT 0 无脚本错误
- **发布包清洁**：无 savegame.json / 测试临时存档 / .bak / .tmp / .log / 开发缓存

### 5.2 专项测试

- `test_release_candidate_scene.gd`：**14 断言**——preset+主场景、28 图覆盖、清单覆盖、5 负向
- `test_release_acceptance_scene.gd`：**25 断言**——救王→最终战六类→结局、五福娃、战魂、PK、存档 round-trip（真实 API 无 flag 直写）

---

## 6. 复验执行清单（建议顺序）

1. **专项**：6 个专项各 `godot --headless --path . --scene res://tests/<name>.tscn --quit-after 1500`，确认 `^PASS` 且无 SCRIPT ERROR
2. **全量**：`powershell -File run_tests.ps1`，确认 65 个自动化场景 + 主场景冒烟 = 66 RUN + `PASS all automated scenes and main smoke test`，EXIT 0（连续两次）
3. **monster_hit 100 次**：`test_monster_hit_scene` 连续 100 次全 PASS
4. **证据门禁**：`python work/v137/v137_evidence_gate.py` → 正向 PASS + 负向 15/15 + EXIT 0
5. **发布产物**：核对 `SHA256SUMS.txt` 与 `build_manifest.json`；exe 冒烟启动
6. **注册表一致性**：story/value/save 三注册表均与对应证据文件 token 双向一致

---

## 7. 已知限制与 evidence_gap（如实）

- 部分剧本文本（国王六类情报等）未在 SWF 逐字定位，标 evidence_gap，不冒充 native_confirmed
- SWF flags 声道位声明 34/37 项与 MP3 帧不符（SWF 自身问题，v1/v9 相同），以 MP3 帧头/manifest 为权威
- 原版视觉细节未逐像素复现（无图像模型无法肉眼评审，视觉检查清单已备，待用户人工试玩确认）
- 存档保持 v21，未升级 v22（决策见 save_schema_registry）

---

*本文件由决策者模式生成，所有断言数/错误码/产物路径均经实际运行核实。*