# v1.32 复验通过记录

评审角色：项目经理 / 系统工程师  
复验日期：2026-07-28  
对应整改：`docs/tasks/v1.32_整改任务_01.md`  
最终结论：通过，准入下一阶段

## 1. 整改范围确认

资深开发工程师本次仅修改：

- `docs/swf_evidence_registry.json`
- `tests/test_swf_evidence_registry_scene.gd`

未修改活动玩法脚本、UI脚本、数值数据、场景、存档逻辑和测试场景数量。注册表版本仍为1.32，基线仍为v1.31，存档仍为v21，自动化测试仍为47项。

## 2. 事实复验

`image_0051` 已由错误的纯legacy改为 `active+legacy`，登记内容同时覆盖：

- 活动主场景中的雷角风牙兽。
- 活动主场景中的雷鸣大陆龙怪。
- `data/pets.json` 中的 `attack_defense_heavy` 图标字段。
- 隐藏旧 `main.gd` / `battle_panel.gd` 中的历史引用。

注册表同时记录：`pets.json` 的该图标字段会在幻兽面板运行时被 `image_0744` 覆盖。该差异作为证据缺口保留，没有擅自修改活动数据。

## 3. 独立数据质量检查

- UI、动画、共享角色、资源和地图五类主键重复数：0。
- image ID与文件basename不一致数：0。
- 非法scope数：0。
- 空 `known_uses` / `evidence_source` 数：0。
- 纯legacy资源活动引用泄漏数：0。
- 注册表地图数：28；`data/maps.json`地图数：28；集合和背景不一致数：0。
- `image_0051.scope`：`active+legacy`。

新增测试不是对单个ID写死判断，而是扫描活动 `scripts/*.gd` 与全部 `data/*.json`，排除明确登记的legacy脚本后检查所有纯legacy资源，因此可阻止同类错误再次出现。

## 4. 独立回归结果

执行 `run_tests.ps1`：

- 47个自动化测试场景全部通过。
- 主场景冒烟测试通过。
- 无 `SCRIPT ERROR`、非忽略项 `ERROR` 或 `FAILED`。

## 5. 准入决定

v1.32已满足：结构可用、事实纠正、引用完整性自动保护、无玩法/存档影响、全量回归通过。原 `v1.32_验收评审.md` 保留为首次评审记录，本文件为最终状态并取代其中“暂不准入”的结论。

批准发布v1.33实施任务。
