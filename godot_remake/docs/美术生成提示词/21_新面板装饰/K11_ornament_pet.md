# K11 面板装饰：幻兽终局边饰

- `asset_id`：`ornament_pet`
- 最终路径：`res://assets/expansion/ui_ornaments/ornament_pet.png`
- 最终规格：512×128 PNG，透明背景。
- 对应版本：v1.53。
- 状态：`optional_reserved`。它只能作为新面板的视觉装饰；不得修改、覆盖或伪造当前已签署原生 HUD/面板框。

## 可直接复制提示词

```text
生成一条原创复古暗黑奇幻 RPG 的横向 UI 面板装饰：金绿晶核、抽象翼兽石纹和深蓝细线构成的无文字横向面板边饰。

画面严格为 4:1 横向，适配 512×128。装饰只分布在左右端和上/下边缘，中央至少 55% 保持完全透明，供 Godot 运行时绘制真实文字、按钮和列表。禁止把任何文字、数字、按钮、进度条、可读符号或虚假 UI 烘焙到图片里。

风格：原创 2D 像素插画感，低饱和暗黑奇幻 MMORPG，细节克制、边缘清晰，不仿制已有游戏 UI。背景必须完全透明；若平台不能透明，使用完全均匀 #FF00FF 背景。

不要文字、数字、logo、水印、边框文字、人物、怪物、宠物、地图、完整场景、现代 GUI、写实摄影或 3D 渲染。
```

## 验收

中央透明区必须足够放置运行时 UI；不能用于覆盖当前原生 HUD 或替换既有面板边框。正式启用前需完成 UI 几何、层级与可访问性测试。

