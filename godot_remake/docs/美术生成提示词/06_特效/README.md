# 战斗与交互特效提示词

本目录的每个资产是一个静态特效帧或一个明确的短序列源图。除非文档明确要求序列，不要生成拼贴、多个阶段或复杂粒子动画。

统一规格：网页生成 1024×1024；最终单帧 96×96 PNG；透明背景优先，或 #FF00FF 抠图底。特效必须不包含人物、武器、文字、logo、UI 和地面阴影。

特效只在对应玩法签署并有明确 `effect_id`、触发路径、时长与取消策略后才导入生产。
