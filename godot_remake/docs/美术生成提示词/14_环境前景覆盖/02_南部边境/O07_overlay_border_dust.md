# O07 环境覆盖层：边境风尘层

- `asset_id`：`overlay_border_dust`
- 最终路径：`res://assets/expansion/overlays/overlay_border_dust.png`
- 最终规格：700×550 PNG，透明背景，地图前景/天气叠加层。
- 对应版本：v1.49。
- 状态：`optional_reserved`。不启用时不应影响地图、出口、交互和碰撞。

## 可直接复制提示词

```text
生成一张原创复古暗黑奇幻 RPG 地图前景或天气覆盖层：低饱和土黄细沙从左右边缘横向飘过，中心透明、不遮挡演员。

画面严格为横向 7:5，最终适配 700×550；它必须是一张透明背景的叠加层，而不是完整地图背景。覆盖元素只能贴近画布边缘或上/下缘，中央至少 55% 面积保持透明并可供运行时演员、战斗和交互叠加。

风格：原创 2D 像素插画感，复古暗黑奇幻 MMORPG；低饱和、层次克制、不要遮挡 HUD 或底栏。不要形成可点击物、出口、路牌、宝箱或角色。

背景必须完全透明；若平台不能透明，使用均匀 #FF00FF 背景且元素不得使用 #FF00FF。不要文字、数字、logo、水印、UI、玩家、NPC、宠物、怪物、Boss、地图主背景、完整建筑、地面、箭头、按钮、写实摄影、3D 渲染或已有游戏资产。
```

## 验收

导入前验证中央透明通行区、透明边缘和 7:5 尺寸；仅可作为不改变交互逻辑的视觉叠加层。

