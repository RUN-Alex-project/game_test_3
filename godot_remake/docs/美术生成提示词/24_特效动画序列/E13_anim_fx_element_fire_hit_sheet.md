# E13 特效动画序列：火元素命中

- `asset_id`：`anim_fx_element_fire_hit_sheet`
- 对应静态母版：`fx_element_fire_hit`（位于 `06_特效`）。
- 最终路径：`res://assets/expansion/effect_animations/fx_element_fire_hit_sheet.png`
- 最终规格：4×4 网格、每格 96×96、整张 384×384 PNG、透明背景。
- 对应版本：v1.44–v1.54（按静态母版对应版本）。
- 用途：可选逐帧特效；默认仍可仅使用静态母版。未获单独实现批准时，不得在 Godot 中引用本路径。
- 来源策略：`original_generated` 候选。

## 可直接复制提示词

```text
生成一张原创复古暗黑奇幻 RPG 的 4×4 逐帧特效精灵表，主题是“火元素命中”。核心动作：橙红火花由中心爆开后冷却缩小。
画面必须严格为 4 列×4 行、共 16 个等大格子；每格 96×96，整张 384×384，透明背景。帧序从左到右、从上到下：第 0 帧接近静止，第 1–5 帧开始与加速，第 6–10 帧达到主要形态，第 11–15 帧收束、淡出或回到可循环状态。不要额外帧，不要格线、帧编号或文字。
所有帧中的特效中心点保持一致，四周至少留 12% 透明安全边距；单帧能被独立裁切，连续播放时不跳位。色彩、轮廓与“fx_element_fire_hit”静态母版保持同一原创低饱和暗黑奇幻像素风。
输出透明 PNG；若不能透明，使用纯 #FF00FF 背景。不要角色、宠物、怪物、Boss、武器、地面、阴影、场景、UI、文字、数字、Logo、水印或可读符号。
```

## 验收

- 必须能严格切成 16 个 96×96 单元格；无跨格残留、格线或帧号。
- 逐帧预览应有真实连续变化，不能重复粘贴同一帧；若质量不达标，继续使用静态母版。
- 通过裁切与哈希登记后，才允许由独立动画任务决定帧率、循环和取消策略。

