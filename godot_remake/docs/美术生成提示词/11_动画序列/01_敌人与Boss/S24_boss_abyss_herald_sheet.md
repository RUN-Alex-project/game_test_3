# S24 动画精灵表预留：深渊使徒

- `asset_id`：`anim_boss_abyss_herald_sheet`
- 对应静态资产：`boss_abyss_herald`。
- 最终路径：`res://assets/expansion/animations/boss_abyss_herald_sheet.png`
- 规格：4×4 网格，每格 256×320，整表 1024×1280 PNG，透明背景。
- 行定义（从上至下）：第 1 行 待机；第 2 行 攻击；第 3 行 受击；第 4 行 退场。
- 帧定义（每行从左至右）：帧 0 为起始/中性；帧 1 为过渡；帧 2 为最大幅度；帧 3 为回到可循环或结束姿态。
- 状态：`optional_reserved`。未有正式动画时，生产必须使用静态待机图，不可引用此未来路径。

## 可直接复制提示词

```text
生成一张原创复古暗黑奇幻 RPG 的 Boss 动画精灵表，角色为“深渊使徒”。必须和已批准的静态基础演员完全一致：相同轮廓、配色、服装/甲壳、晶核、种族特征和朝向。

画面严格为 4 列×4 行，共 16 格的透明背景精灵表；每格尺寸相等，单格为 256×320。行从上至下依次表示：待机、攻击、受击、退场。每行从左至右是同一个动作连续的 4 帧：起始、过渡、最大幅度、回归。不要额外帧、不要不规则格子、不要跨格肢体、不要不同镜头角度。

构图：每一格中的同一角色脚底基线完全一致，主体高度和左右边界稳定；第 1、2、4 行首尾可自然衔接，第 3 行可在帧 3 回到待机。所有格子空白透明，角色不带地面、阴影、环境、文字或 UI。

风格：原创 2D 像素插画感，复古暗黑奇幻 MMORPG，低饱和、清晰轮廓。背景必须完全透明；若平台不能透明，全部背景使用均匀 #FF00FF，主体不可出现该颜色。

不要文字、数字、帧编号、格线、logo、水印、UI、多个角色、角色换装、角色变脸、地面、场景、写实摄影、3D 渲染或任何已有游戏资产。
```

## 导入前人工验收

1. 先把图切成 16 个单格，检查每格大小一致、脚底基线一致、无跨格残留。
2. 每行播放帧序必须具有肉眼可读的连续性；否则不要导入，继续使用静态图。
3. 动画时长、帧率、取消策略、效果路径和 manifest 均须由单独开发任务定义；本提示词不构成动画实现。
