# X08 对话表情源图：布兰，矿区领班

- `asset_id`：`portrait_adv_bran_mine_foreman_expressions`
- 最终路径：`res://assets/expansion/portraits/portrait_adv_bran_mine_foreman_expressions.png`
- 最终规格：384×96 PNG 精灵条，按从左至右四格裁成 96×96：`neutral`、`friendly`、`concerned`、`resolute`。
- 先决条件：必须先确认 `adv_bran_mine_foreman` 的基础肖像风格，再决定是否生成这一条。
- 来源策略：`original_generated_optional`。当前可先只使用基础肖像，不因缺少表情图阻塞玩法。

## 可直接复制提示词

```text
生成一张原创复古暗黑奇幻 RPG 对话头像表情条。角色为：壮年男性矿区领班，褐灰胡须、厚皮围裙、旧铜矿灯。

画面严格为横向 4 等分的四格头像精灵条，每格只画同一角色的头肩近景，角色身份、服装、发型、年龄、配色和视角必须完全一致。四格从左至右依次为：平静中性、温和友善、谨慎担忧、坚定决断。每格独立、脸部居中、头顶和肩部不被裁切；四格之间留极细但无文字的间隔，不能添加标签。

风格：原创 2D 像素插画感，复古暗黑奇幻 MMORPG 对话肖像；低饱和、清晰轮廓、面部表情可读。背景要求每格完全透明；如果不能透明，四格都使用完全均匀 #FF00FF 背景，不能有场景、阴影或渐变。

不要文字、数字、表情符号、logo、水印、UI 边框、其他人物、全身像、怪物、宠物、现代物件、写实摄影、3D 渲染或任何已有游戏角色。
```

## 裁切与验收

- 必须由人工裁为四张独立 96×96 PNG，并在 manifest 登记格序与哈希。
- 四格必须是同一角色，不允许换脸、换发型、换服装或左右镜像。
- 当前制作计划中属于增强资源；未导入时应继续使用基础头像，不可在代码中引用不存在路径。

