# B02 UI 图标：未读邮件封蜡

- `asset_id`：`ui_mail_unread`
- 最终路径：`res://assets/expansion/ui/ui_mail_unread.png`
- 最终规格：64×64 PNG，透明背景。
- 用途：未读状态、邮件列表角标。

## 可直接复制提示词

```text
生成一枚原创复古暗黑奇幻 RPG UI 状态图标：一枚完整圆形深酒红色封蜡，蜡印中央只有抽象的四角星压痕，不含字母、数字、徽章或真实世界标志；边缘有少量古铜粉末质感。

构图：单个小图标居中，正视，主体占画布约 68%，轮廓厚实清楚，缩小到 64×64 时仍可用作未读角标。

风格：原创 2D 像素插画感，复古暗黑奇幻 MMORPG，深酒红、暗红高光、少量古铜色，低饱和、清晰像素边缘。

背景必须完全透明；若平台不能透明，使用完全均匀 #FF00FF 背景，无阴影、渐变或纹理。不要在主体中使用 #FF00FF。

不要文字、数字、logo、水印、皇冠、信件、现代通知铃、额外物体、写实摄影或任何现有游戏标识。
```

## 验收

仅包含一枚无文字封蜡；不能与“成交印记”混淆，颜色必须更偏深酒红。
