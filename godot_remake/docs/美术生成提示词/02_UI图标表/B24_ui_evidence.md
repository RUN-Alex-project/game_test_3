# B24 UI 图标：调查证据

- `asset_id`：`ui_evidence`
- 最终路径：`res://assets/expansion/ui/ui_evidence.png`
- 最终规格：64×64 PNG，透明背景。
- 用途：调查点、线索和证据进度。

## 可直接复制提示词

```text
生成一枚原创复古暗黑奇幻 RPG UI 图标：一枚开裂的古铜放大镜，镜片内有抽象的三点微光，不含文字、纸张或真实符号。

构图：单个放大镜居中略倾斜，主体约占画布 70%，镜片和短柄在 64×64 下清楚。

风格：原创 2D 像素插画感，复古暗黑奇幻 MMORPG；古铜、冷青绿、深蓝灰，低饱和、清晰像素边缘。

背景必须透明；若不能透明，使用纯 #FF00FF 背景，无阴影、地面、渐变、反光或纹理，主体不得有 #FF00FF。

不要文字、数字、logo、水印、现代放大镜、侦探帽、地图、额外物体、写实摄影或任何已有游戏资产。
```

## 验收

镜片内不得出现文字或地图；三点微光只作抽象线索标识。
