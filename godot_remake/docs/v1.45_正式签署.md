# v1.45 正式签署

签署日期：2026-08-20
角色：本对话开发 + SE（用户授权双角色）
工程：E:\deepseek-work\TKS3_mod\godot_remake

## 结论

**正式签署 v1.45。** 当前基线从 v1.44 前进到 v1.45。未签署前不得开始 v1.46。

依据：
1. `python tools/run_tests.py` → registered=75 applicable=75 passed=75 failed=0 + 主场景冒烟（约 107s）
2. `python work/v145/verify_ranking_arena_negatives.py` → NEG_ALL_PASS（N1–N10 真实变异）
3. `python docs/doc_gate.py` → DOC_GATE PASS
4. 专项三场景、v1.44 相邻日结/皇宫/卡萨诺、启动隐藏 17 面板、bounds 16 块、存档 v22 均 PASS
5. 拒签条件核对：六榜从持久数据计算、同分 stable_id、练习赛不加分、挑战赛胜利才改分、取消/ 切图 abandon 不结算、快照冻结、无新卡萨诺演员、代理节点不进 interactive_actors、无 `set_combat_power` / `transfer_item_to_npc`

## 不作为签署条件

- Linux 全量
- exe/pck 重建与 release gate
- git commit（用户未要求提交）
- 用户试玩（用户声明 v1.55 前不游玩，中间版由本对话门禁评审）

## 下一版

v1.46 可在本签署之后开始；仍一次一个版本。本批节奏：v1.45 → v1.46 → v1.47，签完 v1.47 再问用户。
