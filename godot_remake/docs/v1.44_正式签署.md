# v1.44 正式签署

签署日期：2026-08-20
角色：本对话开发 + SE（用户授权双角色）
工程：E:\deepseek-work\TKS3_mod\godot_remake

## 结论

**正式签署 v1.44。** 当前基线从 v1.43 前进到 v1.44。未签署前不得开始 v1.45。

依据：
1. `python tools/run_tests.py` → registered=72 applicable=72 passed=72 failed=0 + 主场景冒烟（约 120s）
2. `python work/v144/verify_day_mail_market_negatives.py` → NEG_ALL_PASS（10 项真实变异）
3. `python docs/doc_gate.py` → DOC_GATE PASS
4. 专项三场景、v1.43 相邻、启动隐藏 15 面板、存档 v22 均 PASS
5. 拒签条件核对：日结无 `randi`、同日不重复结算、邮件/交易不复制物品、背包满附件保留、无 `transfer_item_to_npc`、无新卡萨诺演员、UI 不直接写账本

## 不作为签署条件

- Linux 全量
- exe/pck 重建与 release gate
- git commit（用户未要求提交）
- 用户试玩（用户声明 v1.55 前不游玩，中间版由本对话门禁评审）

## 下一版

v1.45 可在本签署之后开始；仍一次一个版本。
