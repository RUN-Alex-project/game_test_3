## v1.55（2026-08-21，平衡长测与发布候选）

- 存档保持 v22；不改用户冻结覆盖值。
- 10 条长流程 + 20 项负向；重建发布包并跑 release gate。
- 自动化场景 89->90, RUN 90->91.

## v1.54（2026-08-21，十四日赛季与终局后事件）

- 存档保持 v22：扩展既有 expansion_state.season（保留 arena_seed_state）。
- 14 日赛季 + 两套主题 + 5 源委托 + 8 个终局后 NPC 事件；入口日常任务官「赛季簿」。
- 自动化场景 88->89, RUN 89->90. 未重建发布包。

## v1.53（2026-08-21，幻兽终局与收藏循环）

- 存档保持 v22：启用 expansion_state.pet_endgame。
- 收藏册/支援槽/试炼/研究所委托；入口研究所「幻兽终局」。
- 自动化场景 87->88, RUN 88->89. 未重建发布包。

## v1.52（2026-08-21，九大挑战与战士技能成长）

- 存档保持 v22：启用 expansion_state.challenges / warrior_mastery / equipment_mastery。
- 9 挑战 + 4 精通 + 装备槽；入口 PK 官「挑战簿」。
- 自动化场景 86->87, RUN 87->88. 未重建发布包。

## v1.51（2026-08-21，黑暗深渊与图腾终局）

- 存档保持 v22：启用 expansion_state.chapters.abyss_finale。
- 五图 + 3 回响 + 3 图腾 + 最终战/epilogue；入口国王「深渊终局」。
- 自动化场景 85->86, RUN 86->87. 未重建发布包。

## v1.50（2026-08-21，冰原与元素试炼）

- 存档保持 v22：启用 expansion_state.chapters.ice_element。
- 五图 + fire/ice/thunder + 2 Boss + weekly；入口研究所「冰原试炼」。
- 自动化场景 83->85, RUN 84->86. 未重建发布包。

## v1.49（2026-08-21，南部城邦与边境战线）

- 存档保持 v22：启用 expansion_state.chapters.south_border。
- 六图 + 侦察/补给/守备/反攻 + 周常；入口皇宫元帅「南部城邦」。
- 自动化场景 81→83，总 RUN 82→84。未重建发布包。

## v1.48（2026-08-21，树心与港口篇章）

- 存档保持 v22：启用 expansion_state.chapters.treeheart_harbor。
- 六图 + 七阶段 + 两分支 + 两 Boss走真实战斗；入口日常任务官「树心郊区」。
- 自动化场景 79→81，总 RUN 80→82。未重建发布包。

## v1.47（2026-08-21，产业城堡与领地经营）

- 存档保持 v22：启用 expansion_state.properties 与 territory_economy。
- 六领地日结产出/事件/委任与城堡升级走服务层；入口为领地报名官「领地与城堡」。
- 自动化场景 77→79，总 RUN 78→80。未重建发布包。

## v1.46（2026-08-20，商会/拍卖）

- 存档保持 v22：启用 expansion_state.market 与 auction。
- GuildMarketService 不覆盖 MarketService；入口收藏家/PK 官。
- 自动化场景 75→77，总 RUN 76→78。未重建发布包。

## v1.45（2026-08-20，排行榜与异步擂台）

- 存档保持 v22：启用 expansion_state.rankings 与 season.arena_seed_state。
- 六榜与异步擂台挂在现有 PK 官；不新增卡萨诺演员；擂台代理不进 interactive_actors。
- 自动化场景 72→75，总 RUN 73→76。未重建发布包。

## v1.44（2026-08-20，固定冒险者 II）

- 存档保持 v22：启用 day_sequence / mailbox / economy.adventurer_ledgers / daily_settlement_log。
- 日结挂在 GameState.advance_day；邮件与直接交易走账本，不新增卡萨诺演员。
- 自动化场景 69→72，总 RUN 70→73。未重建发布包。

## v1.43（2026-08-20，固定冒险者 I）

- v21→v22：顶层新增 expansion_state；12 名首批固定冒险者名册/关系/六类好友委托。
- 入口在卡萨诺日常任务官对话「冒险者公告板」，不新增地图演员。
- 自动化场景 66→69，总 RUN 67→70。

## v1.42（2026-08-13，内容证据与扩展数据合同，开发完成待复验）

- 2.2/2.3/2.4 可重跑纯文本反查（work/v142/extract_swf_text.py，原始字节扫描，三版本 exit 0；2.2 的 FFDec 动作码导出失败但字节扫描稳定成功）。
- 新增 docs/moyu_23_24_content_registry.json（178 条：22 地图 / 72 任务 / 12 首批 + 24 候补冒险者 / 22 物品 / 20 技能Boss / 6 系统；108 confirmed / 5 partial / 65 singleplayer_extension），docs/moyu_23_24_evidence.md、docs/expansion_data_contract_v22.json（NOT_ENABLED）、docs/expansion_content_decisions.md、docs/current_test_baseline.json。
- 新增专项 tests/test_moyu_expansion_registry_scene.{gd,tscn}（加入 runner，自动化场景 65→66，总 RUN 66→67）+ work/v142/verify_registry_negatives.py（10 项真实变异负向）。
- docs/doc_gate.py 机械读取 runner 数量 + 唯一测试基线，不再硬编码 65；历史数量冻结。
- 未改生产代码/数值/场景/存档(v21)/发布包；发布包不重建。

## v1.37 整改08（2026-08-02，已实施待复验）

- **整改07 拒签两项已修复**（详见版本改动.md 整改08 节）：①production_event 必须映射本条目 sound_id（M13 负向：事件名+调用点同时改他声音精确命中）；②TSV 多重集 Counter 比较 + 重复 sound_id 行检测（M14/M15 负向精确命中）。
- 门禁正向 PASS + 负向 **15/15 精确集合** + EXIT 0 无残留；classify OK；专项×3 PASS；monster_hit 100/100；全量 ×2（55 RUN EXIT 0）。
- 未开始 v1.38。

> **REVOKED：待综合整改复验。**

- SE/PM 额度用尽，开发兼决策者自查。v1.37 已正式签署，v1.38 起步。
- 新增 `docs/evidence/story_dialogues_v103_v9.txt` + `docs/story_dialogue_registry.json`（救王+王国情报三节点规则与证据分类）+ `tests/test_story_dialogue_state_machine_scene.gd`（数据驱动，35 断言，六项负向）。
- 救王：首次授王爵位/20万魔石分支、重复幂等、跨日不重置、v21 读档恢复；国王对话未救拒开+已救六类情报走真实 dispatcher。
- 相邻回归 12 场景 PASS；全量 56 RUN 连续两次 EXIT 0。
- **范围声明**：本阶段覆盖救王+王国情报域；其余域（公主/侍女/后花园/日常官/PK/五福娃/战魂/最终战/研究所）v1.38 状态机闭环待后续阶段，现有实现与测试已存在但未在 v1.38 框架内逐节点登记。
- 未开始 v1.39。

> **REVOKED：待综合整改复验。**

- 安装 Godot 4.6.3 导出模板，实际构建 exe（100MB）+ pck（22MB），冒烟 EXIT 0；新增长流程验收专项（救王→最终战→结局 + 五福娃 + 战魂 + PK + 存档，真实 API 无 flag 直写）；全量回归 ×2 EXIT 0 EXIT 0。未开始 v1.42。

## v1.41（2026-08-08，决策者模式，发布候选版与人工验收包）

- 建立 export_presets.cfg（Windows）+ artifacts/releases/v1.41/（验收清单/已知问题/manifest/SHA256）+ RC 专项测试 + 五负向；全量 59 RUN ×2 EXIT 0。exe 待导出模板就绪后构建。未开始 v1.42。

> **REVOKED：待综合整改复验。**

- schema 决策：保持 v21（v1.36-39 未新增顶层持久字段）；修复 load_game 拒绝未来版本；建立 save_schema_registry.json（26 字段）+ 专项完整性测试 + 六负向；相邻 16 场景 + 全量 58 RUN ×2 全 PASS。未开始 v1.41。

> **REVOKED：待综合整改复验。**

- 建立 value_audit_registry.json（17 项值）+ value_audit_v103_v9.txt 证据；专项一致性测试 + 六负向；相邻 13 场景 + 全量 57 RUN ×2 全 PASS。未改数值生效值/存档 v21；未开始 v1.40。

## v1.38 七次整改（2026-08-08，决策者模式，cross_day 真实日期API + 测试隔离）

- PM/SE 七次复验拒签 5 项返工：cross_day 执行阶段用真实日期 API（advance_day 推进，不直接写 current_day/story_flags，不调 prepare）；precondition_hook 由 run_registry_case 调度；N8 差分到达读档（变异 action+cross_day=daily+匹配 verify+缺字段→命中 SAVE_DEFAULT）；测试隔离（唯一 user:// 路径 + finally 清理，无存档残留）；maid_combat_stone cross_day 修正 daily。专项 PASS + 全量回归 ×2 EXIT 0。v1.39/40/41 整改待后续。等待 v1.38 签署，不开始 v1.39。

## v1.38 六次整改（2026-08-08，决策者模式，registry 全贯穿 precondition/repeat + cross_day 分离）

- PM/SE 六次复验拒签 5 项返工：cross_day 执行与夹具真正分离（revive 夹具移入 prepare，执行阶段只调 API）；N8 到达读档（变异 action+匹配 verify+缺默认字段→命中 STORY_SAVE_DEFAULT_MISSING）；N7/N8 差分断言（先验证原始成功再变异）；registry 贯穿 precondition/repeat（repeat_guard/precondition_hook 字段，变异逃逸被捕获）；口径同步 N1-N8。save failed 分歧确认本环境清理残留后不复现。专项 PASS + 相邻 12 场景 + 全量回归 ×2 EXIT 0。等待 v1.38 签署，不开始 v1.39。

## v1.38 五次整改（2026-08-08，决策者模式，registry 全贯穿 + N6 走统一执行器）

- PM/SE 五次复验拒签 7 项返工：registry 全贯穿（_setup/_cross_day/_save_load 全部显式接收 registry，从副本读 action/setup/cross_day，不再读全局 _nodes）；N6 变异副本经 run_registry_case 命中 STORY_TOKEN_MISS；cross_day 准备/执行分离；新增逃逸 N7（合法 setup 变异影响结果）/N8（合法 action 变异读档用副本）；写入审计扫描覆盖 _dispatch/_dispatch_main。专项 PASS + 相邻 12 场景 + 全量回归 ×2 EXIT 0。等待 v1.38 签署，不开始 v1.39。

## v1.38 四次整改（2026-08-08，决策者模式，统一 run_registry_case + 全函数写入审计）

- PM/SE 四次复验拒签 2 项返工：建立统一 `run_registry_case(registry, node_id)`（正向遍历与六负向共用，负向全部传入变异注册表副本命中精确错误码）；写入审计改全函数源码扫描（覆盖 _ready/_run_node/_dispatch/_cross_day，主流程禁写终态 flag，setup/precondition 视为夹具）。逃逸（变异 setup 字段 → run_registry_case 命中）被捕获。专项 PASS + 相邻 12 场景 + 全量回归 ×2 EXIT 0。等待 v1.38 签署，不开始 v1.39。

## v1.38 三次整改（2026-08-08，决策者模式，写入审计+负向同验证器+token按段+真实地图入口）

- PM/SE 三次复验拒签 4 项全部返工：写入审计改源码正则扫描（非手工自证）；六负向复制变异真实注册表走同一验证器；token 加 domain 按证据段验证；后花园/能量塔走 _travel_to 真实地图 dispatcher + verify 检查真实 current_map_id。负向逃逸 4 项（错段 token/none→daily/入口伪造成不切地图）全被捕获。专项 PASS + 相邻 12 场景 + 全量回归 ×2 EXIT 0。等待 v1.38 签署，不开始 v1.39。

## v1.38 二次整改（2026-08-08，决策者模式，真正注册表驱动）

- PM/SE 二次复验拒签 7 项（setup/verify 未驱动、ROUTE 伪占位、读档只查非null、cross_day 硬编码、负向纯函数、token 兜底、无写入审计）全部返工：setup/verify 改唯一函数名 + call() 调度（变异即失败）；ROUTE 走真实 main_original 入口；读档深比较；cross_day 按 policy；六负向 has_method 变异；token 严格；写入审计。专项 PASS + 相邻 12 场景 + 全量回归 ×2 EXIT 0。等待 v1.38 签署，不开始 v1.39。

## v1.38 整改（2026-08-08，决策者模式，真正数据驱动）

- PM/SE 复验拒签 v1.38：原测试为手写分段非数据驱动，仅查节点名称，存在直接写终态 flag。本整改重写为真正数据驱动：注册表 27 节点补 test 元数据，测试遍历注册表每节点执行前置/首次/重复/跨日/读档五验证，双向集合 27==27，六负向变异注册表走同一验证器。专项 PASS + 相邻 12 场景 + 全量回归 ×2 EXIT 0。等待 v1.38 签署，不开始 v1.39。

## v1.38 第八阶段（2026-08-07，决策者模式，最终战/研究所域，十域收官）

- v1.38 十域全部完成：救王/王国情报/公主侍女/日常官/五福娃/后花园抽奖/PK/战魂/最终战/研究所（story_dialogue_registry.json 27 节点 / 数据驱动测试 / 证据 180 行）。相邻 12 场景 + 全量 56 RUN ×2 全 PASS。未开始 v1.39。

## v1.38 第七阶段（2026-08-07，决策者模式，战魂域）

- story_dialogue_registry.json 20→24 节点（新增战魂 4 节点）；story_dialogues 证据追加战魂域；专项测试扩展战魂正向+负向；相邻 12 场景 + 全量 56 RUN ×2 全 PASS。未开始 v1.39。

## v1.38 第六阶段（2026-08-07，决策者模式，PK 域）

- story_dialogue_registry.json 18→20 节点（新增 PK 2 节点）；story_dialogues 证据追加 PK 域；专项测试扩展 PK 正向+负向；相邻 12 场景 + 全量 56 RUN ×2 全 PASS。未开始 v1.39。

## v1.38 第五阶段（2026-08-07，决策者模式，后花园/抽奖域）

- story_dialogue_registry.json 16→18 节点（新增后花园/抽奖 2 节点）；story_dialogues 证据追加后花园/抽奖域；专项测试扩展后花园/抽奖正向+负向；相邻 12 场景 + 全量 56 RUN ×2 全 PASS。未开始 v1.39。

## v1.38 第四阶段（2026-08-07，决策者模式，五福娃域）

- story_dialogue_registry.json 12→16 节点（新增五福娃 4 节点）；story_dialogues 证据追加五福娃域；专项测试扩展五福娃正向+负向；相邻 12 场景 + 全量 56 RUN ×2 全 PASS。未开始 v1.39。

## v1.38 第三阶段（2026-08-07，决策者模式，日常官域）

- story_dialogue_registry.json 9→12 节点（新增日常官 3 节点）；story_dialogues 证据追加日常官域；专项测试扩展日常官正向+负向；相邻 12 场景 + 全量 56 RUN ×2 全 PASS。未开始 v1.39。

## v1.38 第二阶段（2026-08-07，决策者模式，公主/侍女域）

- story_dialogue_registry.json 3→9 节点（新增公主/侍女 6 节点）；story_dialogues 证据追加公主/侍女域；专项测试扩展公主/侍女正向+负向；相邻 12 场景 + 全量 56 RUN ×2 全 PASS。未开始 v1.39。

## v1.37 整改07（2026-08-02，已实施待复验）

- **整改06 拒签四项已修复**（详见版本改动.md 整改07 节）：①负向验证错误集合**精确相等**（禁止额外错误码）——12/12 精确集合命中；②生产调用点真验证（事件名登记/文件/行号/调用/声音一致性 + M9-M12 变异）；③TSV 双向完整字段比较；④链检查分档（same_function 6 项强验证 + separated_bind_play 7 项如实降级措辞"绑定-播放关联"）。
- 回归：门禁正向 PASS + 负向 12/12 + EXIT 0 无残留；classify OK；专项×3 PASS；monster_hit 100/100；全量 ×2（55 RUN EXIT 0）。
- 未开始 v1.38。

## v1.37 整改06（2026-08-02，已实施待复验）

- **整改05 拒签四项阻断已全部修复**（详见版本改动.md 整改06 节）：标签解析器位域掩码 + v1/v9 双输出逐项 diff IDENTICAL + manifest 对比不无条件 OK；证据门禁本地临时目录/finally 清理 + 正向 PASS 负向 8/8 + EXIT 0 无残留；分类脚本 tsv 重建 pcode_evidence + 逐项规则校验 + 保留 evidence_source URI；handler 从 pcode 内容提取。
- **帧号修正（重大发现）**：StartSound 帧号从 FrameLabel 序号计数改为 ShowFrame 计数（与 FFDec/labels.tsv 一致）——123@frame7 攻击帧、132@frame6 攻击帧、518@frame27 飞天连斩、520@frame63 星魔剑——4 项修正为 native_confirmed；1196 皇宫帧、502 风斩帧。
- **最终分类**：17 native / 2 used_unconfirmed / 18 evidence_gap；provisional_unverified 0 残留。
- 未开始 v1.38。

## v1.37 整改05（2026-08-02，已实施待复验）

- **REVOKED 声明（整改05，见版本改动.md 整改05 节）**：①"FFDec CLI 无 p-code 导出"错误——PM/SE 独立复验证实 `-format script:pcode -export script` 可导出 817 个 .pcode（EXIT 0，约 1.09 MB），本工程整改05 已实际执行三类导出并记录 SHA-256；②"0x1C=Jump" 等标准 opcode 表错误——FFDec pcodehex 权威证明 0x1C=GetVariable、0x99=Jump、0x9D=If、0x81=GotoFrame；③v5 的 313/10/0/23 仅代表自制解析器失败，不得描述为原 SWF 不标准；④"76 项状态矩阵已独立证明"撤回——原测试读取生产常量，属循环自证。
- 停止自研完整 ActionScript 解析器（v3/v4/v5 保留为失败历史证据，禁止 v6）；FFDec p-code 作为 attachSound/start/事件处理器/调用链的权威结构证据。
- 37 项声音分类按 p-code 证据逐项重建（native_confirmed / used_unconfirmed / evidence_gap / unrelated），注册表不残留 provisional_unverified。
- 76 项状态矩阵返工：测试内独立固化 EXPECTED_ALLOWED_EVENTS_BY_STATE，禁止读取生产常量；mutation 篡改生产表必须命中 STATE_MATRIX_MISMATCH。
- 同步重入补强：胜利重入后等待 2.2s 恢复段、新增失败流程同步重入、断言新 session/Controller session/目标怪物/feedback state。
- 未开始 v1.38。

## v1.37 整改03（2026-08-02，已实施待复验）

- REVOKED 声明（见版本改动.md 整改03 节）：58 处 attachSound / 54503 action / 46 码完整 / hitsb label / v2 独立验证 / 0x8E GotoLabel / 历史分类均已撤销。
- 解析器 v4 按 SWF v19 重写（数据裁决码表）；真实 SWF 审计：unknown=0、push_kinds=0、nested=0、truncated=1（Button2 root frame 4 布局差异，如实缺口）。
- 战斗状态机（FEEDBACK_BATTLE_STATE_MISMATCH）+ per_item operation id（FEEDBACK_OPERATION_ID_MISSING）+ end_session 幂等（expected_session_id）。
- 主场景生命周期集成测试 + 专项 N11-N13；未开始 v1.38。

# 《魔域 1.03》Godot 重制版开发进度

> 2026-07-18 根据当前工程和测试恢复。完成状态仅代表已有可运行实现，不代表与原 SWF 已达到逐像素、逐数值完全一致。

## 版本状态（2026-08-08 综合复验后）

> - **v1.37：已签署**
> - **v1.38：待综合整改复验**（precondition 统一调度 + N8 按序 + finally + cross_day 真实 API 已实施，专项/全量 PASS）
> - **v1.39：待复验**（registry↔production 双向 11 项已实施）
> - **v1.40：待整改**（原子存档未实施）
> - **v1.41：待整改**（发布包重构建未实施）
> - 未开始 v1.42，未发布新包。

## 当前里程碑

| 里程碑 | 状态 | 说明 |
|---|---|---|
| 1. 工程与素材提取 | 已完成 | Godot 4.6、700×550、原 SWF 图片/声音清单 |
| 2. 原版基础 UI | 第八轮完成 | 原版顶部人物/双幻兽/地图卡、固定底栏、背包/仓库、三套商店、NPC对话、人物装备窗及SWF原坐标场景边缘出口 |
| 3. 地图与场景战斗 | 第五轮完成 | 可移动、点击怪物原地开战、原版80×8血条、左右键攻击、原版怪物分布、掉落、Boss动作与死亡反馈 |
| 4. 装备养成 | 已完成 | 品质、魔魂、战魂、天魂、地魂及战斗力计算 |
| 5. 幻兽系统 | 第二轮完成 | 属性、等级、出征与合体/解体、伤害代理、幻化、经验球、研究所与产量任务 |
| 6. 人物成长 | 已完成一轮 | 等级经验、军衔、爵位、亲密度、关系技能 |
| 7. 日常和活动 | 已完成一轮 | 每日任务、地下城、冰雪精英、PK 赛、地图占领 |
| 8. 存档与容错 | 已完成一轮 | JSON v21、幻兽合体/人物体力、随机掉落装备/六栏装备/五福娃/当天时间/矿石品质/PK参赛/战魂谜宫/最终战状态、旧档迁移、拖放和交易回滚 |
| 9. 原版音效与动画 | 部分完成 | 37 段音效、玩家原版待机/四方向行走、攻击、怪物受击/死亡；仍有大量 SWF 精灵待还原 |
| 10. 主线剧情和 NPC | 第四批完成 | 已完成国王救援、皇宫剧情、六段最终战役、结局评价与五福娃事件 |

## 当前可验证测试

- 背包/存档、战斗、持久生命、场景战斗、日常任务官、皇宫、地图占领。
- 世界地图/装备、幻兽逻辑、幻兽 UI、人物成长、关系、任务、技能。
- 主场景、原版 UI、装备强化，以及主场景启动冒烟测试。
- 最近一次回归覆盖72个自动化测试场景，连同主场景冒烟测试全部通过（共 73 RUN）；新增 v1.42 内容证据注册表专项测试（docs/moyu_23_24_content_registry.json），既有背包/仓库、商店、场景入口、顶部/底部UI、战斗、剧情、养成、掉落与注册表测试继续通过。
- 当前测试口径：**90 个自动化场景 + 主场景冒烟，共 91 RUN**（数量由 docs/doc_gate.py 从 run_tests.ps1 的 $scenes 数组机械读取，基线见 docs/current_test_baseline.json；历史版本数量冻结不机械替换）。

## 已完成：主线第一批

1. 地下城三层首领首次击败后触发救出国王，并写入永久剧情状态。
2. 按原版规则发放救王奖励：未达到“王”则授予“王”，已经是“王”则获得 200,000 魔石；奖励不可重复。
3. 国王获救后回到皇宫并提供最终战争六类敌军情报。
4. 冰宫到雪域边境恢复“星期五临时进入 / 救王后永久开放”门槛。
5. 皇宫补首相 NPC：金币捐献入口、功勋查询、国王消息与爵位说明。
6. 公主补每日聊天、关系阶段对白和幻兽赠送；侍女补 5,888 魔石购买年猪事件。
7. 剧情状态、奖励幂等性、存档迁移和地图门槛均有自动化测试。

## 已完成：最终战役

1. 按 SWF 第 22～27 帧还原六段战线：雪域边境、魔中军阵地、魔左军阵地、魔右军阵地、能量塔禁地、魔军帅旗；出口方向、背景位图和敌人分布已有精确断言。
2. 中军放守卫军和突击队，左军放图腾兽，右军放神秘部队，帅旗放主帅，能量塔放“魔的能量”；旧存档的 `demon_camp` 自动落在中军。
3. 突击队、守卫军、神秘部队、图腾兽存活时，分别使所有最终战役敌人的攻击、防御、生命、战斗力乘以 1.5；消灭对应部队立即取消该项增益。
4. 魔军主帅存活时禁止从帅旗进入能量塔禁地；击败后开放最终地图和“魔的能量”战斗。
5. 国王获救后，只要当天有魔军被消灭且魔的能量仍存在，进入新的一天便按原版逻辑复活六类魔军并恢复四项增益。
6. 摧毁魔的能量写入主线胜利状态，之后不再跨日复活；战役状态随 JSON v14 存档迁移，并立即显示原版第 37 帧的游戏结束评价内容。
7. 结局按总战斗力、等级、装备、幻兽、军衔、爵位、关系和财富计算分项及综合评价，保留原版最高评价计数等于 7 的特殊行为。
8. 战魂已激活时，四支支援部队按 25% 获得战魂之心、否则获得战魂晶石；主帅固定获得战魂之心。
9. SWF 中五类军队的原始等级、生命、攻击区间和防御数据保留在怪物数据的 `original_*` 字段；实际战斗攻击/防御按当前重制版战斗曲线归一化。
10. 最终战与结局专项测试覆盖地图图、背景、分布、增益、掉落、复活、门禁、评价阈值和 v14 存档；完整 20 场景回归和主场景冒烟测试通过。

## 已完成：雷鸣矿洞与挖矿循环

1. 雷鸣大陆新增原版雷鸣矿洞入口，矿洞使用 SWF 第 9 帧的 `image_1091` 背景和四个矿点坐标。
2. 每次挖矿消耗 1 点当天时间，品质为 1～10；随机边界 `>100` 出金矿、否则银矿。
3. 每天共 15 点时间，耗尽后统一触发已有的跨日恢复、任务重置和最终战魔军复活。
4. 银矿只卖杂货商换金币，金矿只卖收藏家换魔石；品质 10 的特殊价格按原版分别为 500,000 金币和 280 魔石。
5. 矿石是带品质的独立背包实例，v15 存档保存品质与当天时间，并兼容旧档。
6. 后花园、抽奖房、PK赛场2/3和战魂封印谜宫已在 v1.6～v1.8 补齐；此前明确列出的缺失地图现已补完，后续继续逐帧审计尚未还原的 NPC、剧情和动画。

## 已完成：草原五福娃与蜘蛛分布校正

1. 草原/草原2按 SWF 第 10～11 帧恢复为五福娃隐藏事件，删除无原版依据的蜘蛛洞穴；旧 `spider_cave` 存档自动迁回卡萨诺城。
2. 2008 奥运使者按每日 1～9 随机位置出现；接受后进入草原，击败雷角风牙兽，前往草原2领取当前福娃礼物，共五轮。
3. 贝贝、欢欢、迎迎、妮妮、晶晶及三类随机礼物、雷角风牙兽互斥掉落、树心城 100,000 魔石/VIP 最终奖励均已实现并存档。
4. 蜘蛛位于迷梦沼泽，蜘蛛王后艾达位于亚维特岛；地下城一层由星期日日常任务官进入并返回卡萨诺城。
5. 七张早期串位背景已按根时间轴位图引用校正；专项测试对草原、树心城、亚维特、雷鸣、戈壁、地下城、深渊和 PK 场背景建立精确断言。
6. 三张 700×550 真实渲染图已生成并校验尺寸；当前工具限制下尚未人工目视评审。

## 已完成：后花园与抽奖房

1. 按 SWF 第13～15帧确认后花园与抽奖房是两条独立入口：皇宫方向出口进入后花园，抽奖官直接传送到抽奖房。
2. 后花园要求爵位至少达到勋爵；公主、丫环2、丫环1使用原始素材和坐标，分别接入公主关系、5,888魔石年猪与每日2,800魔石高级战斗力石。
3. 抽奖房恢复8个宝箱原始坐标与`image_1213`关闭帧；每次固定扣28魔石并消耗2点时间，可重复抽取，无每日次数限制。
4. 奖池按根脚本恢复为2%、5%、38%、55%四档；宝箱颜色变量不参与概率或奖池计算，八个宝箱机制相同。
5. 奖励覆盖噜噜幻兽、圣天使幻兽、8/12星奇异兽、技能书、经验球、玫瑰、稀有材料与随机六类装备；随机装备保存品质、魔魂与孔洞数。
6. 装备栏扩展为武器、头盔、项链、衣服、手镯、战靴；存档升级为v17，专项测试覆盖四档边界、资源消耗、地图门槛、交易重置和孔洞回读。
7. 完整23场景回归与主场景冒烟测试通过；两张700×550真实OpenGL截图已生成并验证尺寸，当前工具限制下未做人工目视评审。

## 已完成：三组 PK 赛场

1. 按 SWF 第31～33帧把早期合并在一张图内的三名擂主拆为“PK赛场1层、2层、3层”；三图均使用原始背景`image_1294`，各自只保留对应等级组擂主。
2. 皇宫不再显示可直接行走进入的 PK 方向出口，只能点击 PK 赛报名官进入；三个赛场的原版出口都返回卡萨诺城。
3. 恢复星期六报名门槛和自动分组：人物等级`<=60`进入1层，`61～100`进入2层，`>100`进入3层；报名时即消耗当天资格，失败、撤退或离场后当天不能再次报名。
4. 恢复三组原版奖励：60级组为高级飞天连斩、40,000基础经验、27,000魔石；100级组另有月光宝盒增强版、150,000基础经验、56,000魔石；最高组为高级斗志昂扬、月光宝盒增强版、电浆药水、999朵白玫瑰、250,000基础经验、82,800魔石。人物经验继续按此前修改要求结算10倍。
5. 三名擂主使用 SWF 坐标`(539.7,218.85)`、`(479.7,197.85)`、`(403.6,159.45)`；战斗结束后本次赛场状态关闭，旧存档若停在赛场但没有有效参赛状态会迁回卡萨诺城。
6. 存档升级为v18，保存当天 PK 资格与进行状态；新增专项测试后完整回归为24个场景加主场景冒烟测试。
7. 使用真实 OpenGL 3.3 / NVIDIA 渲染生成三张赛场截图：`pk_arena_60.png`（381,553字节）、`pk_arena_100.png`（383,166字节）、`pk_arena_130.png`（385,794字节），均为700×550；受当前本地图像查看工具限制未做肉眼视觉评审。
## 已完成：战魂封印谜宫

1. 反查 SWF 第34帧，新增“战魂封印谜宫”，使用原始背景`image_1297`和人物出生坐标`(190,245)`；该图原版没有方向出口。
2. 恢复前置条件：武器、头盔、项链、衣服、手镯、战靴六件装备的品质均达到极品（品质4）后，装备锻造师永久开启寻找战魂秘密的任务。
3. 戈壁探险家仅在任务进行时出现，使用原始坐标`(18,325.1)`；选择带路需支付50,000魔石并调用原版`times_go(15)`等价逻辑，额外消耗一整天而不清空原有当天时间余数。
4. 谜宫封印之箱使用`image_1213`与原始坐标`(649.95,217.95)`；点击后才出现无名氏。无名氏使用原始第28帧对应的`image_0101`和坐标`(505.2,175.95)`。
5. 无名氏最低50级；人物超过50级时，等级及战斗属性随人物等级同步成长，战斗力保持原版`100+等级`规则。
6. 击败无名氏固定获得一个战魂之心，不额外发放经验、军功或货币；任务永久完成，胜负都会返回卡萨诺城，失败后仍可重新付费挑战。
7. 存档升级为v19，保存任务、谜宫进行和守卫显形状态；无有效进行状态却停在谜宫的存档会安全迁回卡萨诺城。
8. 新增专项测试后完整回归为25个场景加主场景冒烟测试；三张700×550真实OpenGL截图均已生成并验证尺寸，受当前本地图像查看工具限制未做肉眼视觉评审。
## 已完成：七张野外地图怪物种群

1. 自动解析 SWF 第8、16～21帧的`char1074`放置脚本，提取雷鸣大陆、戈壁、迷梦沼泽、冰宫、亚维特岛、火山、深渊迷宫全部普通怪物名称、等级和坐标。
2. 恢复19种普通怪物：巨杰士、龙怪、冰妖剑士、杰克灯笼、提风、望齿魔人、角蜥、塔亚龙、死亡骑士、鱼妖、恐兽、巨斧怪、刺虫人、四牙怪、炎女、蝎怪、暗黑弥塞亚、暗黑格拉斯、叹息骑士。
3. 七张地图共恢复29个普通怪物场景实例；同种怪物使用`@实例`内部键独立点击，进入战斗前再还原为同一个怪物数据ID，避免点击一个实例却在另一个实例上显示战斗反馈。
4. 逐帧标签校正`image_0049`至`image_0089`怪物素材；七张地图的等级Boss统一使用通用怪物精灵的BOSS帧`image_1072`并恢复原始坐标。
5. 蜘蛛由早期错误的35级/`image_0051`修正为原版45级/`image_0063`；蜘蛛王后艾达修正为80级/`image_0075`，原有技能书和月光宝盒增强版掉落规则保持不变。
6. 普通怪物使用重制版战斗曲线归一化生命、攻击、防御和基础经验；人物经验继续按已定10倍规则结算。通用掉落加入50%灵魂晶石、`等级/1000`灵魂王概率（最高13%），60级以上另有5%月光宝盒增强版。
7. 新增野外种群专项测试后完整回归为26个场景加主场景冒烟测试；雷鸣大陆、戈壁、深渊迷宫三张700×550真实OpenGL截图均已生成并验证尺寸。
## 质量要求

- 新增机制先写自动化测试，再合入主流程。
- 每批完成后运行对应测试，里程碑结束后运行完整回归和主场景冒烟测试。
- UI 改动需要真实启动并截图检查尺寸、遮挡、点击区域和原版布局相似度。
- 版本名称只使用简洁编号；所有具体改动持续追加到本文件与《版本改动.md》。


## 已完成：卡萨诺城原生 NPC 重排

1. 直接解析 SWF 第12帧根时间轴、角色定义和局部边界，确认人物锚点就是左上角坐标，不再使用早期近似布局。
2. 恢复11个可点击对象：杂货商、收藏商店、收藏家、仓库管理员、日常任务官、宝石合成师、装备打造师、幻兽幻化师、经验导师、幻兽研究所、地图占领报名官。
3. 修正关键错配：`image_1126`属于经验导师而不是杂货商；杂货商改回`image_1156`，收藏家改回`image_1138`；主城移除误放的公主和重复可见挑战者。
4. 新增经验导师原版提炼公式：品质1～4分别给1～4个满经验球，一洞额外2个、二洞额外5个，魔魂+9额外1个、+12额外2个。
5. 新增宝石合成师五类20合1配方，并补齐幻魔晶石、中级经验石和中级战斗力石物品定义。
6. 新增主城专项自动化测试；两张700×550真实OpenGL截图已生成，分别为主城原生布局和经验导师对话。
## 已完成：原版野外掉落算法

1. 反查SWF `drop1/drop2`，将19种普通怪、7个等级Boss、蜘蛛和蜘蛛王后分别接入原生掉落档案，纠正v1.9的临时通用猜测表。
2. 普通怪恢复幸运倍率金币、2.5%灵魂晶石、25%随机装备，以及等级、品质、魔魂、洞数的原版生成流程。
3. 等级Boss恢复固定金币、必掉晶石、灵魂王、按等级区分的月光宝盒，以及必掉优良/极品随机装备；战魂秘密解锁后接入战魂晶石替换分支。
4. 蜘蛛与王后的技能书按用户要求翻倍；月光宝盒增强版保持固定50%的用户修改，其余材料概率继续乘原版幸运倍率。
5. 随机装备支持待领取、满背包保护、物品说明、领取和v20存档读回，不会把实例属性压成普通堆叠物品。
6. 新增专项测试，完整回归为28个场景加主场景冒烟测试；真实OpenGL截图`native_loot_equipment.png`已生成并验证为700×550。
## 已完成：原版场景内战斗交互

1. 反查SWF `sprite124/sprite135`及其共用血条对象，确认怪物头顶血条原始边界为80×8；改用无主题色块保证Godot不会把高度强制放大。
2. 移除底部400×36战斗操作栏；左键点当前怪物立即普通攻击，右键点怪物自动选择已学会的最高伤害主动技能。
3. 战斗中锁定具体场景实例，同种怪物的另一个实例也不能替换当前目标；攻击动画期间点击其他怪物同样会被拒绝。
4. 旧`_start_battle`和地图数据入口全部重定向到当前场景怪物；遗留战斗面板保持不可见，不再有第二套玩家可达的战斗流程。
5. 保留人物与幻兽生命顺序、方向出口/移动锁定、攻击动画、受击抖动、伤害飘字、技能倍率、掉落领取、失败回城和原地刷新。
6. 完整28场景回归及主场景冒烟测试通过；真实OpenGL 3.3截图`scene_battle_native_click.png`与`scene_battle_native_skill.png`均为700×550。
## 已完成：原版怪物悬停信息

1. 反查SWF通用怪物的`onRollOver/onRollOut`和全局`tsxs`提示对象，恢复原版123×74怪物信息框。
2. 提示显示怪物名称、原始等级、战斗力和当前人物经验加成；当前10倍经验规则对应“经验增加900%”。
3. 按原版比较怪物与人物战斗力，恢复深灰、红、白、绿四档文字颜色；提示位置跟随具体怪物实例并约束在700×550画面内。
4. 战斗怪物名称不再常驻显示；移出、换图、打开面板和进入战斗都会关闭提示，怪物死亡刷新也不会重新暴露隐藏名称。
5. 新增专项测试后完整回归为29个场景加主场景冒烟测试；真实OpenGL截图`monster_native_hover.png`已生成并验证为700×550。
## 已完成：原版怪物攻击动作

1. 反查通用普通怪`sprite124`的“站立/攻击”标签，确认攻击反馈是单张本体位图向左移动5像素后复位。
2. 反查通用Boss`sprite135`及子动画`sprite131`，接入两张原生186×173攻击位图、12fps节奏和相对站立图的`(-33,-42)`偏移。
3. 场景战斗改为先表现人物攻击和怪物受击，再表现怪物对幻兽/人物的反击；击杀回合不播放反击。
4. 普通怪和Boss在动作完成后精确还原位置、尺寸与纹理；强制取消战斗也会同步还原，避免残留放大Boss帧。
5. 新增动画专项测试后完整回归为30个场景加主场景冒烟测试；真实OpenGL截图`boss_native_attack.png`已生成并验证为700×550。
## 已完成：原版 Boss 受击与死亡反馈

1. 解析通用Boss`sprite135/sprite134`和`shape133`，确认原版受击段是站立图两帧、矩阵变形图两帧，不是旋转抖动。
2. 从SWF位图97和矩阵精确生成123×148受击PNG，保持`(0,-7)`原始偏移；提取脚本会对填充数量、四条直边和位图ID做严格校验。
3. 普通怪恢复为仅血条与伤害数字的原版反馈；通用Boss按实际站立素材识别，地下城/剧情Boss不会误套等级Boss帧。
4. 怪物死亡立即从场景隐藏，保留重制版2.2秒刷新循环；强制取消会原子还原受击/攻击纹理、坐标和尺寸。
5. 新增专项测试后完整回归为31个场景加主场景冒烟测试；真实OpenGL截图`boss_native_hit.png`已生成并验证为700×550。
## 已完成：PK 擂主与领地挑战者 Boss 路由

1. SWF第31～33帧确认三档PK擂主都使用`char1074 + isBoss=true`，现已统一为101×132通用Boss站立图并保留三组原始坐标。
2. 六个地图占领报名官的实例动作均确认传入`isBoss=true`；挑战者只在选择“我要挑战”后进入战场，不再以探险家素材常驻地图。
3. 报名成功时动态生成通用Boss并立即进入场景战斗；胜利结算重建地图清理临时实例，失败回城和每日挑战限制保持原行为。
4. PK/领地专项测试、31场景完整回归和主场景冒烟测试全部通过；两张真实OpenGL 3.3截图均验证为700×550。
## 已完成：人物原版待机与四方向行走

1. 从`sprite523`确认人物的站立、向下、向上、向左和向右五类原版剪辑及其子时间轴，不再根据位图名称猜测方向。
2. 待机/上下按8帧、左右按10帧恢复原始关键帧保持关系，并统一使用SWF的12fps。
3. 向右恢复独立三张位图，向左补回第三张迈步帧；静止和战斗结束都回到原版站立图。
4. 新增专项测试后完整回归为32个场景加主场景冒烟测试；真实OpenGL截图`v117_native_player_idle.png`已验证为700×550。


## 已完成：原版顶部状态卡与幻兽合体

1. 反查SWF根时间轴，将人物、双幻兽与地图卡恢复到`(1,2)`、`(198,2)`、`(394,2)`和`(590.5,2)`；人物卡恢复生命、体力、经验三行。
2. 幻兽卡恢复原始头像、等级、召回与合体/解体按钮；合体状态可在战斗动作空档实时切换，按钮文字与战斗配置同步刷新。
3. 出征未合体幻兽只提供战斗力；合体幻兽把攻防并入人物、依次代理人物受伤，不再额外造成第二次独立伤害。死亡合体幻兽保留生命状态但不再贡献攻防。
4. 恢复1级110体力与两类主动技能各等级体力消耗；不足时退化为普通攻击，升级回满。
5. 存档升级到v21；旧档已出征幻兽迁移为合体，人物体力缺失时按当前等级上限补齐。
6. 新增专项测试后完整回归为33个场景加主场景冒烟测试；两张700×550真实OpenGL截图已人工目视检查，首轮发现的容器拉伸和地图文字重叠已修复后重拍。

## 已完成：原版固定底栏

1. SWF第6帧确认日期/时间、音乐、垃圾箱与五个功能按钮均是根时间轴绝对放置，不应由容器自动均分。
2. 底栏恢复`y=512`；日期/时间、音乐和垃圾箱分别按`sprite908/963/button889`的根坐标重排，星期恢复中文名称。
3. 背包、装备、幻兽、技能、VIP使用角色893～897的固定x坐标及62×32边界；共享形状890/891/892对应橙色、黄色、深橙三态。
4. 所有按钮均保留真实交互；专项测试直接验证背包关闭/打开、音乐静音/恢复和垃圾箱提示。
5. 新增专项测试后完整回归为34个场景加主场景冒烟测试；700×550真实OpenGL截图已人工检查，无布局挤压和文字截断。

## 已完成：原版场景边缘切图入口

1. 解析SWF角色1071及根时间轴矩阵，按地图恢复41×90竖向或90×41横向的真实命中区域。
2. 卡萨诺城恢复右亚维特岛、上皇宫、左雷鸣大陆、下戈壁；其他主大陆、地下城、最终战和活动图同步登记原版坐标。
3. 左右入口竖排、上下入口横排，绿色地图名与红色“进入/返回”直接画在场景边缘，不再显示独立地图按钮或灰色面板。
4. 保留所有等级和剧情门槛；未解锁入口灰显，切图后自动重绑目标并刷新位置。
5. 专项测试后完整回归为35个场景加主场景冒烟测试；700×550真实OpenGL截图已人工检查。

## 已完成：原版背包与仓库框架

1. SWF `sprite572/shape566`确认背包为`(453,300)`、247×207和6×4格；标题、金币、魔石及关闭钮均按子角色坐标恢复。
2. SWF `sprite565/shape562`确认仓库为`(208,220)`、247×287和6×6格；打开仓库时与右下背包同时显示。
3. 关闭钮按角色564的三态形状恢复为橙色圆环，不再使用通用方形标题栏按钮。
4. 保留现有48格存档容量，以紧凑页码访问原版单页范围之外的物品，避免UI还原造成旧档物品丢失。
5. 专项测试后完整回归为36个场景加主场景冒烟测试；700×550真实OpenGL截图已人工检查。

## 已完成：原版三套商店窗口

1. 杂货商按`sprite609/shape573`恢复`(285,90)`、333×207、左买右卖的双栏结构。
2. 收藏商店按`sprite645/shape610`恢复`(285,90)`、167×207购买窗；收藏家按`sprite652/shape646`恢复`(260,300)`、167×207出售窗。
3. 三套购买/出售格、标题、货币栏、42×24按钮及橙色关闭钮均按SWF子角色坐标落位。
4. 无限金币/魔石仍直接接在出售标题按钮；矿石按品质出售与购买失败回滚保持可用。
5. 专项测试后完整回归为37个场景加主场景冒烟测试；两张700×550真实OpenGL截图已人工检查。

## 已完成：原版 NPC 对话窗口

1. SWF `sprite882/shape834`确认对话窗口为`(200,80)`、362×269.3和`#ffcc99`底板。
2. 已提取并映射19类原生NPC头像；NPC头像、正文、玩家头像、六行回答和关闭钮均按子角色坐标恢复。
3. 所有原有商店、任务、剧情、传送和战斗回答继续工作，并接回NPC对话音效。
4. 专项测试和两张700×550真实OpenGL截图已人工检查；v1.23完整回归为38场景加主场景冒烟测试。

## 已完成：原版人物信息与装备窗口

1. SWF `sprite561/shape524`确认人物装备窗为`(210,85)`、243×289和`#666666`底板。
2. 六个装备位按实例矩阵恢复为右侧三格、底部三格；装备图标与既有物品说明联通。
3. 人物属性、战魂等级、天魂/地魂套装标志实时刷新；42×24“详细”按钮可向左展开178×231战斗力评定页。
4. 专项测试和两张700×550真实OpenGL截图已人工检查；v1.24完整回归为39场景加主场景冒烟测试。
## 已完成：原版幻兽背包与详情

1. SWF `sprite787/shape732`确认幻兽背包为`(0,150)`、213×333、4只一页；四个193×69栏及六类幻兽原生头像均已恢复。
2. 恢复分页、选中出征/召回、右侧213×333详情、六字改名和203×123丢弃确认；出征幻兽不可丢弃。
3. 经验球训练、日常上交和幻化继续保留在详情页；新增状态接口与专项测试。
4. v1.25回归为40场景加主场景冒烟测试；两张700×550真实OpenGL截图已人工检查。

## 已完成：原版六栏技能窗口

1. SWF `sprite731/shape697`确认技能窗为`(23,220)`、183×253，六个40×40图标按5/45/85/125/165/207.6纵向排列。
2. 四个已实现技能显示实时等级和效果；两个原版保留栏不虚构机制；爱的力量仍可点击施放。
3. 技能学习继续走背包双击，不在技能窗重复添加非原版学习按钮。
4. v1.26完整回归为41场景加主场景冒烟测试；700×550真实OpenGL截图已人工检查。
## 已完成：原版装备精炼窗口

1. SWF `sprite662/shape653`确认精炼窗为`(238,375)`、191×133；装备状态和五类养成操作均在原窗口内完成。
2. 品质/魔魂必成、战魂50%、战魂双倍加成与天魂/地魂现有机制全部保留。
3. v1.27新增专项测试与真实OpenGL截图。

## 已完成：军衔、爵位与关系操作流

1. 军功、军衔、爵位功勋、每日任务、捐献、玫瑰、周日军饷和公主礼物已统一接通。
2. 所有一次性和每日操作均根据当前状态灰显，窗口不遮挡底栏。
3. v1.28新增完整成长流专项测试与真实OpenGL截图。

## 已完成：原版幻兽研究所周期

1. SWF `sprite827/shape823`确认研究所为`(250,180)`、203×183，购买、投资与关闭操作按原坐标恢复。
2. 产量任务每次+2；每天自动生产，跨周自动提升技术，最高300级。
3. v1.29新增生产周期专项测试与真实OpenGL截图。

## 已完成：任务簿、路线引导与主线闭环

1. 三条数据任务和九阶段主线目标已统一到原版杏色任务簿，状态与下一目标实时刷新。
2. 世界地图已支持按当前地图计算真实最短路线，地下城NPC入口有专门提示。
3. 新增从地下城三层救王到四路魔军、统领、能量和结局页的端到端测试。
4. v1.30～v1.31完成后完整回归为46场景加主场景冒烟测试；两张700×550真实OpenGL截图已人工检查。

## 已完成：无图像证据注册表（v1.32）

1. 新增 `docs/swf_evidence_registry.json` 结构化文本注册表，把已确认 UI/动画的 SWF 角色号、父角色、根帧、根矩阵、形状边界、子控件矩阵、帧数、色值、Godot 文件与测试文件集中为可被无图像能力模型定位的纯文本证据。
2. 覆盖 19 个 UI 组件（顶部 HUD、底栏、场景出口、背包、仓库、三套商店、对话、装备与详细页、幻兽背包/详情/丢弃确认、技能、精炼、研究所、怪物悬停、任务簿）与 5 项动画（人物待机/四向行走、人物四帧攻击、普通怪横移、Boss 攻击帧、Boss 受击形状）。
3. 登记 111 个 `image_####`/派生资源的已知用途与证据来源，按运行时/legacy 旧面板标注作用域；登记 28 张地图背景位图；共享角色（关闭钮、购买/出售按钮、Boss 站立图、回答悬停形状）单独标注避免冲突。
4. 任务簿复用对话底板 sprite882、人物攻击段复用 sprite523 均以 `reuse_of` 显式标注为同源复用，非角色号冲突。
5. 新增 `test_swf_evidence_registry_scene` 验证测试：校验注册表结构、Godot/测试路径存在、image 资源存在、角色号不重复冲突（重复须有 `reuse_of`）、地图与共享字段完整，并断言不含“看截图/目测”类依赖图像的验收描述。
6. 不修改玩法、UI、用户覆盖数值或存档；专项、相邻回归与全量回归通过，完整回归现为 47 个自动化场景加主场景冒烟测试。
7. 已登记证据缺口：部分窗口 SWF 根帧号、顶部 HUD 三段 sprite 与四卡 1:1 对应、HUD 卡片原始底色待后续版本补证。

## 已完成：活动主场景退役旧面板（v1.33）

1. 从 main_original.gd 移除 MapPanel/BattlePanel 的 preload、成员、_build_panels 创建与 _hide_all_panels 关闭项；_start_battle 保留为兼容入口，只走场景内战斗，找不到怪物时提示。
2. 迁移 test_main_scene/test_scene_battle_scene/capture_v112_scene_battle 的 main.battle_panel 断言为场景树无旧脚本节点检查；visual_battle_scene 改用 _engage_world_monster 演示。
3. 新增 test_no_legacy_panels_scene 专项测试，覆盖场景树无旧节点、源码无 preload/var 声明、_start_battle 路由、无效怪物提示、NativeExitButton 切图五项断言。
4. 历史脚本 main.gd/map_panel.gd/battle_panel.gd/shop_panel.gd 保留；image_0051 保持 active+legacy；注册表测试继续通过。
5. 修复 test_scene_battle_scene 在 --quit-after 600 下被提前终止的预存在脆弱性：场景测试 --quit-after 提到 1200 作为超时上限，run_tests.ps1 同时要求每个自动化场景日志出现 `^PASS ` 完成标记，缺少即判失败并保留日志。
6. 不改玩法/UI/数值/存档（仍 v21）；完整回归 48 场景加主场景冒烟通过。活动运行时已退役旧面板，历史脚本尚保留。
7. v1.33 整改 01：run_tests.ps1 增加 PASS 完成标记校验；注册表 56 处易漂移数字行号改为 81 项 source_refs（path+token）稳定定位，注册表专项测试新增 source_refs 的 path 存在与 token 命中校验；开发进度顶部测试数 47 统一为 48。

## 已完成：剩余系统入口原版流程校正（v1.34）

1. 元帅/首相/公主/日常官六处原 progression 聚合窗替代入口改为专属NPC对话action：military_status/prime_donate/princess_gift/daily_deliver。
2. 元帅军功查询显示当前军功/军衔/下一军衔差额/军衔战斗力；首相捐献750,000金币=1功勋；公主99/999朵白玫瑰；日常官魔魂晶石/灵魂王交付，均调现有GameState原子方法，不复制公式。
3. 研究所初次仅核对sprite827窗口证据；整改02进一步确认sprite1174为NPC对话并补齐四个原版选项，sprite827继续作为专属窗口。
4. 新增 docs/npc_progression_flow_registry.json 登记五组NPC选项action/状态差/SWF证据/evidence_tokens。
5. 新增 test_native_npc_progression_routes_scene 专项测试，从NPC入口验证四类流程+研究所+聚合窗隔离+注册表。
6. NPC action不再用 progression 代替原生流程；ProgressionPanel 保留为VIP便捷聚合窗。
7. 不改数值/用户覆盖/地图/战斗/UI/存档（仍v21）；完整回归 49 场景加主场景冒烟通过。
8. 证据缺口待PM决策：首相75,000,000档无原版证据；日常官原版“灵魂晶石”+500功勋与当前“魔魂晶石”+灵魂王2000功勋冲突。
9. v1.34 整改 02：区分研究所NPC sprite1174（四选项：进入/研究所的当前信息/关于2008奥运使者/提高产量任务）与窗口 sprite827；补齐四个NPC选项实现（research_info/research_olympic_info/research_production_task），只读分支不改状态，产量任务调 complete_research_production_task；证据文件加 sprite1174 章节、sprite827 改窗口、元帅“少尉至上尉”修正；注册表 research 1->4项（总22项）；测试加固公主礼物物品数量与研究所资助/产量/购买资源扣除断言。不改数值/存档，测试总数49。
10. v1.34 整改 03：修复 _open_research_olympic_info 待交付判断（beast_defeated 改为 found_count>=FUWA_NAMES.size()&&!completion_claimed）；补 2008奥运使者未完成/待交付/已领取三状态只读测试；清理注册表“少尉至上校1000战功”残留为“少尉至上尉”并加 JSON 断言；修正 v1.34 正文旧研究所结论为当前事实。不改数值/存档，测试总数49。

## 已完成：全地图演员与交互覆盖审计（v1.35）

1. 新增 docs/world_interaction_registry.json（28图107实体48出口）+ docs/evidence/world_interactions_v103_v9.txt（28图SWF证据）。
2. _add_actor/_add_decoration/NativeExitButton 加 world_entity_id/world_entity_kind 审计metadata。
3. 新增 test_world_interaction_registry_scene 专项测试：注册表结构 + 28图运行时实体边界 + 动态矩阵（palace国王未救/已救）。
4. 审计发现 grass_reward 无交互实体（仅装饰）；Boss坐标底部在512-550底栏区域按原版保留。
5. 不改数值/存档（仍v21）；完整回归50场景加主场景冒烟通过。
6. 证据缺口：出口direction/position待逐图确认；部分SWF帧号为版本历史值；怪物/装饰逐图scope对照待补。
7. v1.35 整改 01：28图精确运行时比较（双向实体集合/资源/坐标/尺寸/action+出口+背景+装饰）；dynamic_scenarios扩至7项并参数化测试；116实体token全部命中证据文件；实体分类改evidence_gap+结构化gaps；_add_decoration加稳定entity_id；action可达性验证+边界700×512。六项负向验证全部通过。不改数值/存档，测试总数50。
8. v1.35 整改 02：A)28图双向精确比较+出口全字段+Boss例外assert+gap复合键+token全覆盖。B)43动态场景数据驱动+组覆盖断言。C)12类真实action路径。D)9项内存负向验证全部命中目标断言。不改数值/存档，测试总数50。
9. v1.35 整改 03：61动态场景独立期望+完整GameState隔离(level/magic_stones/inventory/demon_campaign/territory/pk等)+12类真实action路径(矿点/抽奖/战魂守卫battle/NPC speaker断言)+_world_action_route dispatcher分类器+出口metadata验证+边界x+width<=700+9项负向(含UNSUPPORTED_ACTION dispatcher)。不改数值/存档，测试总数50。
10. v1.35 整改 04（最终验收）：PM/SE 于 2026-08-01 独立复验 A-F 全部关闭。最终注册表28图/116实体/48出口/61动态场景/76结构化gap，23字段状态隔离、105项world action、11项dialogue、24个领地状态、出口metadata/runtime rule、9项负向及50场景+主场景冒烟全部通过；未确认字段仍如实保留为 evidence_gap。v1.35正式验收，v1.36未开始。

## v1.37 整改01（2026-08-02，REVOKED：本段分类/证据结论已被整改02/03/04 撤销，不可作为当前证据；历史保留）

- PM/SE 拒签 v1.37 并下发整改01：六大部分已逐项修复。
- **去重机制**：dedupe key 按注册表策略生成（per_attack=session+attack_sequence+event_type；per_battle=session+event_type；per_level=session+event_type+level；per_item=session+领取序列 serial，区分队列中两个相同 item_id 的独立领取）；同一战斗连续三回合逐回合事件集合/dedupe key/声音请求全部接受一次；顺序状态只在事件最终 accepted 后提交（_sequence_transition 纯函数，被 dedupe 拒绝的事件不得伪造 attack_started 等前置状态——共用 key 负向证明）。
- **技能生命周期**：新回合 attack_sequence 建立后才发 skill_started/skill_failed_stamina（原实现在回合建立前发，第二回合会被旧回合校验拒绝）；成功施放产生 skill_finished（与 skill_started 同 session/回合，先于 player_attack_finished）；flying_slash 2 级精确扣体力 5、star_sword 1 级扣 10；同种子 BattleSession 对比验证技能伤害状态差；体力不足无 skill_started/finished 且普通攻击只一次。
- **AudioService 真实记录**：play() 在 headless 下先记录事件/请求再跳过播放器；只读历史/计数与测试清理接口；busy/重复 dedupe/dodge 路径断言无重复请求；SceneBattleController 幸运耗尽退出改为 battle_retreat 语义事件（唯一反馈通道，无直接 AudioService 战斗调用）。
- **确定性 dodge**：test_dodge_roll 强制 roll（0.0 必闪避）+ 六件战魂装备 95% 闪避；断言有 dodge 事件、无 player_hit、无受击声音请求、玩家 HP 不变、无伤害浮动文字。
- **busy 快照**：输入前后比较 turn/体力/玩家HP/怪物HP/accepted 事件数/AudioService 事件数/活动调度器数全部不变。
- **level_up**：连续两次升级不被去重吞掉（per_level 含 level）；同 level 重复被 FEEDBACK_DUPLICATE_EVENT 拒绝；一次经验结算（含多级连升）只提示一次已在注册表说明。
- REVOKED（整改04）：**SWF 工具链**：DefineSound 解析把 SoundSize 转 1/2 字节、SoundType 转 1/2 声道（swf_parser.gd 与 python 审计脚本同步修正）；审计 v2 覆盖 DefineButton2 条件动作/DoInitAction SpriteId 前缀/ActionConstantPool/Push 常量池索引/GetURL/GotoLabel（46 个 action 码 54503 个 action 全覆盖）——**修正 v1 错误结论**：找到 58 处 attachSound（sprite37 喝药/561 装备/662 精练/731 学习技能/882 NPC对话框/932 死亡+升级+其它升级/963 back.mp3/1071 切换地图/1074 指向怪物/22 个 NPC sprite 指向NPC+选择NPC/1161·1185·1186 传送/1206 送花/1222 open+升级），分类按 binding/trigger 规则重新统计（整改02）：native_confirmed 4 / used_unconfirmed 16 / evidence_gap 6 / unrelated 11（证据自动统计，不预设）；受击仍无 StartSound/attachSound（hitsb 为动画段 label，gap 保持）。
- **专项测试**：原 5 项负向保留 + 新增 7 项（第二回合被错误去重证明、skill_finished 缺失/错回合、同 item 双领取、per_level 错误去重、被拒事件污染顺序状态、AudioService headless 未记录、SWF 格式字段不匹配——SWF/manifest/注册表三方比对 + 篡改命中 FEEDBACK_SOUND_FIELD_MISMATCH + 证据路径从注册表读取 + token 段归属校验）；dodge 不再用概率循环；专项 1 PASS/0 错误/0 泄漏。
- run_tests.ps1 超时 1200→2000 帧（专项全流程约 25s 需更大上限；正常 PASS 后立即退出无额外开销）；删除 tests/_diag_ref.gd/.tscn。未改数值/剧情/地图/存档 v21/用户覆盖；未开始 v1.38。

## v1.37 实施完成（2026-08-02）

- v1.37 正式任务书（技能、战斗特效、掉落与声音反馈校正）已实施完成，**等待 PM/SE 独立复验**；未开始 v1.38。
- 唯一生产反馈通道 CombatFeedbackService（autoload）：`emit()` 唯一事件入口、单调递增序号（不依赖系统时间）、session/attack_sequence 双重校验、dedupe key 一次接受、顺序规则状态机、有界历史（512）、headless 仍记录事件、测试钩子只读副本。
- 37 段音频逐项审计与分类：native_confirmed 13（StartSound 证据）/ used_unconfirmed 18（导出文件名+生产映射）/ evidence_gap 4（无任何证据）/ unrelated 2（音乐与未接入），37 条全部登记 docs/combat_feedback_registry.json（生产数据源）+ docs/evidence/combat_feedback_v103_v9.txt（token 逐行独立）。
- SWF 证据驱动的战斗声音接入：普通怪攻击 sound_0123（sprite124 帧7 label 攻击 StartSound）、Boss 攻击 sound_0132（sprite135 帧6 StartSound）、飞天连斩 sound_0518（sprite523 帧27 label 飞天连斩）、星魔剑 sound_0520（sprite523 帧63 label 星魔剑）。
- 生产接入：攻击开始/动画帧/命中/完成、技能开始/体力不足降级/结束、怪物攻击开始/结束、受击/闪避、怪物/玩家死亡、胜利、奖励入队、掉落领取成功/失败、升级全部走反馈事件；busy 期间重复输入继续拒绝（不重复扣体力/伤害/声音）；cancel_battle 与地图切换后旧 session 事件被拒（FEEDBACK_STALE_SESSION_EVENT）。
- 专项测试 17 项正向 + 5 项负向（FEEDBACK_DUPLICATE_EVENT / FEEDBACK_SEQUENCE_MISMATCH / FEEDBACK_SOUND_UNREGISTERED / FEEDBACK_PASSIVE_SKILL_TRIGGERED / FEEDBACK_STALE_SESSION_EVENT，每项先证明 mutation 实际作用于目标分支、恢复后同一验证器正向对照），真实 SceneBattleController/AudioService/BattleSession/GameState + 可控时间推进。
- 52 场景全量 + 主场景冒烟全部通过（专项 ExitCode 0、1 PASS、0 SCRIPT ERROR/ERROR）。未改数值/剧情/地图/存档 v21/用户覆盖；未开始 v1.38。

## v1.36 正式验收（2026-08-02）

- PM/SE 终审通过，v1.36 正式验收（PASS）；v1.37 门禁解除但**未开始**（等待正式任务书）。
- 终审勘误一项：mutation_applied 描述已更正（启动后槽位+1 与 untrack 无关；改取消后槽位差异证明），不阻断验收。

## 已完成：NPC、特殊怪物与剧情对象原生时间轴（v1.36 整改12）

0. 整改12（拒签整改11 的 Scheduler 强引用/真实释放验证阻断关闭）：**承认整改11 两个不实**——(a) _last_handle 是永久强引用 handle（保留已结束对象强引用），被误报为"活动 handle"；(b) "no crash, no dangling refs" 只是 print 无断言。(1) _last_handle 改活动 handle：untrack（自然完成/cancel/battle_cancel/map_change）时匹配当前 Scheduler 即清空。(2) 三计数独立：active_scheduler_count/tracked_scheduler_slot_count（含失效槽残留）/has_active_scheduler_handle。(3) 100 次自然/取消**每次**断言三计数为 0。(4) 真实释放：Scheduler WeakRef 在完成/取消/地图切换/actor 重建后 get_ref()==null；**修复真实泄漏**——finished 连接 bind(tween)/bind(sched) 自引用循环（实测槽空但对象存活）→ 连接改绑 WeakRef。(5) LEAK 负向经注册槽残留命中（PM/SE 终审勘误：'启动后槽位+1'不是 mutation 证明——与 untrack 开关无关；已改为启用/禁用 untrack 的取消后槽位差异证明）；恢复后同一验证器完全通过。(6) 场景退出对 main/controller/sched/tween/actor 逐项 WeakRef 断言，删无断言 print。自检修复：循环局部 handle 强引用、多残留循环清理、WeakRef 显式类型。保持 149/186、Boss 帧序、map_change Timer、数值、存档 v21、地图拓扑不变。

## 已完成：NPC、特殊怪物与剧情对象原生时间轴（v1.36 整改11）

0. 整改11（拒签整改10 的 Scheduler 生命周期/生产 Tween 验证阻断关闭）：(1) _active_schedulers 只存活动 Scheduler 的 WeakRef；自然完成（finished→untrack）、cancel/battle_cancel/map_change（cleanup→untrack）解除引用；active_scheduler_count() 计被跟踪调度器。(2) 100 次自然完成（tween.custom_step 推进）与 100 次取消后计数均回 0。(3) _drive_scheduler 返回生产 handle（scheduler+tween），test_last_handle 提供（非永久历史数组）。(4) duration 负向：真实 _play_* + tween.custom_step 确定性推进（不用 sched.seek 制造），边界序列用 SWF_EXPECTED 独立期望（篡改 dur→期望边界 1/12 观察 actor 仍帧0 → PRODUCTION_TIMELINE_BOUNDARY_MISMATCH）。(5) 负向后恢复人物基线再正向对照。(6) 原 texture=null 精确恢复 null。(7) ended/cancelled 后 seek/advance 不应用帧（断言 cancel 后节点不变）。(8) 负向：untrack 禁用→SCHEDULER_REFERENCE_LEAK（残留用生产 untrack 清理后正向通过）；错误 duration→PRODUCTION_TIMELINE_BOUNDARY_MISMATCH；cancel 后 seek 无变化。(9) driver 验证器覆盖 4 战斗时间轴（真实 _play_* + custom_step，t=0/边界±ε/结束点观察真实 actor）。自检修复：tween custom_step interval 提前量（±0.002 窗口）、prev 累计精度、期望帧来源（帧表内容+独立期望时间）、count 语义、负向残留清理。保持人物/目标取消、149/186、Boss 帧序、map_change Timer 不变。

## 已完成：NPC、特殊怪物与剧情对象原生时间轴（v1.36 整改09）

0. 整改09（拒签整改08 的生产时间轴阻断关闭，不触碰根事件状态机与 map_change Timer）：(1) 统一生产调度器 scripts/timeline_scheduler.gd——生产 _play_* 与测试 seek/advance 启动同一 TimelineScheduler 对象，测试不再直接逐帧写入（_apply_timeline_frame_state 删除）。(2) 离散帧排程唯一语义：t=0 帧0 -> 等待 dur -> 下一帧 -> 末帧 dur -> once_restore，无额外段；player_attack 帧1/2/3/restore 在 1/12/2/12/3/12/4/12。(3) normal_monster_attack 离散 offset（sprite124 frame7 单帧位移，无补间证据），总时长=2×1/12。(4) boss_hit 帧序统一（sprite134 帧1-2 站立/帧3-4 受击）：注册表/TIMELINE_FRAMES/SWF_EXPECTED/生产播放 = [站立, 受击]，t=0 站立、2/12 受击、4/12 restore。(5) 真实生产边界测试：验证器基于生产调度器实例在真实节点观察 t=0/边界±ε/end + once_restore；duration 负向启动调度器在旧边界 1/12/新边界 2/12 观察 actor 状态变化。(6) anchor 静态属性语义（调度器不写 anchor，seek 前后不变）。(7) 修正"五个 TIMELINE_FRAMES"残留注释（实际 4 战斗时间轴）。(8) 相邻测试：monster_attack 改事件驱动轮询（离散帧断言），monster_hit 天然适配。保持 149/186 根事件与 map_change Timer 不变。

## 已完成：NPC、特殊怪物与剧情对象原生时间轴（v1.36 整改07）

0. 整改07（拒签整改06 的三项阻断关闭）：(1) 根移除改事件流状态机——root_events 按原始 tag 顺序模拟 depth->当前 character，同 depth 被替换后再删除的旧角色不算被移除（PM 指定反例固化）；18 字符固化（8 点名 + 10 旧误判）。(2) TIMELINE_FRAMES 唯一帧数据源——生产播放与确定性入口共用（整改08 升级为生产共用状态转换函数）；专项按 t=0/边界-ε/边界点/边界+ε/结束点对 4 个战斗时间轴逐项断言帧索引。(3) map_change Timer 分支——WaitDriver 驱动生产 _cancellable_wait 登记 on_map_change Timer：battle_cancel 不停、真实 _apply_current_map 停并解除跟踪、协程退出后节点释放，测试不手动 stop/remove/clear。

## 已完成：NPC、特殊怪物与剧情对象原生时间轴（v1.36 整改06）

0. 整改06（拒签整改05 的六项阻断关闭）：自然结束清除断言（不手工 clear，验证生产回调解除跟踪）；路径验证去空值绕过（删除/空串负向）；根移除固定夹具 oracle（5 用例直接调用生产纯函数，消除同算法循环自证）+ 10 个误判字符固化；真实 map_change 生命周期（handle_map_change 由 _apply_current_map 调用，真实切换 kill/untrack on_map_change 资源）；确定性逐帧边界矩阵（帧0-3 + once_restore 结束点 + 循环点 wrap）；文档事实修正（根 285 非 320、"分布在帧3-38 部分帧、帧7/37 无"非"每帧清空"）。负向总数 12。

## 已完成：NPC、特殊怪物与剧情对象原生时间轴（v1.36 整改05）

1. 引用链完整化：swf_parser.gd 解析 PlaceObject2 完整 matrix（translate+scale+rotate）+ColorTransform 实际参数（CXFORM add/mult；sprite1159 frame2/3 add=[77,77,77,0]/[38,38,38,0]）+RemoveObject2（帧序状态机：放置后被移除=显示列表变化），find_placers_of 含根时间轴，is_bitmap_visually_static 递归到根。视觉变化判定：char_swap/translate/scale_rotate/color/remove 任一触发即非静态。
2. 标签分布修正口径（整改05）：全文件递归统计（递归进入全部 233 个 DefineSprite）——tag4=0、tag5=0、tag26(PlaceObject2)=1577、tag28(RemoveObject2)=320、tag39=233，与 PM/SE 独立递归扫描一致；根时间轴口径 tag26=327/tag28=285 单独保留；swf_parser.gd 与 work/_v136_tagscan.py 均递归统计。
2b. 根 RemoveObject2 查询修复（整改05）：has_root_remove_after_placement() 按 (frame, depth) 匹配（原只比帧号会误判其他 depth 删除）；测试内独立参考实现对比根时间轴全部 219 个 character，零不一致。
2c. cancel_policy 门禁补全（整改05）：删除字段 -> TIMELINE_CANCEL_POLICY_MISSING（任务书必测负向）；合法但错误值 -> MISMATCH 保留。
2d. 注册表路径与帧边界（整改05）：implementation_path/test_scene 存在校验；frames 数量与 SWF_EXPECTED 完全一致，缺帧/多帧 -> TIMELINE_FRAME_COUNT_MISMATCH（两项新负向）。
2e. 策略取消丢跟踪修复（整改05）：_kill_active_tweens/_clear_active_timers 仅移除实际 kill/stop 条目，不匹配事件策略的资源保留跟踪；测试登记 on_map_change 资源经 battle_cancel 后断言存活且仍被跟踪。
3. 根时间轴 RemoveObject2 场景状态机事实如实记录（marshal 帧13放置帧14移除；research/mine_nodes 无任何放置记录），不构成对象自身动画证据，gotoAndStop 常驻帧列为证据缺口；static_native 分类仅基于对象自身动画证据。
4. 专项用例 image_1156->shape1157->sprite1158->sprite1159：sprite1159 frame2/3 ColorTransform 实际参数 add=77/38（悬停色变），"视觉不变"不成立 -> grocery 为 evidence_gap。
5. 引用链分类（完整判定）：3 实体无任何视觉变化（marshal/research/mine_nodes）-> static_native；41 实体 wrapper 有视觉变化（17 NPC color、15 怪物选择器含内部RemoveObject2、princess/chest 真动画、福娃选择器）-> evidence_gap。
6. 修改 scene_battle_controller.gd：cancel_policy 生产真实接入——_load_timeline_policies() 读注册表，Tween/Timer 登记 timeline_id，事件站点 timeline_cancelled_by() 按策略取消（battle_cancel 事件=on_battle_cancel；map_change 事件=on_map_change）；_create_tracked_tween 支持 cleanup callable（kill 时执行 label.queue_free / modulate 恢复）；_finish_defeat 用 is_running() 轮询替代 await fade.finished（kill 后协程不悬挂）；Timer 用可 stop 的 Timer 节点。
7. 修改 main_original.gd：_apply_current_map 按 on_map_change 策略重置 player_motion（回 idle 首帧）；_rebuild_map_actors->cancel_battle() 保留。
8. 证据文件整改：全部 token 独立成行（image_0503/0505/0507/0509、frame=6、image_0127/0129、3 static+41 gap 逐实体行），validate_registry 三分类 token 全量校验。
9. test_native_timeline_registry_scene：真实事件 + 生产策略断言（5 timeline 生产读取==SWF_EXPECTED）+ Timer 真实引用 is_stopped()==true + 协程退出可观察（节点释放）+ 浮动 Label 取消前后无残留 + defeat 淡出取消不悬挂（modulate cleanup 恢复/流程中断）；单一 validate_registry 正向0错误+7项合法值负向（含 cancel_policy MISMATCH/anchor/fps）；文档统计按解析 character ID 去重（49=5+3+41/33/44/解析失败0）。
10. 不改数值/存档（仍v21）；完整回归51场景加主场景冒烟全部通过（RUN+PASS、无FAIL/SCRIPT ERROR）。未开始v1.37。