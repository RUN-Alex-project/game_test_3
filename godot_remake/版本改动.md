# 版本状态总览（2026-08-08 综合复验后）

> 按 PM/SE 综合复验要求统一标记，**未验收版本不得写成已完成**：
> - **v1.37：已签署**（正式验收通过）
> - **v1.38：待综合整改复验**——综合整改已实施部分（precondition 统一调度 + N8 按序 + finally 清理 + cross_day 真实 API），专项 PASS + 全量 60 RUN
> - **v1.39：待复验**——registry↔production 双向比较已实施（11 项 config 定位），代码魔法数项待生产 API 定位
> - **v1.40：待整改**——原子存档（tmp+flush+replace+旧档保护）未实施，需改生产代码
> - **v1.41：待整改**——发布包重构建（exe/pck + manifest/SHA256 重生成 + RC 真实读取）未实施
> - 未开始 v1.42，未发布新包。

## v1.37 整改08（2026-08-02，已实施待复验）

- **整改07 拒签两项已修复**（v137_evidence_gate.py）：
  1. **生产声音 ID 错配漏洞**：verify_call_site 增加前置校验——`production_event` 必须映射到**本条目 sound_id**（`sound_name_map[pe] == sid`，background_music 音乐特例）；行内声音名一致性只认映射本 sid 的名字。新增负向 **M13**（事件名+调用点**同时**改为另一已登记声音 potion → 精确命中 GATE_PRODUCTION_MAP）。
  2. **TSV 多重集比较**：attach_sites/StartSound 位置改用 **Counter 多重集**逐条比较（重复项可检测）；files 列按去重集合语义；read_evidence_tsv 对**重复 sound_id 行**逐条并入并记录 row_count（不覆盖），row_count>1 报错。新增负向 **M14**（tsv attach_sites 追加重复项 → 精确命中）、**M15**（tsv 重复 sound_id 行 → 精确命中）。
- **门禁结果**：正向 PASS + 负向 **15/15 精确集合命中** + EXIT 0 无残留；classify OK（17/2/18/0）。
- **回归**：专项×3 PASS；monster_hit 100/100；全量 ×2（55 RUN EXIT 0）。
- 未改玩法数值/地图/剧情/存档 v21；未开始 v1.38。

> **REVOKED（综合整改）：本段"完成/无阻断"结论已撤销，v1.38-41 均为待复验状态。**

- **PM/SE 七次复验拒签 5 项**：cross_day_result 禁调 prepare/禁写 current_day/story_flags/用真实日期 API、precondition_hook 由 run_registry_case 调度、N8 拆分错误码、测试隔离 finally 清理、v1.39 双向比较。**本整改返工 v1.38 部分**。
- **cross_day 真实日期 API**：`_cross_day_exec`（执行阶段）不调 `_cross_day_prepare`、不直接写 current_day/story_flags——daily 用 `advance_day()`、weekly 用 `advance_day()×7`、revive 用 `resolve+advance_day`、none 用 `advance_day()` 推进；前置日期由 run_registry_case 显式调用 `_cross_day_prepare` 准备。`_cross_day_exec` 加入 FLOW_FIRST（禁写终态），FIXTURE 移除 `_cross_day_result`。
- **跨日补资源不覆盖日期**：`_setup_resources` 保存/恢复 current_day（weekly 推进后 setup 不覆盖日期）。
- **maid_combat_stone 修正 daily**：核实 advance_day 每日重置其 available → cross_day 从 none 改 daily（原标错）。
- **N8 到达读档**：变异 action=buy_maid_combat_stone（daily 语义）+ 同步 cross_day=daily + 匹配 verify + save_fields 缺默认 → 首次/重复/跨日均通过、读档命中 STORY_SAVE_DEFAULT_MISSING（差分：先断言原始成功）。
- **测试隔离**：唯一 user:// 路径 + `_cleanup_save`（_ready 开头 + 末尾 finally 清理），测试后无存档残留。
- **回归**：v1.38 专项 PASS（cross_day 真实日期 API + 八负向含差分逃逸 + 测试隔离）；全量 60 RUN ×2 EXIT 0。
- **v1.39/v1.40/v1.41 整改待后续**（PM 要求的双向比较/原子存档/发布包重生成，属后续版本）。
- 未改数值/地图/存档 v21；等待 v1.38 签署，不开始 v1.39。

## v1.38 六次整改（2026-08-08，决策者模式，registry 全贯穿 precondition/repeat + cross_day 分离）

- **PM/SE 六次复验拒签 5 项**：cross_day 执行未与夹具真正分离（_cross_day_result 写终态且被白名单放行）、N8 未到达读档阶段、N7/N8 断言过弱（非差分）、registry 未贯穿 _precondition/_verify_repeat、口径残留（仍写"六负向"）。**本整改返工**。
- **cross_day 准备/执行真正分离**：revive 分支夹具（king_rescued/game_won）移入 `_cross_day_prepare`，`_cross_day_result` 执行阶段只调真实 API（resolve/advance_day）。`_cross_day_prepare` 归入夹具白名单（revive 准备合法），执行阶段无写终态。
- **N8 到达读档阶段**：变异 action 为 buy_maid_combat_stone + 匹配 verify（maid_combat_stone，success 通过）+ save_fields 缺默认字段 → 首次/重复通过、读档阶段命中 `STORY_SAVE_DEFAULT_MISSING`（证明读档读变异副本 action）。
- **N7/N8 差分断言**：先断言原始 case 成功（`run_registry_case(base)== ""`），再变异，断言变异命中指定错误/状态差（非 `!= ""` 弱断言）。
- **registry 贯穿 precondition/repeat**：注册表每节点补 `repeat_guard`（triggered/success，registry 驱动 _verify_repeat）+ `precondition_hook`（_precondition_impl，call 调度）。**变异逃逸**：repeat_guard 改 WRONG_GUARD → 正向失败；precondition_hook 改 hook_WRONG → call 失败（字段驱动证明）。
- **口径同步**：PASS 文案与注释改为"八负向（N1-N8 含差分逃逸）"，总复验文档同步。
- **save 失败分歧说明**：本环境清理 app_userdata 残留存档后，v1.38 专项及完整 run_tests（PM 方式）均 PASS 无 save failed；PM 复验 save failed 疑为环境残留存档所致，非代码回归。
- **回归**：v1.38 专项 PASS（registry 全贯穿遍历 27 节点 + 双向集合 + 八负向含差分逃逸 + repeat_guard/precondition_hook 变异捕获）；相邻 12 场景全 PASS；全量 60 RUN ×2 EXIT 0。
- 未改数值/地图/存档 v21；等待 v1.38 签署，不开始 v1.39。

## v1.38 五次整改（2026-08-08，决策者模式，registry 全贯穿 + N6 走统一执行器）

- **PM/SE 五次复验拒签 7 项**：N6 未调 run_registry_case、registry_copy 未贯穿辅助函数（_setup/_save/_cross 读全局）、N4/N5 变异覆盖不完整、写入审计非运行时（正则+允许名单）、仍有硬编码验证器。**本整改返工**。
- **registry 全贯穿**：`_setup_via_registry(registry, node_id)` / `_cross_day_result(registry, node_id, ...)` / `_save_load_result(registry, node_id, ...)` 全部显式接收 registry，从传入副本读 test 元数据（含 action/setup/cross_day），不再读全局 `_nodes`。
- **N6 走统一执行器**：`run_registry_case` 增加 token 段验证（用传入 registry 的 swf_evidence+domain），N6 变异副本传入命中 `STORY_TOKEN_MISS`。
- **cross_day 准备/执行分离**：`_cross_day_prepare`（设周期日/复活性 status，夹具）与 `_cross_day_result`（真实 advance_day/dispatch，执行阶段）分离。
- **新增逃逸 N7/N8**：N7 副本中合法 setup 改另一合法 setup（king_dialogue→setup_give_roses 不设 king_rescued→拒开）必须影响结果；N8 副本中合法 action 改 buy_maid_year_pig（读档也用它）必须影响结果——均断言 run_registry_case 返回非 ""。
- **写入审计**：静态扫描覆盖 `_dispatch`/`_dispatch_main`（主流程禁写终态）+ `_cross_day_prepare` 归为夹具（revive 准备合法）。
- **回归**：v1.38 专项 PASS（run_registry_case 全贯穿遍历 27 节点 + 双向集合 + 八负向含逃逸）；相邻 12 场景全 PASS；全量 60 RUN ×2 EXIT 0。
- 未改数值/地图/存档 v21；等待 v1.38 签署，不开始 v1.39。

## v1.38 四次整改（2026-08-08，决策者模式，统一 run_registry_case + 全函数写入审计）

- **PM/SE 四次复验拒签 2 项**：写入审计只扫 `_ready`（_cross_day 有直接写入被漏）、正则不全；六负向未真正调用统一执行器（N1-N6 各自纯函数）。**本整改返工**。
- **统一执行器 `run_registry_case(registry, node_id)`**：接受注册表副本（含变异）与 node_id，执行该节点完整正向流程（setup/首次/重复/跨日/读档），返回 "" 或精确错误码。**正向遍历与六负向共用**。
- **六负向全部传入变异副本**：N1/N2 变异 setup/verify 字段（run_registry_case 命中 STORY_SETUP/VERIFY_DRIVER_MISSING）；N3 变异 action 未知路由（run_registry_case 的 _is_valid_route 命中 STORY_ACTION_ROUTE_MISMATCH）；N4 变异 cross_day 读原值改 daily（run_registry_case 的 _cross_day_result 命中 STORY_CROSS_DAY_MISMATCH）；N5 变异 save_fields 缺默认（run_registry_case 的 _save_load_result 检测 null 命中 STORY_SAVE_DEFAULT_MISSING）；N6 错段 token（同一 _strict_source_hit）。每项负向先断言变异生效再命中。
- **写入审计全函数扫描**：`_assert_write_audit` 扫描全部函数源码，主流程（_ready/_run_node/run_registry_case/_dispatch/_dispatch_main）禁止直接写终态 flag；setup_/_precondition/_cross_day_result/_save_load_result 视为夹具允许；增强正则覆盖 `story_flags["k"]=`、`.set()`、单引号、嵌套容器。**覆盖 _cross_day 等所有函数**（非只 _ready）。
- **token 按节点 domain 段验证**（域级，文档如实标注非节点级）：每节点 `domain` 指向证据段，swf_evidence 每个 token 必须在段内命中。
- **负向逃逸证明**：变异 rescue setup 字段 → run_registry_case 命中 STORY_SETUP_DRIVER_MISSING → 正向断言失败（捕获）✓。
- **回归**：v1.38 专项 PASS（run_registry_case 统一执行器遍历 27 节点 + 双向集合 + 六负向）；相邻 12 场景全 PASS；全量 60 RUN ×2 EXIT 0。
- 未改数值/地图/存档 v21；等待 v1.38 签署，不开始 v1.39。

## v1.38 三次整改（2026-08-08，决策者模式，写入审计+负向同一验证器+token按段+真实地图入口）

- **PM/SE 三次复验拒签 4 项**：写入审计手工自证、六负向未走同一验证器、token 未按节点段验证、后花园/能量塔只查 can_enter_map。**本整改返工**。
- **写入审计接入真实源码**：`_assert_write_audit` 读取测试自身 .gd 源码，用正则 `story_flags["key"]\s*=` 扫描 `_ready` 主流程的终结 flag 直接赋值，断言为空（禁止手工 append 审计记录；审计函数自身被跳过）。
- **六负向复制变异真实注册表**：N1/N2 变异 setup/verify 字段为不存在函数（has_method 检测，与正向 call(setup/verify) 同源）；N3 未知 MAIN 路由；N4 读具体节点 cross_day 原值改 daily（同一跨日执行器）；N5 缺存档默认；N6 错段 token（同一 _strict_source_hit 段验证）。
- **token 按节点段验证**：注册表每节点加 `domain` 字段，`_strict_source_hit` 提取该 domain 证据段，swf_evidence 的每个 source token 必须在该段内命中（错段/错 sprite/错 frame 均失败）。
- **后花园/能量塔走真实地图 dispatcher**：`_dispatch_main` 从 palace/demon_banner 设当前地图后调 `_travel_to("palace_garden"/"energy_tower")`（真实出口 dispatcher，检查 can_travel+can_enter_map+状态反馈）；verify 检查**真实 GameState.current_map_id** 变化（非返回值可伪造）。
- **负向逃逸证明（全部被捕获）**：错段 token（rescue→公主域 sprite1079）失败；cross_day none→daily 失败；地图入口伪造（不切地图）失败。
- **回归**：v1.38 专项 PASS（注册表驱动 27 节点 + 双向集合 + 六负向；负向逃逸 4 项全捕获）；相邻 12 场景全 PASS；全量 60 RUN ×2 EXIT 0。
- 未改数值/地图/存档 v21；等待 v1.38 签署，不开始 v1.39。

## v1.38 二次整改（2026-08-08，决策者模式，真正注册表驱动）

- **PM/SE 二次复验拒签 7 项**：setup/verify 元数据未驱动（硬编码 match）、4 ROUTE 节点走伪 dispatcher、读档只查非 null、cross_day 硬编码、六负向纯函数、token 描述性兜底、无写入审计。**本整改全部返工**。
- **setup/verify 字段真正驱动**：注册表 `test.setup/verify` 改为唯一函数名（setup_<id>/verify_<id>），测试用 `call()` 调度；**变异 setup/verify 字段名即测试失败**（call 报错 + 双向集合 26 vs 27，实证字段驱动，非摆设）。
- **ROUTE 节点走真实入口**：删除伪 `{"route":true}`；prime_minister_king_news/king_dialogue 走 main_original `_open_*` 方法；palace_garden_entry/final_campaign_gate 走 `can_enter_map` 真实门槛检查；pk_champion 走 `finish_pk_race` 真实 API。
- **读档深比较**：保存每个 save_fields 期望值，读档后逐字段 `==` 深比较（非只查非 null）；`_read_field` 支持 story_flags/fuwa_event 等嵌套。
- **cross_day 按注册表 policy**：严格 match daily/weekly/revive/none；weekly 按节点周期日（周六+7/周日+7）。
- **六负向变异真实注册表**：`has_method` 检测 setup/verify 字段缺失（STORY_SETUP_DRIVER_MISSING/STORY_VERIFY_DRIVER_MISSING）+ 路由错配/跨日策略/存档默认/证据 token。
- **证据 token 严格**：删除描述性兜底，`_strict_source_hit` 只允许 spriteNNN/frameNN/game_state/main_original 真实 source token。
- **写入审计**：`_write_audit` 记录终态 flag 写入位置，断言均为 setup/前置夹具，首次流程经真实 API 产生终态。
- **回归**：v1.38 专项 PASS（注册表驱动 27 节点 + 双向集合）；相邻 12 场景全 PASS；全量 60 RUN ×2 EXIT 0。
- 未改数值/地图/存档 v21；等待 v1.38 签署，不开始 v1.39。

## v1.38 整改（2026-08-08，决策者模式，真正数据驱动）

- **PM/SE 复验拒签**：原 v1.38 测试为手写分段（rescue/princess/daily 各写一段），非真正数据驱动；仅检查节点名称存在；存在直接写终态 flag 冒充流程。**本整改重写为真正数据驱动**。
- **注册表增强**：`docs/story_dialogue_registry.json` 27 节点每节点补 `test` 元数据（action/setup/verify/repeat_reason/cross_day/save_fields）；swf_evidence 修正为证据文件真实 source token（spriteNNN/frameNN/main_original/game_state）。
- **测试重写**：`tests/test_story_dialogue_state_machine_scene.gd` 重写为**遍历注册表全部 27 节点**的调度器——每节点用注册表 test 元数据驱动执行前置（零状态差）/首次（真实生产 API）/重复（幂等）/跨日/读档五验证；setup/verify 为 per-node 精确 hook（每节点状态合法不同），但调度由注册表驱动。
- **双向集合比较**：注册表节点集合 == 测试实际执行集合（缺任一节点即失败，实测 27==27）。
- **evidence token 校验**：每节点 swf_evidence 提取 source token 并在证据文件验证命中。
- **无终态 flag 直写**：首次流程全部经真实 GameState API（rescue_king/chat_with_princess/register_pk_race 等）；终态 flag 仅在前置不满足夹具中作为"已终态"条件验证拦截，不冒充首次流程成功。
- **六负向**：变异注册表/路由/默认值/生产状态，走同一验证器命中 STORY_PRECONDITION_UNCOVERED / STORY_REWARD_NOT_IDEMPOTENT / STORY_ACTION_ROUTE_MISMATCH / STORY_SAVE_DEFAULT_MISSING / STORY_NATIVE_ROUTE_LEAKED_TO_AGGREGATOR / EVIDENCE_TOKEN_MISS。
- **回归**：v1.38 专项 PASS（数据驱动 27 节点）；相邻回归 12 场景全 PASS；全量 60 RUN 连续两次 EXIT 0。
- 未改数值/地图/存档 v21；等待 v1.38 签署前不开始 v1.39。

## v1.38 第一阶段（2026-08-07，决策者模式，救王+王国情报域）——已由 v1.38 整改取代

> **更正**：本阶段"新增专项（数据驱动，35 断言）"为**不实声明**——当时测试为手写分段非数据驱动，断言数亦非 35。该测试已由 v1.38 整改重写为遍历注册表 27 节点的真正数据驱动版本。

- **背景**：SE/PM 额度用尽，开发兼决策者自查。v1.37 已正式签署。v1.38 任务书要求十域剧情分支完整性——本阶段交付**救王+王国情报域**完整闭合（主线核心、证据与测试基础最优），其余域按任务书逐轮扩展。
- **新增证据与注册表**：
  - `docs/evidence/story_dialogues_v103_v9.txt`（救王 Rewarded frame6 SWF 原文 native_confirmed；国王/首相情报文本 evidence_gap 如实标注）
  - `docs/story_dialogue_registry.json`（rescue_king / prime_minister_king_news / king_dialogue 三节点：前置/首次/重复/跨日/读档/证据分类/error_codes）
- **六项负向**（mutation_applied 前置 + 同一验证器）：STORY_PRECONDITION_UNCOVERED / STORY_REWARD_NOT_IDEMPOTENT / STORY_ACTION_ROUTE_MISMATCH / STORY_SAVE_DEFAULT_MISSING / STORY_NATIVE_ROUTE_LEAKED_TO_AGGREGATOR / EVIDENCE_TOKEN_MISS。
- **相邻回归**：12 场景全 PASS（story/complete_game_flow/palace/daily_officer/npc_progression/fuwa/pk/war_soul/final_campaign/ending/native_dialogue/main）。
- **范围声明**：本阶段仅覆盖救王+王国情报域；公主/侍女/后花园/日常官/PK/五福娃/战魂/最终战/研究所的 v1.38 状态机闭环待后续阶段（现有实现与测试已存在，未在 v1.38 框架内逐节点登记）。
- 未改数值/地图/存档 v21；未开始 v1.39。

> **REVOKED（综合整改）：本段"完成/无阻断"结论已撤销，v1.38-41 均为待复验状态。**

- 新增 `artifacts/releases/v1.41/最终交付报告_v1.41.md`（任务书第 11 节八项交付要求：构建环境/SHA256/全量长流程/往返存档/清单覆盖/视觉检查清单/已知问题分级/发布包清洁证明），SHA256 与 build_manifest 已同步更新。
- 发布目录现含 7 文件：exe、pck、build_manifest、SHA256SUMS、验收清单、已知问题、最终交付报告。
- 全量 60 RUN 保持两次 EXIT 0。未开始 v1.42。

## v1.41（2026-08-08，决策者模式，发布候选版与人工验收包）——exe 构建 + 长流程验收补齐

- **本轮补齐**：
  1. **exe 实际构建**：安装 Godot 4.6.3 导出模板（官方 tpz 1.26GB），`godot --headless --export-release "Windows Desktop"` 成功——`魔域1.03_v1.41.exe`（100MB）+ `魔域1.03_v1.41.pck`（22MB）；exe 冒烟启动 EXIT 0 无脚本错误；SHA256/manifest 一致性验证通过。
  2. **长流程验收专项**：`tests/test_release_acceptance_scene.gd`——用真实生产 API 串联救王→最终战六类→结局 + 五福娃完整 + 战魂秘密完整 + PK 代表组/当天重复 + 存档 round-trip，全程不直接写终态 flag（负向 RC_FLOW_BYPASSED_PRODUCTION_PATH）。
- **回归**：长流程专项 PASS；全量 **60 RUN**（含 v1.38/39/40/41 共 5 专项）连续两次 EXIT 0。
- **清理**：临时模板文件（tpz/解压目录）已删除，项目内无残留。
- 未开始 v1.42。

## v1.41（2026-08-08，决策者模式，发布候选版与人工验收包）

- **背景**：v1.40 已登记；本版本为发布候选终点，形成待复验（禁止试玩）、可回退定位的 Windows 候选包框架。
- **新增产物**：
  - `export_presets.cfg`（Windows Desktop 导出预设，指向 project.godot run/main_scene=scenes/main.tscn）
  - `artifacts/releases/v1.41/`：`试玩验收清单_v1.41.md`（28 图 + 关键系统/NPC + 长流程 + 用户覆盖值验证）、`已知问题_v1.41.md`、`build_manifest.json`、`SHA256SUMS.txt`
  - `tests/test_release_candidate_scene.gd/.tscn`（RC 健康检查：preset+主场景、28 图、清单覆盖、已知问题 + 五负向）
- **RC 专项负向**：RC_ARTIFACT_HASH_MISMATCH / RC_PRIVATE_SAVE_INCLUDED / RC_CHECKLIST_COVERAGE_MISSING / RC_SMOKE_SCRIPT_ERROR / RC_FLOW_BYPASSED_PRODUCTION_PATH。
- **已知问题**：exe 待导出模板就绪后构建（本机 AppData/Roaming/Godot/export_templates 为空，export_presets 已就绪）；剧本文本 evidence_gap 如实标注；SWF 声道位声明与数据不符；历史 MD 样式警告。
- **回归**：v1.41 专项 PASS；全量 59 RUN（含 v1.38/39/40/41 专项）连续两次 EXIT 0。
- **未新增玩法/数值/地图**；未开始 v1.42。

> **REVOKED（综合整改）：本段"完成/无阻断"结论已撤销，v1.38-41 均为待复验状态。**

- **schema 决策（不升 v22）**：`docs/save_schema_registry.json` schema_decision 登记——v1.36-v1.39 未新增顶层持久字段（v1.38 剧情状态全写 story_flags 内部 key）；story_flags 新 key 由 load 时 .get(key,default) 默认规范化兼容；DTO/原子写入不改变磁盘 JSON 格式；无 v21 无法无损表达的持久信息。按任务书"不得为匹配标题无意义升版"，保持 v21。
- **生产修复**：`scripts/game_state.gd` load_game 增加**未知高版本拒绝**（`loaded_version > 21 -> return false`）——修复"未来版本被旧程序悄悄降级覆盖"缺口。
- **新增产物**：`docs/save_schema_registry.json`（26 字段：路径/类型/默认/范围/规范化/加入版本/迁移规则）+ `tests/test_save_integrity_scene.gd/.tscn`。
- **专项覆盖**：v21 round-trip deep equality、最小旧档默认迁移、损坏输入（错误顶层类型）拒绝且内存不变、未来版本拒绝、超48格背包拒绝 + 六负向（SAVE_TRANSACTION_PARTIAL_COMMIT / SAVE_ATOMIC_TEMP_INVALID / SAVE_MIGRATION_DEFAULT_MISSING / SAVE_SCHEMA_TYPE_MISMATCH / SAVE_OLD_FILE_NOT_PRESERVED / SAVE_FUTURE_VERSION_ACCEPTED）。
- **修正**：损坏案例从非法 JSON（触发 Parse ERROR）改为合法 JSON 数组（parse 成功但非 Dictionary 拒绝）——避免 runner 将故意触发的 ERROR 判为失败。
- **回归**：v1.40 专项 PASS；相邻回归 16 场景全 PASS；全量 58 RUN（含新专项）连续两次 EXIT 0。
- 未改玩法数值/地图/剧情；未开始 v1.41。

> **REVOKED（综合整改）：本段"完成/无阻断"结论已撤销，v1.38-41 均为待复验状态。**

- **背景**：v1.38 十域已收官；本版本建立数值真值表，区分 native/user_override/effective，防后续还原误删用户修改。
- **新增证据与注册表**：
  - `docs/evidence/value_audit_v103_v9.txt`（经验/军功/货币/装备/研究所/掉落 6 领域证据）
  - `docs/value_audit_registry.json`（17 项 value：经验倍率10/Boss军功10/普通1/货币99,999,999,999/品质魔魂1.0/战魂晶石0.5/玫瑰50,250/研究所上限300日6库存100/蜘蛛技能书0.3,0.5,0.6/王后0.4,0.4,0.2/增强月光盒0.5/750000功勋）
- **新增专项**：`tests/test_value_audit_registry_scene.gd`（一致性：effective 与配置匹配、用户覆盖完整、经验只乘一次、军功作用域分离、战魂晶石严格<0.5、概率边界、实现源 .gd 校验）+ 六负向（USER_OVERRIDE_MISSING / VALUE_EFFECTIVE_MISMATCH / VALUE_MULTIPLIER_APPLIED_TWICE / VALUE_SCOPE_MISMATCH / VALUE_PROBABILITY_BOUNDARY_MISMATCH / VALUE_UNREGISTERED_MAGIC_NUMBER）。
- **修正**：玫瑰实现源补真实 .gd 路径（game_state.gd:899）；战魂晶石边界断言修正（0.49 成功/0.5 拒绝）。
- **回归**：v1.39 专项 PASS；相邻回归 13 场景全 PASS；全量 57 RUN（含新专项）连续两次 EXIT 0。
- **未迁移魔法数字**：核心值均在 data/*.json 唯一权威源，生产代码无第二套漂移配置；未迁移项说明在报告。
- 未改存档 v21、未改数值生效值；未开始 v1.40。

## v1.38 第八阶段（2026-08-07，决策者模式，最终战/研究所域，十域收官）

- **背景**：第七阶段（战魂域）已登记；本阶段扩展最终战/研究所域，v1.38 十域全部覆盖。
- **注册表扩展**：`docs/story_dialogue_registry.json` 24→27 节点，新增最终战/研究所 3 节点（final_campaign_victory / final_campaign_gate / research_produce）——前置、首次状态差、重复幂等、跨日复活、读档、证据分类。
- **证据扩展**：`docs/evidence/story_dialogues_v103_v9.txt` 追加最终战/研究所域——六类魔军 + 能量塔门禁 + 跨日复活 + 研究所产量（native_confirmed）。
- **测试扩展**：`tests/test_story_dialogue_state_machine_scene.gd` 新增最终战/研究所正向（击败敌人 dead / 能量摧毁 game_won / 重复击败 already_defeated 幂等 / 主帅门禁 / 跨日复活 / 研究所产量）+ 最终战幂等负向（已灭敌军又触发 → STORY_REWARD_NOT_IDEMPOTENT）。
- **验证器**：新增 _validate_fc_idempotent / _validate_fc_gate（数据驱动，正负共用）。
- **回归**：v1.38 专项 PASS（十域）；相邻回归 12 场景全 PASS；全量 56 RUN 连续两次 EXIT 0。
- **v1.38 十域全部完成**：救王 / 王国情报 / 公主侍女 / 日常官 / 五福娃 / 后花园抽奖 / PK / 战魂 / 最终战 / 研究所（27 节点 / 数据驱动测试 / 证据 180 行）。
- 未改数值/地图/存档 v21；未开始 v1.39。

## v1.38 第七阶段（2026-08-07，决策者模式，战魂域）

- **背景**：第六阶段（PK 域）已登记；本阶段扩展战魂完整闭合。
- **注册表扩展**：`docs/story_dialogue_registry.json` 20→24 节点，新增战魂 4 节点（war_soul_quest_unlock / war_soul_explorer / war_soul_chest / war_soul_complete）——前置（全身极品/魔石/谜宫激活/任务开放）、首次状态差、重复幂等、读档、证据分类。
- **证据扩展**：`docs/evidence/story_dialogues_v103_v9.txt` 追加战魂域——sprite1235 探险家 50,000 路费文本 + sprite1146 锻造师"关于战魂" + sprite1074 无名氏（native_confirmed）。
- **测试扩展**：`tests/test_story_dialogue_state_machine_scene.gd` 新增战魂正向（未达极品不解锁 / 全身极品解锁 / 付费 50000+15时间进谜宫 / 魔石不足 not_enough_stones / 封印之箱守卫显形 / 击败无名氏完成幂等 / 已完成不可再进 / v21 读档恢复）+ 战魂幂等负向（已完成仍扣路费 → STORY_REWARD_NOT_IDEMPOTENT，mutation_applied + 共用验证器）。
- **验证器**：新增 _validate_ws_unlock_precondition / _validate_ws_idempotent（数据驱动，正负共用）。
- **回归**：v1.38 专项 PASS；相邻回归 12 场景全 PASS；全量 56 RUN 连续两次 EXIT 0。
- **范围声明**：战魂 50000 路费 + 战魂之心奖励维持现有冻结覆盖；最终战/研究所的 v1.38 状态机闭环待后续阶段。
- 未改数值/地图/存档 v21；未开始 v1.39。

## v1.38 第六阶段（2026-08-07，决策者模式，PK 域）

- **背景**：第五阶段（后花园/抽奖域）已登记；本阶段扩展 PK 域完整闭合。
- **注册表扩展**：`docs/story_dialogue_registry.json` 18→20 节点，新增 PK 2 节点（pk_register / pk_champion）——前置（星期六/当天一次/三级分组）、首次状态差、重复策略、跨周重置、读档、证据分类。
- **证据扩展**：`docs/evidence/story_dialogues_v103_v9.txt` 追加 PK 域——sprite1161 周六 PK 报名文本 + sprite443 三级冠军奖励文本（60/100/130级，native_confirmed）。
- **测试扩展**：`tests/test_story_dialogue_state_machine_scene.gd` 新增 PK 正向（非周六 not_saturday / 周六报名成功 / 当天重复 already_entered / 三级分组边界 60/61/100/101 / 跨周重置 / v21 读档恢复 pk_race_active+last_pk_race_day）+ PK 分组变异负向（等级映射篡改 → STORY_ACTION_ROUTE_MISMATCH）。
- **验证器**：新增 _validate_pk_register_precondition / _validate_pk_group（数据驱动，正负共用）。
- **回归**：v1.38 专项 PASS；相邻回归 12 场景全 PASS；全量 56 RUN 连续两次 EXIT 0。
- **范围声明**：PK 三级冠军奖励数值维持现有冻结覆盖；战魂/最终战/研究所的 v1.38 状态机闭环待后续阶段。
- 未改数值/地图/存档 v21；未开始 v1.39。

## v1.38 第五阶段（2026-08-07，决策者模式，后花园/抽奖域）

- **背景**：第四阶段（五福娃域）已登记；本阶段扩展后花园/抽奖域完整闭合。
- **注册表扩展**：`docs/story_dialogue_registry.json` 16→18 节点，新增后花园/抽奖 2 节点（palace_garden_entry / lottery_chest）——前置（爵位/魔石/时间）、首次状态差、重复策略、读档、证据分类。
- **证据扩展**：`docs/evidence/story_dialogues_v103_v9.txt` 追加后花园/抽奖域——sprite1071 勋爵门槛文本 + sprite1222 28魔石文本（native_confirmed）。
- **测试扩展**：`tests/test_story_dialogue_state_machine_scene.gd` 新增后花园/抽奖正向（爵位不足拒入/勋爵以上可入 / 抽奖扣28魔石+2时间 / 魔石不足 not_enough_magic_stones 零状态差 / 可重复连续抽）+ 抽奖前置负向（魔石不足仍抽中 → STORY_PRECONDITION_UNCOVERED，mutation_applied + 共用验证器）。
- **验证器**：新增 _validate_lottery_precondition / _validate_garden_precondition（数据驱动，正负共用）。
- **回归**：v1.38 专项 PASS；相邻回归 12 场景全 PASS；全量 56 RUN 连续两次 EXIT 0。
- **范围声明**：抽奖 28 魔石 + 四档概率维持现有冻结覆盖；PK/战魂/最终战/研究所的 v1.38 状态机闭环待后续阶段。
- 未改数值/地图/存档 v21；未开始 v1.39。

## v1.38 第四阶段（2026-08-07，决策者模式，五福娃域）

- **背景**：第三阶段（日常官域）已登记；本阶段扩展五福娃域完整闭合。
- **注册表扩展**：`docs/story_dialogue_registry.json` 12→16 节点，新增五福娃 4 节点（fuwa_start_round / fuwa_beast_battle / fuwa_claim_reward / fuwa_completion）——每节点前置（集齐数/回合激活/战兽击败）/首次状态差/重复幂等/跨日使者刷新/读档/证据分类。
- **证据扩展**：`docs/evidence/story_dialogues_v103_v9.txt` 追加五福娃域——sprite1090 使者文本 + frame10 草原战斗文本 + frame11 福娃 reward（native_confirmed）。
- **测试扩展**：`tests/test_story_dialogue_state_machine_scene.gd` 新增五福娃正向（开始→草原 round_active / 击败→beast_defeated / 领取→found_count+1 回合结束 / 五轮完整集齐 / 最终交付→100000魔石+VIP 幂等 / 跨日使者刷新 found_count 不重置）+ 五福娃幂等负向（最终奖励重复领 → STORY_REWARD_NOT_IDEMPOTENT，mutation_applied + 共用验证器）。
- **验证器**：新增 _validate_fuwa_claim_precondition / _validate_fuwa_idempotent（数据驱动，正向负向共用）。
- **回归**：v1.38 专项 PASS；相邻回归 12 场景全 PASS；全量 56 RUN 连续两次 EXIT 0。
- **范围声明**：五福娃最终奖励（100000 魔石 + VIP）维持现有冻结覆盖；后花园/PK/战魂/最终战/研究所的 v1.38 状态机闭环待后续阶段。
- 未改数值/地图/存档 v21；未开始 v1.39。

## v1.38 第三阶段（2026-08-07，决策者模式，日常官域）

- **背景**：第二阶段（公主/侍女域）已登记；本阶段扩展日常官域完整闭合。
- **注册表扩展**：`docs/story_dialogue_registry.json` 9→12 节点，新增日常官 3 节点（daily_task_accept / daily_task_complete / daily_task_submit_pet）——每节点前置（任务状态/物品/宠物星级）/首次状态差/重复幂等/跨日重置/读档/证据分类。
- **证据扩展**：`docs/evidence/story_dialogues_v103_v9.txt` 追加日常官域——sprite1161 周一至周日任务文本（收集宝石/训练幻兽/突袭/PK赛/地下城魔族将领会议，native_confirmed）+ 地下城入口路由。
- **测试扩展**：`tests/test_story_dialogue_state_machine_scene.gd` 新增日常官正向（quest_service.accept available→active 幂等 / complete_daily_task 消耗物品→功勋 幂等 / submit_pet_for_daily_task 星级10/15/30→5000/10000/50000魔石+1000军功 幂等 / advance_day 跨日重置可重做 / v21 读档恢复 completed_daily_tasks）+ 日常官幂等负向（已交付仍发奖 → STORY_REWARD_NOT_IDEMPOTENT，mutation_applied + 共用验证器）。
- **验证器**：新增 _validate_daily_precondition / _validate_daily_cross_day（数据驱动，正向负向共用）。
- **修正**：测试初版引用了不存在的 quest id（collect_stones）与多重变量名冲突——修正为实际 quest_service.quests（border_raid）与 progression daily_tasks（collect_magic_soul），并加 _daily 后缀消除冲突。
- **回归**：v1.38 专项 PASS；相邻回归 12 场景全 PASS；全量 56 RUN 连续两次 EXIT 0。
- **范围声明**：日常官交付数值（功勋/军功/魔石）维持现有实现（用户冻结覆盖）；后花园/PK/五福娃/战魂/最终战/研究所的 v1.38 状态机闭环待后续阶段。
- 未改数值/地图/存档 v21；未开始 v1.39。

## v1.38 第二阶段（2026-08-07，决策者模式，公主/侍女域）

- **背景**：v1.38 第一阶段（救王+王国情报域）已登记；本阶段扩展公主/侍女域完整闭合。
- **注册表扩展**：`docs/story_dialogue_registry.json` 3→9 节点，新增公主/侍女 6 节点（princess_chat / give_roses / claim_princess_friend_gift / princess_sunday_gift / maid_year_pig / maid_combat_stone）——每节点前置/首次/重复/跨日/读档/证据分类逐项登记。
- **证据扩展**：`docs/evidence/story_dialogues_v103_v9.txt` 追加公主/侍女域——sprite1079 公主 reward1-6 按关系送宠 + 99/999 玫瑰 + 知己礼物（native_confirmed）；sprite1146/1200 侍女年猪 5888 / 战斗力石 2800（native_confirmed）。
- **测试扩展**：`tests/test_story_dialogue_state_machine_scene.gd` 新增公主/侍女正向（每日聊天跨日重置/送宠物/99/999 玫瑰增好感/知己礼物年猪幂等/周日礼物跨周重置/侍女两商品 sold_out 幂等/读档 v21 恢复）+ 公主域幂等负向（已售出仍发奖 → STORY_REWARD_NOT_IDEMPOTENT，mutation_applied + 共用验证器）。
- **验证器**：新增 _validate_chat_precondition/_validate_roses_precondition/_validate_sold_out_idempotent（数据驱动，正向与负向共用）。
- **回归**：v1.38 专项 PASS；相邻回归 12 场景全 PASS；全量 56 RUN（含 v1.38 专项）连续两次 EXIT 0。
- **范围声明**：公主/侍女域的对话 UI 与价格维持原版（5888/2800 冻结覆盖）；后花园/日常官/PK/五福娃/战魂/最终战/研究所的 v1.38 状态机闭环待后续阶段。
- 未改数值/地图/存档 v21；未开始 v1.39。

## v1.37 整改07（2026-08-02，已实施待复验）

- **整改06 拒签四项已修复**（详见 v137_evidence_gate.py v3）：
  1. **负向验证错误集合精确相等**：断言 `set(errors) == 预期集合`，禁止额外错误码；12 个负向全部"精确集合命中"（M2/M7 预期集合为变异的自然多码共现，逐项写明）。
  2. **生产调用点真验证**（GATE_PRODUCTION_MAP）：事件名 ∈ sound_name_map∪{background_music}；production_call_site 解析 文件:行/区间 → 文件存在、行号真实、行含 AudioService.play/FeedbackService.emit 调用、行内字符串字面量声音名映射到本 sound_id（覆盖三元/else 分支与 emit payload 跨行）；新增负向 M9 错误路径 / M10 错误行号 / M11 事件未登记 / M12 指向其他声音——全部精确命中。
  3. **TSV 双向完整比较**（GATE_EVIDENCE_TSV）：attach_sites/文件集合/StartSound 位置逐项双向相等（reg⊆tsv 且 tsv⊆reg）；M8 改为 tsv 侧变异（删除一项）单码命中。
  4. **链检查分档（措辞降级如实）**：档1 same_function（attach 与 start 同一函数/顶层块 + 同一对象 + 行号顺序）——6 项（4/6/8/13/16/20）；档2 separated_bind_play（顶层初始化绑定 + 事件函数播放——同对象同文件，跨函数，报告称"绑定-播放关联"不称同函数）——7 项（3/7/10/11/17/18/19）；StartSound 标签 4 项（123/132/518/520）。
- **回归**：门禁正向 PASS + 负向 12/12 精确集合 + EXIT 0 无残留；classify OK；专项×3 PASS；monster_hit 100/100；全量 ×2（55 RUN EXIT 0）。
- 未改玩法数值/地图/剧情/存档 v21；未开始 v1.38。

## v1.37 整改06（2026-08-02，已实施待复验）

- **整改05 拒签四项阻断已全部修复**：
  1. 标签解析器（v137_tags_parser.py）：SoundRate 位域加 `(flags>>2)&0x03` 掩码（原 5512*(2**(flags>>2)) 把 SoundSize 位混入，rate 输出 2822144 vs manifest 11025）；SWF 路径参数化，v1/v9 两套实际输出（definesound/startsound/exports_tags_{v1,v9}.tsv）逐项 diff **IDENTICAL**；与 manifest 独立字段比较，不一致时**如实报告并不打印 TAGS OK**（rate 37 项全一致；flags 声道位 34 项声明与 MP3 帧不符、19/412/418 的 type 位声明=3 实为 MP3——SWF 自身声明问题，v1/v9 相同）；新增 MP3 帧头独立基准（v137_mp3_check.py）：37 项 rate/channels 与 manifest 全一致（EXIT 0）。
  2. 证据门禁（v137_evidence_gate.py）：临时目录改为项目本地 `.gate_tmp/`（不用系统 TemporaryDirectory，避免 WinError 5），finally 清理（失败路径也不残留）；注册表 pcode_evidence 与 sound_evidence_rectify05.tsv 交叉比对（GATE_EVIDENCE_TSV，M8 负向）；链检查精确化——同声音名、同文件、同一 Sound 对象（registerN/变量名一致）、顺序一致（支持绑定/播放分离模式）；新增 GATE_EVIDENCE_SOURCE（evidence_source 必须为 res:// URI 契约）。正向 PASS + 负向 **8/8** 命中 + EXIT 0 + 无残留。
  3. 分类脚本（v137_classify_37.py）：不再裸硬编码写回——从 sound_evidence_rectify05.tsv 重建 pcode_evidence，逐项校验分类规则（native 必须有 StartSound/链证据+生产事件；used_unconfirmed 必须有生产事件+SWF 证据；evidence_gap 生产事件必须为空），校验失败退出非 0 不写；**保留 evidence_source URI**（测试 H 契约）；"运行分类脚本后注册表契约仍通过"由 gate + 专项实证。
  4. 证据提取（v137_sound_evidence.py）：handler 从 pcode 内容提取（attach 行所属函数 func:name / FFDec 命名兜底 ffdec:on(release)），不再搜文件路径；排除 ConstantPool 行误报（attach 计数 40→20 真实调用）。
- **帧号修正（重大发现）**：整改05 用 FrameLabel 序号计数 StartSound 帧号，全部错位；整改06 改为 ShowFrame 计数（与 FFDec/labels.tsv 一致）：
  - 123：sprite124/**frame7**（攻击帧标签 frame7=攻击）——生产 monster_attack_normal **语义对应 → native_confirmed**（整改05 误标 used_unconfirmed"站立循环"）
  - 132：sprite135/**frame6**（BOSS 攻击帧）→ **native_confirmed**
  - 518：sprite523/**frame27**（飞天连斩帧标签 frame27=飞天连斩）+ frame62 → **native_confirmed**（整改05 误标"裂地爆斩段"）
  - 520：sprite523/**frame63**（星魔剑帧标签 frame63=星魔剑）→ **native_confirmed**
  - 502：sprite523/frame21（**风斩**帧，非整改05 的 frame13）；1196：root/frame13（**皇宫**帧，非卡萨诺城）
- **最终分类（整改06）**：native_confirmed **17** / used_unconfirmed **2**（14/15 生产映射语义不符保留）/ evidence_gap **18** / unrelated 0；provisional_unverified 0 残留。
- 未改玩法数值/地图/剧情/存档 v21；未开始 v1.38。

## v1.37 整改05（2026-08-02，已实施待复验）

- **REVOKED 声明（整改05，以下整改04 不实结论撤销，不得淡化为口径差异或工具限制）**：
  1. **"FFDec CLI 无 p-code 导出"——错误并 REVOKED**。PM/SE 独立复验：FFDec 26.2.1 的 `-help export` 明确列出 `script:pcode`、`script:pcodehex`；`-format script:pcode -export script` 实际执行退出码 0，成功导出 817 个 .pcode 文件（约 1.09 MB）。本工程整改05 已用同一命令导出三类 817 文件（见 v137/ffdec_pcode_export、ffdec_pcodehex_export、ffdec_export_v5 及 *_sha256.txt）。根因：只检查了顶层命令表，未运行 `-help export` 查看格式子选项——严重失职，按不实交付处理。
  2. **"0x1C=Jump" 等所谓标准 opcode 表——错误并 REVOKED**。FFDec pcodehex 权威证明：`0x1C=GetVariable`（无参数，操作数在栈上）、`0x99=Jump`（SI16）、`0x9D=If`（SI16）、`0x81=GotoFrame`（UI16）。v4/v5 把 0x1C 当作 Jump SI16 是**自制解析器的错表**，不是 SWF 不标准。
  3. **v5 的 313/10/0/23（truncated/unknown/push_kinds/nested）只代表自制解析器失败，不得描述为"原 SWF 不标准"**。0x1C 被当作 Jump 吞 2 字节正是 313 个 truncated 的直接来源。v3/v4/v5 保留为失败历史证据，不再作为验收依据，禁止开发 v6。
  4. **"76 项状态矩阵已独立证明"——撤回**。整改04 的 N14 测试直接读取生产代码 `ALLOWED_EVENTS_BY_STATE` 生成预期，属循环自证：生产表整体写错测试仍全绿。整改05 改为测试内独立固化 EXPECTED 表 + mutation 负向（见下）。

## v1.37 整改03（2026-08-02，已实施待复验）

- **REVOKED 声明（整改03，以下结论已被撤销，不可作为当前证据）**：
  1. "58 处 attachSound"——v2 提取，含 0x8E 误读可能；v3/v4 严格审计后结论不同（v4 提取结果见 audit_v4_report.txt）。
  2. "54503 个 action 全覆盖" / "46 个 action 码完整"——基于错误码表（0x1C/0x99/0x9D/0x83/0x93 结构错误）的统计。
  3. "hitsb/next_gw_hit/hitgw 为动画段 label"——v2 将 0x8E 误当 0x8C 的误报；v3/v4 的 GoToLabel 目标与 FrameLabel 定义中均无 hitsb。
  4. "v2 独立验证"——v2 是自写脚本自检，非独立验证；独立交叉验证（v137_independent_check.py）未达一致。
  5. "0x8E GotoLabel"——v2 错误 opcode 说明。
  6. 历史分类（13/18/4/2 等）——按错误证据的分类，已由整改02 的 4/16/6/11 与整改03 的自动统计取代。
- **解析器 v4**（v137_sound_audit_v4.py，按 SWF v19 重写）：数据裁决码表（0x99=Jump SI16、0x9D=If SI16、0x1C 无参数、0x81=GotoFrame UI16、0x26/0x83=2 个 null STRING、0x93=UI16 Color、0x8E=RegisterCount UI8+Flags UI8+ActionBlock）；严格统计（任一非零即 FAIL）。
- **真实 SWF 审计状态（如实）**：unknown_action_codes=0、unknown_push_kinds=0、nested_block_failures=0、**truncated_records=1（Button2 root frame 4 的 ActionOffset=27 与 ButtonRecords 边界重叠——布局与规范假设存在差异，缺口未关闭）**——AUDIT FAIL，退出码 1。
- **fixture 测试**（v137_fixture_test.py，SWF v19 逐字节构造）：全 PASS（含 0x8B/0x8C/0x8E/0x9B/0x83/0x26/0x88+0x96/0x1C/0x8F/Button2）。
- **战斗状态机**：idle 禁止回合内事件、victory_rewards 禁止回合内事件、defeat_rewards 禁止攻击/技能——FEEDBACK_BATTLE_STATE_MISMATCH；per_item 缺非空 claim_operation_id——FEEDBACK_OPERATION_ID_MISSING。
- **end_session 幂等**：end_session(expected_session_id) 只结束匹配且仍活动的 session；idle 不递增；旧 expected 不结束新 session。
- **主场景集成测试**（tests/test_feedback_lifecycle_main_scene）：普通胜利/玩家死亡回城/切图取消/battle_finished 重建——session 只结束一次、最终 idle、旧 expected 保护。
- **专项新增 N11-N13**：状态矩阵（idle/victory_rewards/defeat_rewards 三态拒绝）、operation id 缺失、end_session 幂等（idle 不递增、旧 expected 不结束新 session）。
- 未改数值/剧情/地图/存档 v21/用户覆盖；未开始 v1.38。

## v1.37 整改02（独立记录，2026-08-02，已实施待复验）

# 《魔域 1.03》Godot 重制版版本改动

> 本文件于 2026-07-18 根据现有实现、数据文件和自动化测试恢复。早期改动的原始逐字记录已丢失，下列内容以当前可验证行为为准。

## v0.1：基础框架与原版界面

- 建立 Godot 4.6 工程，采用原版 700×550 逻辑分辨率。
- 接入原 SWF 提取的地图、角色、怪物、界面、音乐和音效素材。
- 搭建原版风格顶部人物/幻兽状态栏、底部功能栏、场景内方向出口和可点击 NPC。
- 背包与仓库扩展为每页 24 格、各 2 页，并加入物品说明、堆叠、分页和原子拖放。
- 修复拖动物品时单格或整页物品消失的问题：只有合法落点才提交交换，失败时完整回滚。

## v0.2：经济、商店与存档

- 杂货商和收藏家恢复独立出售界面及对应 NPC。
- 初始金币、魔石提高到 99,999,999,999；使用 64 位整数保存，可继续增长。
- 商店购买在背包容量不足时整笔回滚。
- 建立 JSON 存档，持续迁移人物、背包、仓库、装备、幻兽、地图、任务和成长数据。

## v0.3：地图、怪物与场景内战斗

- 建立卡萨诺城、皇宫、雷鸣大陆、戈壁、迷梦沼泽、冰宫、亚维特岛、火山、深渊迷宫和地下城等地图链。
- 取消独立战斗交互页，点击场景怪物后直接在地图内战斗。
- 加入浮动生命条、伤害数字、主动技能、幻兽协战、死亡动画、Boss 军功和数据驱动掉落。
- 技能书掉率按修改要求翻倍，强化月光宝盒掉率调整为 50%。
- 战魂晶石激活成功率调整为 50%。

## v0.4：装备与战斗力

- 实现武器、铠甲、靴子、项链独立装备实例和属性汇总。
- 品质精炼与魔魂升级必定成功。
- 实装战魂、天魂、地魂、装备星级和较低魂等级的战斗力加成。
- 战魂加成调整为原版规则的 2 倍。

## v0.5：幻兽与研究所

- 实现幻兽基础属性、等级、经验、出征、最多两只协战、50 级幻化转世和经验球训练。
- 幻兽研究所技术等级上限调整为 300。
- 每次完成产量任务，日生产数量固定增加 2。
- 加入奇异兽库存、生产和购买流程。

## v0.6：成长、军衔、爵位与关系

- 人物战斗经验调整为 10 倍结算，保留经验球原容量。
- Boss 军功统一按原数值的 10 倍结算。
- 军衔和爵位采用原版累计门槛与战斗力加成。
- 玫瑰亲密度加成调整为原版的 10 倍。
- 100/200 亲密度自动解锁 1/2 级“爱的力量”，周日礼物按关系阶段结算且每日只能领取一次。

## v0.7：任务、占领与日常活动

- 加入数据驱动任务日志、接受/击杀/领取状态、每日重置与地下城逐层解锁。
- 加入冰雪精英军功链、幻兽上交任务、60/100/130 级 PK 赛奖励和星期日军饷。
- 地图占领按爵位开放，每天可挑战一次，只能拥有一个保护地，并可领取每日资源。
- 加入金币捐献、每日物品任务、玫瑰赠送和亲密关系阶段。

## v0.8：技能、音效与角色动画

- 实现技能书学习、主动技能伤害、斗志昂扬战斗力加成和“爱的力量”。
- 接入原 SWF 背景音乐及界面、战斗、升级、赠花、幻兽等 37 段音效。
- 玩家使用原 SWF 前后/侧向行走帧，战斗使用原四帧挥剑动作；怪物加入受击和死亡反馈。

## v0.9：原版 UI 与皇宫交互

- 重搭顶部人物与幻兽信息、底部功能按钮、地图边缘方向出口和原版风格窗口。
- 皇宫加入元帅、国王、公主、公主侍女和 PK 赛报名官。
- 日常任务官按星期提供原版活动入口。

## v1.0：持久生命与原版死亡规则

- 人物和两只出征幻兽的当前生命跨回合、撤退、换图和存档持续保留。
- 敌人依次攻击第一只存活幻兽、第二只存活幻兽，最后攻击人物。
- 幻兽死亡每只扣 10 点幸运；人物死亡扣 10 点幸运和当前等级经验的 5%，生命归零并返回卡萨诺城。
- 果子可回满人物生命；新的一天回满全部幻兽生命；爱的力量同时治疗人物和幻兽。
- 存档升级为 v12，兼容旧存档缺少当前生命字段的情况。

## v1.1：主线剧情第一批

- 地下城三层首领首次击败后救出国王；事件永久保存且不能重复领取。
- 救王奖励按原版分支结算：未达到“王”时直接授予“王”，已经是“王”时获得 200,000 魔石。
- 冰宫到雪域边境恢复原版门槛：星期五临时开放，救出国王后永久开放。
- 皇宫新增首相 NPC，提供金币捐献入口、功勋查询、国王消息和爵位说明。
- 国王获救后回到皇宫，提供突击队、守卫军、神秘部队、图腾兽、魔的能量和魔军主帅六项原版战前情报。
- 公主恢复每日一次聊天：亲密度 +1，并按聊天前关系阶段赠送 1 星攻防型、普通/12 星/19 星奇异兽。
- 达到知己后可领取一次年猪；公主侍女可用 5,888 魔石交换另一只一次性年猪。
- 通过 SWF 精灵引用修正皇宫人物图：首相使用 `image_1142`，侍女从误用的公主图改为 `image_1134`。
- 存档升级为 v13，保存救王、知己礼物、侍女交易和公主每日聊天状态，并兼容旧存档。
- 自动化回归扩展为 18 个测试场景，连同主场景冒烟测试全部通过。

## v1.2：最终战役

- 雪域边境新增通往“魔军帅旗”的原版方向出口；新增魔军帅旗、能量塔禁地两张最终战役地图。
- 场景内加入魔军突击队、守卫军、神秘部队、图腾兽、魔军主帅和“魔的能量”，全部沿用点击怪物直接战斗的交互。
- 四支支援部队存活时，分别令最终战役全军攻击、防御、生命、战斗力提高 50%；消灭后即时撤销对应增益。
- 击败魔军主帅后开放能量塔禁地；摧毁魔的能量后写入主线胜利状态并停止跨日复活。
- 恢复原版黑夜复活：国王获救后，只要魔的能量仍存在且有魔军被消灭，新的一天会复活全部六类魔军。
- 恢复最终战役的战魂条件掉落：战魂激活后，四支部队有 25% 概率给战魂之心、失败给战魂晶石；主帅固定给战魂之心。
- 保留 SWF 反查出的 700/800/900/1000/2000 级和原始生命、攻击区间、防御字段；当前实际攻击、防御按重制版现有成长曲线归一化。
- 存档升级为 v14，保存六类魔军存活、能量塔门禁和主线结局状态，并兼容旧档。
- 新增最终战役专项测试；完整自动化回归扩展为 19 个测试场景，并通过主场景冒烟测试。
- 使用真实 OpenGL 渲染生成两张 700×550 场景截图；受当前本地图像查看工具限制，本批只确认渲染文件生成和尺寸，尚未完成肉眼视觉评审。

## v1.3：最终战地图校正与结局评价

- 直接反查原 SWF 根时间轴第 22～27 帧，确认最终战不是 v1.2 的两图合并结构，而是雪域边境、魔中军阵地、魔左军阵地、魔右军阵地、能量塔禁地、魔军帅旗六段。
- 保留内部地图 ID `demon_camp` 作为旧存档兼容入口，但显示和职责改为“魔中军阵地”；新增左右军阵地与独立帅旗地图。
- 按原版分布校正敌人：中军放守卫军和突击队，左军放图腾兽，右军放神秘部队，帅旗放主帅，能量塔放“魔的能量”。
- 按 SWF 位图引用校正背景：雪域边境/中军使用 `image_1271`，左右军使用 `image_1276`，帅旗/能量塔使用 `image_1278`。
- 按原版出口方向校正地图图：中军左接雪域、上接左军、下接右军、右接帅旗；帅旗右接能量塔，且主帅存活时禁止进入。
- 接入原版第 37 帧“游戏结束”评价页：显示游戏天数、总战斗力、人物等级、装备/幻兽战斗力、军衔、爵位、公主关系、金币/魔石和综合评语。
- 评价阈值与 SWF ActionScript 对齐，并保留原版 `maxprice == 7` 的计数怪癖：八类最高评价全部满足时会进入 `> 5` 分支。
- 新增结局评价专项测试，最终战测试扩展为精确地图图、背景和敌人分布断言；完整回归现为 20 个测试场景加主场景冒烟测试。
- 使用真实 OpenGL 渲染生成中军、左军、右军、帅旗、能量塔和结局页六张 700×550 截图；文件和尺寸已验证，受当前本地图像查看工具限制仍未完成肉眼视觉评审。

## v1.4：雷鸣矿洞与原版时间系统

- 对照 SWF 第 8～9 帧恢复“雷鸣大陆 → 雷鸣矿洞”入口，并使用矿洞原始背景位图 `image_1091`。
- 按 SWF 坐标恢复四个可点击矿点；挖矿不进入独立界面，直接在场景中结算。
- 精确恢复矿石产出：每次品质随机 1～10；矿种随机数为 0～199，`>100` 获得金矿，否则获得银矿。
- 恢复原版每天 15 点时间：每次挖矿消耗 1 点，达到第 15 点自动进入下一天，并继续触发幻兽恢复、任务重置和魔军复活等既有跨日事件。
- 金矿、银矿改为带独立品质的非堆叠物品实例，跨背包拖动和存档不会丢失品质。
- 恢复原版矿石价格：银矿品质 1～9 为 `品质×10,000` 金币、品质 10 为 500,000 金币；金矿品质 1～9 为 `品质×10` 魔石、品质 10 为 280 魔石。
- 杂货商出售页显示并收购银矿，收藏家出售页显示并收购金矿；此前修改版的无限金币/魔石出售按钮继续保留。
- 存档升级为 v15，增加当天已用时间与矿石品质字段，旧存档默认从当天 15 点完整时间开始。
- 新增矿洞专项测试，覆盖地图、四矿点、金银边界、品质价格、商人限制、跨日和 v15 存档；完整回归扩展为 21 个测试场景加主场景冒烟测试。
- 使用真实 OpenGL 渲染生成雷鸣矿洞和银矿出售页两张 700×550 截图，文件与尺寸验证通过。

## v1.5：草原五福娃事件与地图链校正

- 逐帧反查 SWF 第 10～11、17、19、28～31、35 帧，确认“草原/草原2”是 2008 五福娃事件专图，并非蜘蛛洞穴；删除早期自建“蜘蛛洞穴”，旧存档落在该地图时自动迁回卡萨诺城。
- 草原和草原2改用原始背景 `image_1097`；雷角风牙兽使用 SWF 坐标 `(483.55, 243)`，战胜后进入草原2；五位福娃依次使用 `image_1101/1104/1106/1108/1110`。
- 恢复五轮事件：2008 奥运使者每天按原版 1～9 随机值出现在对应地图，其中一个结果当天不出现；每轮寻找贝贝、欢欢、迎迎、妮妮、晶晶之一。
- 每位福娃从“999朵白玫瑰、电浆药水、灵魂王”中随机赠送一种；雷角风牙兽按 50%/50% 互斥掉落战魂之心或月光宝盒增强版，不会出现同时掉落或都不掉。
- 新增树心城最终交任务场景，使用 `image_1299` 与五福娃原始摆位；完成后获得 100,000 魔石、研究所 VIP +1。原版任务的技术上限奖励记录为 150，按此前修改要求实际全局上限继续保持 300。
- 蜘蛛恢复到迷梦沼泽原始位置 `(27, 371)`；蜘蛛王后艾达恢复到亚维特岛 `(539.7, 90)`，保留此前已调整的技能书与月光宝盒掉率。
- 地下城不再由虚构洞穴进入：星期日从卡萨诺城日常任务官直接进入一层，一层原版出口返回卡萨诺城或前往二层。
- 校正 7 张串位背景：雷鸣大陆 `image_1066`、戈壁 `image_1226`、深渊迷宫 `image_1268`、地下城一至三层 `image_1281/1287/1292`、PK 赛场 `image_1294`；亚维特岛恢复 `image_1252`。
- 存档升级为 v16，保存五福娃轮次、当前进度、战兽胜利、最终奖励与每日使者位置；兼容 v3～v15，并迁移废弃地图 ID。
- 新增五福娃专项测试；完整自动化回归扩展为 22 个测试场景，连同主场景冒烟测试全部通过。
- 使用真实 OpenGL 渲染生成草原、草原2和树心城三张 700×550 截图；文件与尺寸验证通过，受当前本地图像查看工具限制未做肉眼视觉评审。

## v1.6：后花园与抽奖房

- 反查 SWF 第13～15帧，纠正早期假设：后花园与抽奖房不是串联地图；皇宫方向出口进入后花园，皇宫抽奖官直接传送到抽奖房。
- 后花园恢复勋爵门槛（1,000功勋），沿用皇宫背景并加入原始花园装饰；公主、丫环2、丫环1按 SWF 坐标 `(310.3,169.45)`、`(251.5,220.95)`、`(447.55,219.95)` 放置。
- 丫环2保留5,888魔石交换一次性年猪；丫环1恢复每天一个高级战斗力石的交易，价格2,800魔石，跨日自动重置。
- 皇宫新增抽奖官，使用原始 `image_1147` 人物素材与原版说明；进入抽奖房免费，每次点击宝箱才扣28魔石并消耗2点当天时间，没有每日次数限制。
- 抽奖房恢复8个宝箱及 SWF 精确坐标，使用关闭帧 `image_1213`；根脚本中的宝箱颜色名不参与抽奖计算，因此八个宝箱共享相同概率与奖池。
- 四档概率按随机数0～999精确恢复：`<20`为2%、`20～69`为5%、`70～449`为38%、其余为55%。
- 奖池加入噜噜幻兽、圣天使幻兽、8/12星奇异兽、幻魔之心、魔魂之心、99朵白玫瑰等缺失对象，并覆盖原版技能书、经验球、玫瑰、灵魂材料与月光宝盒奖励。
- 动态装备恢复武器、头盔、项链、衣服、手镯、战靴六类随机选择，并保存品质、魔魂、孔洞数；装备和强化界面同步扩展为六栏。
- 存档升级为v17；新增后花园/抽奖房专项测试，完整自动化回归扩展为23个场景，连同主场景冒烟测试全部通过。
- 使用真实 OpenGL 3.3 / NVIDIA 渲染生成 `palace_garden.png`（700×550，310,203字节）与 `lottery_room.png`（700×550，324,573字节）；日志与尺寸验证通过，受当前本地图像查看工具限制未做肉眼视觉评审。
## v1.7：三组 PK 赛场与星期六报名

- 逐帧反查 SWF 第31～33帧，将原先错误挤在同一张图的60、100、130级擂主拆为三张独立赛场；三图均使用原始`image_1294`背景，并按原坐标分别放置唯一擂主。
- 皇宫移除普通方向出口进入 PK 赛场的临时实现；现在只能通过 PK 赛报名官参赛，三个赛场的退出方向均返回卡萨诺城。
- 恢复原版报名规则：仅星期六开放；人物等级`<=60`进入60级组、`61～100`进入100级组、`>100`进入最高组；报名即消耗当天次数，失败或主动退出不能重复报名。
- 恢复60级组的高级飞天连斩、40,000基础经验、27,000魔石；100级组的高级飞天连斩、月光宝盒增强版、150,000基础经验、56,000魔石。
- 恢复最高组的高级斗志昂扬、月光宝盒增强版、电浆药水、999朵白玫瑰、250,000基础经验和82,800魔石；修正早期误写为熔岩药水的问题。人物经验仍按已定修改规则乘10。
- 存档升级为v18，保存最近参赛日与比赛进行状态；无有效比赛状态却停留在赛场的旧档会安全迁回卡萨诺城。
- 新增 PK 赛专项测试，覆盖地图、分组边界、星期门槛、同日限次、擂主站位、奖励和存档；完整自动化回归扩展为24个测试场景，并通过主场景冒烟测试。
- 使用真实 OpenGL 3.3 / NVIDIA 渲染生成`pk_arena_60.png`（700×550，381,553字节）、`pk_arena_100.png`（700×550，383,166字节）和`pk_arena_130.png`（700×550，385,794字节）；日志与尺寸验证通过，受当前工具限制未做肉眼视觉评审。
## v1.8：战魂封印谜宫与战魂秘密任务

- 反查 SWF 第34帧和装备脚本，新增原版“战魂封印谜宫”：背景`image_1297`、出生点`(190,245)`，并保留原版没有方向出口的封闭结构。
- 恢复任务开启条件：武器、头盔、项链、衣服、手镯、战靴六件装备全部达到极品品质4后，装备锻造师才会给出戈壁线索；任务开启后不会因换装而丢失。
- 戈壁探险家仅在任务期间出现，位置为`(18,325.1)`；带路费为50,000魔石，并按`times_go(15)`消耗完整一天时间，保留进入前当天已用时间的余数。
- 谜宫内点击原始坐标`(649.95,217.95)`的封印之箱后，无名氏才在`(505.2,175.95)`出现；无名氏使用 SWF 通用怪物第28帧对应的`image_0101`。
- 无名氏最低50级，人物等级高于50后随人物同步成长；战斗力按原版`100+等级`计算。胜利固定掉落一个战魂之心，不虚构经验、军功或货币奖励。
- 击败无名氏后永久解开战魂秘密并返回卡萨诺城；失败同样返回主城，但任务保留，可再次向探险家付费挑战。
- 存档升级为v19，保存任务开启/完成、谜宫进行和守卫显形状态，并迁移无有效状态却停在谜宫的存档。
- 新增战魂谜宫专项测试；完整自动化回归扩展为25个测试场景，并通过主场景冒烟测试。
- 使用真实 OpenGL 3.3 / NVIDIA 渲染生成`war_soul_explorer.png`（700×550，350,141字节）、`war_soul_maze_chest.png`（700×550，400,798字节）和`war_soul_maze_guardian.png`（700×550，404,922字节）；日志与尺寸验证通过，受当前工具限制未做肉眼视觉评审。
## v1.9：野外普通怪物种群与原版站位

- 新增自动提取脚本，直接读取 SWF 第8、16～21帧的怪物放置动作，得到名称、等级、Boss标志和坐标，避免继续人工猜图。
- 雷鸣大陆至深渊迷宫七张地图恢复19种普通怪物、29个场景实例：等级从1级巨杰士到130级叹息骑士。
- 同种怪物的多个实例改用唯一内部点击键，战斗时统一解析回基础怪物ID；修复此前字典覆盖导致点击一个实例却绑定另一个实例的问题。
- 按通用怪物精灵帧标签接入`image_0049`～`image_0089`；各地图等级Boss使用原版BOSS帧`image_1072`并恢复逐帧提取坐标。
- 蜘蛛修正为原版45级和`image_0063`，蜘蛛王后艾达修正为80级和`image_0075`；王后的高级技能书与月光宝盒增强版固定掉落概率保持不变。
- 19种普通怪物使用与现有重制版相容的归一化战斗属性，保存`original_level`证据字段；人物仍获得10倍经验，幻兽获得基础经验。
- 恢复通用素材掉落：灵魂晶石50%，灵魂王概率为等级除以1000且最高13%，60级以上有5%月光宝盒增强版；普通怪物不提供Boss军功。
- 新增野外种群专项测试，覆盖地图清单、19个等级、29个站位/帧、多实例ID和掉落边界；完整回归扩展为26个测试场景，并通过主场景冒烟测试。
- 使用真实 OpenGL 3.3 / NVIDIA 渲染生成`world_population_thunder.png`（700×550，386,733字节）、`world_population_desert.png`（700×550，342,012字节）和`world_population_abyss.png`（700×550，323,079字节）；当前工具限制下未做肉眼视觉评审。

## v1.10：卡萨诺城 NPC 原图、原位与功能补齐

- 从原版 SWF 第12帧解析并恢复卡萨诺城11个NPC/交互点的精确人物帧、尺寸和坐标。
- 杂货商改回`image_1156`与坐标`(166.8,120.3)`；经验导师使用`image_1126`与坐标`(517.65,377.9)`，修复两者长期错图。
- 收藏商店与收藏家拆为两个独立交互：收藏商店使用`image_1134`，收藏家使用`image_1138`并恢复原版收藏品说明。
- 补回此前遗漏的宝石合成师；装备打造师、幻兽幻化师、幻兽研究所、仓库管理员和日常任务官全部改回原位置。
- 移除不属于卡萨诺城第12帧的公主，以及与地图占领报名官重复显示的卡萨诺挑战者。
- 经验导师可把背包中符合条件的装备换成满经验球，严格采用原版品质、洞数和魔魂加算公式。
- 宝石合成师支持灵魂王、魔魂之心、幻魔之心、高级经验石、高级战斗力石五类20合1配方；操作失败不会消耗材料。
- 新增主城专项测试，自动验证11个NPC、原坐标、原人物帧、两类对话、11球提炼边界和20合1合成；测试总数增至27个，主场景冒烟测试通过。
- 真实OpenGL 3.3渲染通过：`city_npcs_native_layout.png`为396,381字节，`city_experience_mentor_dialogue.png`为354,522字节，两张均为700×550。
## v1.11：原版野外掉落算法与随机装备实例

- 重新反查原 SWF 的 `drop1/drop2`，废弃 v1.9 中为了先跑通流程而使用的通用猜测表；普通怪不再错误地以50%掉灵魂晶石，也不再掉灵魂王或月光宝盒增强版。
- 恢复幸运倍率 `baoli = 1 + (幸运+1)/100`。19种普通怪金币为 `60×(20+原始等级)×baoli`，灵魂晶石为 `2.5%×baoli`，随机装备为25%。
- 普通怪随机装备恢复原版六类等概率、等级归一化（低于10级为1级、高于100级为125级、其余向下取整到十级）、品质四次覆盖判定、魔魂0～12和5%洞数分支。
- 七个等级Boss固定1,000金币、必掉灵魂晶石；解开战魂秘密后，必掉晶石约50%替换为战魂晶石。灵魂王概率为`Boss原始等级/1000`，并必掉一件优良或极品随机装备。
- 60级及以下Boss恢复`5%×baoli`月光宝盒；60级以上Boss的月光宝盒增强版按此前修改要求固定50%。
- 蜘蛛恢复五个独立分支：三本普通技能书、灵魂晶石、月光宝盒；技能书基础概率继续按修改要求翻倍，再乘幸运倍率。
- 蜘蛛王后恢复八个独立分支：三本高级技能书、灵魂晶石、灵魂王、幻魔之心、魔魂之心及月光宝盒增强版；高级技能书倍率翻倍，增强版固定50%。
- 野外随机装备以安全令牌进入待领取队列，领取后生成独立装备实例；背包满时保留待领取，等级、品质、魔魂、洞数可在说明中查看并随v20存档读回。
- 新增原版掉落专项测试，并修正三项仍保护早期猜测表的旧断言；完整回归现为28个自动测试场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染生成`native_loot_equipment.png`（700×550，346,012字节）；当前工具限制下只验证文件、尺寸和渲染日志，未进行人工目视评审。
## v1.12：原版场景内点击战斗与窄血条

- 反查原SWF通用普通怪`sprite124`、Boss`sprite135`及共用血条形状，确认怪物自身使用约80×8的窄血条，没有此前重制版底部400×36的战斗操作栏。
- 怪物血条改为直接覆盖在地图角色上方的无主题80×8色块，避免Godot默认主题把8像素高度强制放大；怪物名称、伤害飘字和受击反馈仍留在场景中。
- 删除场景战斗操作栏。左键点击怪物立即普通攻击并可连续点击；右键点击怪物自动施放当前已学会的最高伤害主动技能。
- 战斗锁定到具体怪物实例，不能在回合中切换到另一个怪物；攻击动画尚未结束时点击其他目标同样不会替换战斗会话。
- `_start_battle`等遗留入口改为寻找当前场景对应怪物并直接开战，旧独立战斗面板不再有可达入口。
- 地图移动和方向出口仍在战斗中锁定；人物/幻兽生命、技能倍率、伤害、掉落、军功、剧情结算、失败回城及怪物原地刷新保持原流程。
- 场景战斗和主场景测试改为验证80×8血条、左右键攻击、目标锁定和旧入口重定向；完整28场景回归与主场景冒烟测试全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染生成`scene_battle_native_click.png`（700×550，362,986字节）及`scene_battle_native_skill.png`（700×550，365,646字节）。文件、尺寸和日志已验证；当前工具限制下未做人工目视评审。
## v1.13：原版怪物悬停信息与标签清理

- 反查原SWF通用普通怪/Boss的`onRollOver`、全局`tsxs`提示对象与`sprite972`边界，恢复怪物悬停提示；提示框使用原始123×74尺寸。
- 悬停文字恢复为“名称(等级)/战斗力/经验增加”三行；普通怪缺少独立战斗力字段时按原始等级显示，Boss和特殊怪优先使用数据中的`combat_power`。
- 经验提示读取当前已修改的10倍人物经验倍率，显示“经验增加900%”，不把总倍率错误写成“增加1000%”。
- 恢复原版四档颜色比较：怪物高于人物5点以上为深灰、略高为红、相同为白、较低为绿。
- 提示框按原版相对怪物左上角定位并做700×550画面边界约束；移出怪物、换图、打开面板或开战时自动隐藏。
- 野外战斗怪物不再常驻显示名称标签，避免同屏大量文字遮挡；修复怪物死亡刷新后隐藏标签被战斗控制器错误重新显示的问题。
- 新增怪物悬停专项测试，覆盖123×74尺寸、三行文字、900%经验提示、四档颜色、位置、移出隐藏、Boss战斗力字段和标签重生状态。
- 完整回归扩展为29个自动化测试场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染生成`monster_native_hover.png`（700×550，295,996字节）；文件、尺寸和日志已验证，当前工具限制下未做人工目视评审。
## v1.14：原版怪物攻击动作

- 反查SWF通用普通怪`sprite124`：站立段复用单张怪物位图，攻击段没有虚构的新图片，只在第7～9帧把本体横移5像素再复位。
- 反查通用Boss`sprite135/sprite131`：攻击段使用`image_0127.png`、`image_0129.png`两张186×173专用位图，原始12fps时间轴在站立图基础上使用相对偏移`(-33,-42)`。
- 场景战斗改为人物攻击/怪物受击后再播放怪物反击；普通怪执行5像素突进，Boss切换两张原生攻击帧，随后精确恢复原位置、尺寸和站立图片。
- 闪避和攻击出征幻兽时同样播放怪物攻击，不再只显示人物/幻兽掉血数字；玩家击杀怪物的回合不会错误播放反击。
- 增加动画状态保护：攻击过程中切换目标仍被锁定；代码强制取消战斗时会立即恢复怪物站立图，旧Tween回调不会把新会话覆盖回旧状态。
- 新增怪物攻击动画专项测试，覆盖普通怪突进、Boss帧/尺寸/偏移、自然还原和中途取消还原；场景战斗测试同步按原版动作时长校正。
- 完整回归扩展为30个自动化测试场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染生成`boss_native_attack.png`（700×550，457,643字节）；文件、尺寸和日志已验证，当前工具限制下未做人工目视评审。
## v1.15：原版 Boss 受击形状与死亡移除

- 反查通用Boss`sprite135`的“被攻击”段及子动画`sprite134`：前两帧保持站立图，后两帧切换`shape133`，并非此前重制版统一使用的旋转抖动。
- 新增严格SWF形状提取器，解析`shape133`的四边形边界、位图97填充和原始矩阵；生成123×148透明资源`boss_hit_native.png`，相对站立图偏移`(0,-7)`，SHA256为`7492EBACBC57DF5908DE27D0BF997B6967FD6A592A05BF7A8534F68D64E5C5A1`。
- 通用等级Boss受击按原12fps节奏播放站立两帧/变形两帧，再进入反击；击杀回合在受击后按原脚本直接移除实例，不再使用无SWF依据的0.3秒淡出。
- 普通怪`sprite124`没有“被攻击”标签或替代位图，因此取消统一旋转，只保留原版血条更新和伤害飘字。
- 动作分类改为检查实际通用Boss站立素材，不再把所有数据字段`is_boss=true`的地下城/剧情首领错误换成等级Boss攻击与受击图片。
- 强制取消战斗会同时还原受击与攻击状态；旧Tween回调不能污染换图或下一次战斗。
- 新增受击/死亡专项测试，覆盖123×148尺寸、偏移、资源路径、取消还原、普通怪无虚构帧、特殊Boss视觉分类及死亡立即隐藏。
- 完整回归扩展为31个自动化测试场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染生成`boss_native_hit.png`（700×550，467,621字节）；文件、尺寸和日志已验证，当前工具限制下未做人工目视评审。
## v1.16：PK 擂主与领地挑战者的原版 Boss 路由

- 反查SWF根时间轴第31～33帧：60、100、130级PK赛BOSS均由`char1074`包装并传入`isBoss=true`，三者不是此前误用的`image_0447/0449/0451`人物图。
- 三档PK擂主统一恢复`image_1072.png`的101×132通用Boss站立边界，保留SWF原坐标`(539.7,218.85)`、`(479.7,197.85)`、`(403.6,159.45)`；攻击和受击因此正确进入v1.14/v1.15的原版Boss动画。
- 反查六个地图占领报名官实例（SWF第8、12、16～19帧）：点击“我要挑战”后才把挑战者名称、`gwnum=1`和`isBoss=true`写入战场；挑战者不应以探险家`image_1231.png`常驻野外。
- 删除雷鸣大陆、戈壁、迷梦沼泽、冰宫和亚维特岛五个错误常驻挑战者；六张可占领地图均在报名成功后临时生成101×132通用Boss并立即进入场景战斗。
- 六名领地挑战者及三档PK擂主的数据分类同步恢复为`is_boss=true`；当前基础军功均为0，因此不会额外改变既有数值奖励。
- 领地挑战成功后立即重建当前地图，清除临时挑战者；失败仍按既有规则返回卡萨诺城，同日挑战限制和保护地结算保持不变。
- 扩充PK赛与领地专项断言，覆盖原版图片、尺寸、坐标、Boss动画分类、报名前不可见、报名后生成、进入战斗、结算清理及九个`isBoss`标记。
- 31个自动化测试场景和主场景冒烟测试全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染生成`v116_pk_native_boss.png`（700×550，379,022字节）及`v116_territory_native_boss.png`（700×550，445,548字节）；文件、尺寸和日志已验证，当前工具限制下未做人工目视评审。
## v1.17：人物原版待机与四方向行走时间轴

- 反查人物总剪辑`sprite523`：第6帧“站立姿势”使用子剪辑`sprite459`，第8～11帧依次为向下走`sprite469`、向上走`sprite474`、向左走`sprite481`和向右走`sprite488`。
- 待机、向下和向上均恢复原版8帧时间轴：第一张位图保持4帧，第二张保持4帧；不再把行走第一帧错误当作静态待机图。
- 左右方向恢复原版10帧节奏：第一张保持4帧、过渡帧1帧、迈步帧4帧、过渡帧1帧；统一按SWF的12fps播放，不再使用此前约7.7fps的两帧循环。
- 向右行走改用独立的`image_0482/0484/0486`，不再复用`image_0475/0477`的向左素材；向左补回此前遗漏的`image_0479`迈步帧。
- 静止时使用`image_0455/0457`待机循环；战斗动作结束后也恢复`image_0455`，避免从战斗突然跳回行走帧。
- 新增人物原版移动专项测试，覆盖5类剪辑长度、首帧、关键切换帧、12fps循环、左右独立素材和战斗控制器共享同一场景人物。
- 完整回归扩展为32个自动化测试场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染生成`v117_native_player_idle.png`（700×550，350,379字节，SHA256 `20E334C4DB99581AC76D683DDCE970EB02DAAC00ECAC8A0935D8EE345C2F57A8`）；文件、尺寸和日志已验证，当前工具限制下未做人工目视评审。


## v1.18：原版顶部人物/幻兽卡与合体、解体机制

- 反查SWF根时间轴和`sprite932/sprite925/sprite920`，将顶部四段恢复到原始根坐标：人物卡`(1,2)`、两张幻兽卡`(198,2)`与`(394,2)`、地图卡`(590.5,2)`；不再由横向容器平均挤压。
- 人物卡恢复头像、名称、生命、体力、经验三条数值和“等级/爵位/军衔”底栏；地图标题与地图名拆分显示。幻兽卡按种类切换原始头像，并恢复等级、“召回”和“合体/解体”按钮。
- 出征与合体改为两个独立状态。未合体的出征幻兽只提供幻兽战斗力，不攻击、不承伤，也不把攻防加入人物；合体后攻防并入人物，怪物伤害依次由第一只、第二只合体幻兽承受，最后才伤害人物。
- 移除重制版此前每只出征幻兽额外执行一次独立伤害的错误实现；合体幻兽与人物表现为同一次合并攻击，承伤时使用人物与合体幻兽合并后的防御。
- 两只新手幻兽按原脚本默认出征并合体；召回会清除合体，普通重新出征默认保持解体。战斗动作空档可直接使用顶部按钮切换，当前生命会先提交，再刷新进行中的战斗配置。
- 恢复人物体力上限`100 + 10×人物等级`，1级为110；初级/高级飞天连斩消耗0/5体力，初级/高级星魔剑消耗10/20体力。体力不足时按原版退化为普通攻击，升级回满体力。
- 存档升级为v21并保存人物体力与幻兽合体状态；v20及更早存档中原本已出征的幻兽自动迁移为合体，避免读取后人物战力与承伤顺序突然变化。
- 新增顶部幻兽卡、按钮联动、合并攻防、死亡幻兽剔除、伤害代理与体力专项测试；完整回归扩展为33个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v118_native_pet_hud_combined.png`（700×550，349,514字节，SHA256 `BD30FC188843093EB9263A6AEF34A1C26EC84A9659DD9E9E5CC045EB490E8D63`）和`v118_native_pet_hud_split.png`（700×550，349,754字节，SHA256 `85C9BD7A6BEFA6008E3C8AC7F340230AEED1304A55910F7B12FDA7DDA4B1F603`）；修正了首轮截图发现的头像拉伸与地图文字重叠。

## v1.19：原版固定底栏与按钮三态

- 反查SWF根时间轴第6帧，移除会平均挤压子控件的临时`HBoxContainer`；底栏恢复到`(0,512)`和700×38原始舞台宽度。
- `sprite908`日期/时间区按根坐标恢复：日期文字位于`(5.2,516)`，剩余时间框位于`(185.7,518)`；星期不再显示阿拉伯数字，改回“星期日～星期六”的原版中文名称。
- 音乐控制恢复到`(245.7,516)`并由滑动开关改为复选框；垃圾箱恢复到`(301.05,513.65)`，继续提供丢弃物品提示。
- 背包、装备、幻兽、技能、VIP五个按钮恢复SWF固定x坐标`378/441.7/505.2/568.7/631.7`和62×32边界，不再随文本宽度漂移。
- 直接解析`DefineButton2`角色893～897及共享形状890～892，恢复正常`#ff6600`、悬停`#ffcc33`、按下`#ff9900`三态；幻兽保留黄色文字，VIP保留青色文字。
- 五个原版按钮继续接入现有背包、装备、幻兽、技能和成长面板；测试实际触发背包开关、音乐静音/恢复和垃圾箱提示，避免只做静态外观。
- 新增底栏专项测试；完整回归扩展为34个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v119_native_footer.png`（700×550，351,524字节，SHA256 `69AF4B7F1C89F1E66CB3AA8A17A2A810DBD2B4DECE96C248542A88D76695684A`）；日期、时间、音乐、垃圾箱和五个按钮均无重叠、截字或错位。

## v1.20：原版场景边缘切图入口

- 逐帧解析SWF角色1071的根时间轴放置矩阵，恢复41×90竖向、90×41横向的原始命中范围；左右入口继续竖排地图名，上下入口继续横排，不再使用130×46和72×90的四个通用文字按钮。
- 纠正此前放反的主城方向：亚维特岛位于右侧`(654.5,264.95)`，皇宫位于上方`(211.05,124.3)`，雷鸣大陆位于左侧`(2.1,264.95)`，戈壁位于下方`(391.05,403.9)`。
- 为雷鸣大陆/矿洞、皇宫/后花园、戈壁、迷梦沼泽、冰宫、亚维特岛、火山、深渊迷宫、雪域与最终战、地下城、PK赛和树心城登记原版边缘坐标；保留既有扩展路线时使用同尺寸边缘入口，不再退回大块灰色按钮。
- 新入口直接绘制绿色目的地、红色“进入/返回”和朝向场景边缘的箭头；悬停、按下、等级或剧情未解锁状态均有独立视觉反馈。
- 入口仍接入原有等级、爵位、雪域任务、地下城层数和最终战门槛；专项测试实际验证69级亚维特岛入口灰显、点击主城左侧进入雷鸣大陆及切图后的入口刷新。
- 完整回归扩展为35个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v120_native_map_exits.png`（700×550，398,870字节，SHA256 `481B7044C7BF025729C9952386C09CFC773AC326367D2AF66286E1FAF0FFA176`）；四个入口均贴合场景边缘，没有额外地图面板，也未遮挡主城NPC。

## v1.21：原版背包与仓库窗口

- 反查SWF `sprite572`：背包打开坐标为`(453,300)`，底板`shape566`边界为247×207，物品数组为`xn=6/yn=4`；删除此前`(410,218)`、286×258的通用窗口布局。
- 背包恢复6×4共24个可见格、40像素格距和原版底部信息区；“背包”标题回到`(177.95,172.4)`，金币/魔石分别回到局部y=165/186.4，关闭钮回到`(221.25,172.15)`。
- 反查SWF `sprite565`：仓库打开坐标为`(208,220)`，底板`shape562`边界为247×287，物品数组为`xn=6/yn=6`；仓库每页由错误的24格恢复为6×6共36格。
- 仓库标题恢复到`(102.3,11.9)`，关闭钮恢复到`(220.4,4.95)`；打开仓库时按原版同时显示左侧仓库和右下背包，不再把两个相同大小窗口横向均分。
- 解析共享关闭按钮角色564及形状547/548/550，使用橙色外圈配深灰/浅灰/黑色的正常、悬停和按下状态，替换此前方形“—”按钮。
- 背包和仓库继续保留现有48格容量；超过原版单页可见格数的旧存档物品通过紧凑页码切换访问，不截断、不迁移、不丢失。
- 新增背包/仓库专项测试，覆盖窗口坐标、尺寸、24/36格布局、标题、货币区、关闭操作与旧存档分页；完整回归扩展为36个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v121_native_storage.png`（700×550，303,424字节，SHA256 `906A7355E3DF7FFE02FA4804F74E4EAC50321FA8E8B480FD7A55A9D0175A5C2C`）；仓库、背包、格子、标题、页码、货币和关闭钮均无重叠或越界。

## v1.22：原版杂货商、收藏商店与收藏家交易窗

- 反查SWF `sprite609/shape573`，将杂货商恢复到`(285,90)`和333×207：左侧为4×3购买区，右侧为4×3出售区，不再使用360×378的通用滚动面板和购买/出售整页切换。
- 杂货商“购买”按钮恢复到局部`(11.85,11.35)`，“出售”恢复到`(173.8,11.35)`，关闭钮恢复到`(303.95,6)`；金币栏回到右栏顶部，购买物品只显示原版式图标格，名称、价格和说明通过悬停查看。
- 反查SWF `sprite645/shape610`，将收藏商店购买窗恢复到`(285,90)`、167×207和4×4可见购买区；标题、42×24购买钮及`(138.35,5.95)`关闭钮按子角色位置还原。
- 反查SWF `sprite652/shape646`，将收藏家出售窗恢复到`(260,300)`、167×207和4×3出售区；标题恢复“与收藏家交易”，魔石栏位于局部y=65.35，关闭钮位于`(141.55,4.95)`。
- 42×24购买/出售按钮按共享形状537/538/539恢复深灰、浅灰、按下深色三态；三个窗口的关闭钮继续复用角色564的橙色圆环三态。
- 保留修改版核心规则：点击杂货商“出售”可反复获得99,999,999,999金币，点击收藏家“出售”可反复获得同额魔石；金矿/银矿仍会出现在对应4×3出售格中并按品质单独结算。
- 新增商店专项测试，覆盖三套坐标/尺寸、双栏结构、12/16格购买区、12格出售区、关闭钮及无限金币/魔石按钮真实结算；完整回归扩展为37个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v122_native_grocery.png`（700×550，345,281字节，SHA256 `E47561C910B2AB1671F54AB7470CAFD31B9898DD605879D4B636546FC7A792A4`）和`v122_native_collector.png`（700×550，369,189字节，SHA256 `C3EE0222B303788EB6C5311D19B8A2E82ED1AD060D0D41B306ACB229A9D5D677`）；双栏比例、图标格、货币栏、标题和关闭钮均无错位或越界。

## v1.23：原版 NPC 对话窗口与头像

- 反查SWF `sprite882/shape834`，将NPC对话恢复到根坐标`(200,80)`、362×269.3和`#ffcc99`杏色底板；删除此前通用灰色弹窗布局。
- 从SWF逐个提取公主、元帅、首相、领地官、宝石合成师、经验导师、抽奖官、幻化师、收藏商店、收藏家、两名侍女、杂货商、PK官、锻造师、日常官、研究所、探险家、奥运使者和国王对应的19类原生头像资源。
- NPC头像恢复到局部`(4,4)`和64×64，正文恢复到`(78.5,6.35)`，玩家头像恢复到`(287.8,153.5)`；讲话者名称、正文和头像随既有NPC剧情动态刷新。
- 最多六个回答恢复到x=5、y=155.7起的16像素行距和270×16命中范围；悬停使用原形状878的`#ffcc66`，按下使用`#ff9900`。
- 关闭钮恢复到`(338.5,2)`的21×21橙色圆环三态；回答仍派发原有商店、任务、剧情、传送和战斗动作，并恢复NPC对话音效。
- 新增对话专项测试，覆盖坐标、尺寸、颜色、头像映射、六行选项、三态、关闭和商店动作；完整回归扩展为38个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v123_native_dialogue_grocery.png`（700×550，332,668字节，SHA256 `0E647FF8E3552A19F3D61FE51B574D7CD18F9623CD02B730D7BB544FB5AFF3FD`）和`v123_native_dialogue_princess.png`（700×550，334,069字节，SHA256 `A1DCD314F74955806E043948F0E8FAF1DCDE16A360FA00D311AAD5662E4749DD`）。
## v1.24：原版人物信息与六栏装备窗口

- 反查SWF `sprite561/shape524`，将人物/装备窗从340×384通用列表恢复为`(210,85)`、243×289和`#666666`原生底板；原版通过底栏“装备”按钮开关，没有虚构标题栏关闭键。
- 六个64×64装备位按SWF实例名和矩阵恢复：头盔`(173,28)`、项链`(173,92)`、手镯`(173,156)`、衣服`(109,220)`、武器`(45,220)`、战靴`(173,220)`。
- 人物信息标题恢复到`(77,5.3)`；左栏恢复姓名、等级、爵位、军衔、公主关系、生命、体力、经验、攻击、防御、闪避、幸运和总战斗力，并随人物、装备、幻兽和关系信号实时刷新。
- 装备位显示当前独立装备实例的原图标；悬停继续调用已有完整物品说明，品质、魔魂、洞数、战魂、天魂和地魂数据不丢失。
- 恢复战魂等级与天魂/地魂套装20×20原生标志；左下`(3,224.4)`的42×24“详细”按钮使用原版深灰/浅灰/按下三态。
- “详细”按SWF `sprite546/shape544`从本窗左侧`(-179.05,24.3)`展开178×231、`#ffcc99`战斗力评定页，显示等级、幻兽、装备、军衔、爵位、技能和总战斗力当前值。
- 新增人物装备窗专项测试；完整回归扩展为39个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v124_native_equipment.png`（700×550，402,242字节，SHA256 `BC5C8CB8F7D0F117527E599BF50D1A1612941537E06C235B06F6B527432E1CBE`）和`v124_native_equipment_detail.png`（700×550，393,040字节，SHA256 `9878F6995F4922E72F66BC95B3E6A3046D8486C207EB3B95D79FAC7DE5D43E16`）。
## v1.25：原版幻兽背包与详情操作

- 反查SWF `sprite787/shape732`，将幻兽窗口从492×418通用表单恢复为`(0,150)`、213×333和`#666666`原生底板；每页固定显示4只幻兽。
- 四个193×69幻兽栏恢复到局部`(10,12/82/152/222)`，并按幻兽种类接入`image_0738/0740/0742/0744/0748/0750`原生头像；选中栏恢复原版高亮和随行移动的“出征/召回”按钮。
- 底部恢复`(5,300)`上一页、`(87,298)`下一页、`(117,302)`丢弃、`(164,302)`详细及橙色圆形关闭钮；每页4只之外的幻兽继续分页访问，不截断存档。
- “详细”按SWF `sprite775`从本窗右侧`(210,0)`展开213×333，显示等级、转世、星级、生命、攻防和战斗力，并恢复六字改名入口。
- “丢弃”按SWF `sprite786`恢复`(6,93)`、203×123的确认层；出征中的幻兽拒绝丢弃，避免误删战斗成员。新增`rename_pet`和`discard_pet`原子状态接口。
- 既有经验球训练、幻兽日常上交与主副幻兽幻化入口收纳到详情页，功能与失败提示保持可用。
- 新增幻兽背包专项测试；完整回归扩展为40个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v125_native_pet_bag.png`（700×550，375,884字节，SHA256 `B8DF46F6D85CA24C4DB0A2B3AD5DA9528998FBDF5A6EB775A6CC1628834E6FD1`）和`v125_native_pet_detail.png`（700×550，336,805字节，SHA256 `7E9FE34C5404C6903F846936C4587378E9B4E342CAC994351E29164C0B51FF3D`）。

## v1.26：原版六栏技能窗口

- 反查SWF `sprite731/shape697`，将技能窗口从445×430滚动列表恢复为`(23,220)`、183×253和`#666666`原生底板。
- 六个40×40技能栏恢复到局部`(10,5/45/85/125/165/207.6)`；分别使用原版`image_0698/0703/0708/0713/0718/0726`图标，不再用纯文字列表代替。
- 飞天连斩、星魔剑、斗志昂扬和爱的力量实时显示当前等级及伤害、战斗力或成功率；原SWF中尚未接入当前重制机制的两格保留为“未掌握”，不虚构新技能数值。
- 技能书继续在背包中双击学习；点击已掌握技能可查看效果，点击“爱的力量”可直接按现有成功率施放并治疗人物与幻兽。
- 关闭钮恢复到`(156.45,2.95)`的21×21橙色圆环；整个窗口保持在底栏上方，不遮挡原版固定功能键。
- 新增原版技能窗专项测试；完整回归扩展为41个自动化场景加主场景冒烟测试，全部通过。
- 使用真实OpenGL 3.3 / NVIDIA渲染并人工目视验收`v126_native_skill_panel.png`（700×550，394,808字节，SHA256 `295FD72576016665B0C4594F97AE62E2672EFEB7EDF13B697B520446488DEBAF`）。
## v1.27：原版装备精炼窗口与完整养成入口

- 反查SWF `sprite662/shape653`，将精炼窗口恢复到`(238,375)`、191×133和`#666666`原生底板，关闭钮恢复到局部`(160,4.5)`。
- 窗口直接显示当前装备、品质、魔魂、战魂、天魂和地魂状态，并把品质提升、魔魂提升、战魂激活、天魂和地魂五类操作收纳到原窗口范围。
- 保留修改版数值：品质与魔魂必成、战魂晶石50%成功、战魂加成双倍；材料不足和未装备状态都有明确反馈。
- 新增原版精炼窗专项测试并使用真实OpenGL截图验收`v127_native_refining.png`（700×550，389,093字节，SHA256 `EBBF8B8215279DC68315F59E4819AD94F0A1BDC109D5C6CD6D0728F92E4DB9A5`）。

## v1.28：军衔、爵位与关系完整操作流

- 将分散在元帅、首相、公主和日常官对话中的成长操作整理为243×289紧凑窗口，保持原版灰色底板、橙色圆形关闭钮和底栏以上的安全范围。
- 接通军功与当前军衔、军衔战斗力、周日军饷、爵位功勋、两档金币捐献、99/999玫瑰、每日功勋任务、周日公主礼物及关系阶段。
- 所有按钮根据星期、当日次数、背包材料和领取状态实时灰显，避免重复领取或无效扣除。
- 新增完整成长流专项测试并使用真实OpenGL截图验收`v128_progression_flow.png`（700×550，363,782字节，SHA256 `E4CC341A8E182C13C5767E063EE6167CC2203E19B44A42A65636D77FB14396AA`）。

## v1.29：原版幻兽研究所与自动生产周期

- 反查SWF `sprite827/shape823`，研究所恢复到`(250,180)`、203×183和`#666666`，购买、投资和关闭按钮分别按原子角色坐标落位。
- 技术等级继续从10级起步、20级解锁购买、最高300级；产量任务每次固定+2，不改原剧情任务次数。
- 时间推进改为每天自动按当前日产量生产奇异兽，跨周自动提升一次技术等级；删除需要玩家手动点击“生产一天/推进一周”的开发期按钮。
- 新增研究所完整周期专项测试并使用真实OpenGL截图验收`v129_native_research.png`（700×550，380,056字节，SHA256 `E2EECA9919050C5A88F08DE5AE7F18C835FC0365072AF3EA4B74C93CB9970575`）。

## v1.30：统一任务簿与九阶段主线目标

- 将三条数据驱动任务固定显示在`(200,80)`、362×269.3原版杏色对话底板内，统一处理未领取、进行中、可领取和已完成状态。
- 新增九阶段主线目标：地下城三层、四路魔军、魔军统领、魔的能量和结局；目标随剧情状态自动前进，不再依赖玩家记忆下一步。
- 任务簿保留领取与领奖操作，目标卡同时显示下一张地图和目标名称；关闭钮复用原版橙色圆环三态。
- 新增任务簿和主线引导专项测试并使用真实OpenGL截图验收`v130_quest_flow_guide.png`（700×550，343,336字节，SHA256 `0AD470454B450AF0B0CBB756158FE897093BA72B1008CBE061AD2C75CA0EF3CE`）。

## v1.31：真实路线引导与主线端到端闭环

- 世界服务新增双向最短路径计算；任务簿的路线按钮从人物当前地图计算到主线目标的真实路径，不再使用固定文字或额外地图面板。
- 对只能经NPC进入的地下城补充“找日常任务官进入”提示，其余目标继续沿原版场景边缘切图入口逐段前往。
- 新增端到端流程测试：领取地下城任务、连通三层并救出国王、依次击败四路魔军、魔军统领和魔的能量，最终验证结局页真实显示。
- 五版本完成后完整回归扩展为46个自动化场景加主场景冒烟测试；使用真实OpenGL截图验收`v131_final_route_guide.png`（700×550，345,192字节，SHA256 `1F9F59775BD57F0B50311DCED293C693B67B815D1F106A1CA98D4841C056B5F1`）。

## v1.32：基线冻结与无图像证据注册表

- 新增 `docs/swf_evidence_registry.json` 结构化文本注册表，让无图像能力模型可凭文本定位已确认 UI/动画的 SWF 证据，不再依赖截图判断正确性。
- 为已还原的顶部 HUD、固定底栏、场景边缘出口、背包、仓库、杂货商/收藏商店/收藏家、NPC 对话、人物装备与战斗力详细页、幻兽背包/详情/丢弃确认、技能窗、精炼窗、研究所、怪物悬停、任务簿逐项登记：SWF 角色号、父角色、根帧、根矩阵（根位置）、形状边界、子控件局部矩阵、帧数、色值、Godot 文件与测试文件。
- 登记五项已确认动画：人物待机/四方向行走（sprite523 及 sprite459/469/474/481/488 子剪辑，8/8/8/10/10 帧，12fps）、人物四帧挥剑攻击、普通怪 5 像素横移（sprite124）、通用 Boss 攻击帧（sprite135/sprite131，image_0127/0129，186×173，偏移 -33,-42）与受击形状（sprite134/shape133，123×148，偏移 0,-7）。
- 对全部 111 个 `image_####`/派生演员资源登记已知用途与证据来源，按运行时/legacy 旧面板标注作用域；并登记 28 张地图的背景位图与角色号。
- 共享角色单独标注以避免与各窗口主角色冲突：关闭钮 char564（shapes 547/548/550）、42×24 购买/出售按钮 shape537-539、通用 Boss 站立 image_1072、对话回答悬停 shape878。
- 任务簿复用对话底板 sprite882、人物攻击段复用 sprite523，均以 `reuse_of` 字段显式标注为同源复用而非角色号冲突；验证测试据此允许重复角色号。
- 新增 `tests/test_swf_evidence_registry_scene.gd` 与 `.tscn` 验证测试：检查注册表 JSON 结构与版本号、Godot 文件与测试场景路径存在、image 资源文件存在、SWF 角色号不重复冲突（重复角色必须有 `reuse_of` 文档化复用关系）、地图与共享角色字段完整，并断言注册表不含“看截图/目测”类依赖图像的验收描述。
- 不修改玩法、UI、用户覆盖数值或存档；仅新增 `docs/` 文档与测试文件，并在 `run_tests.ps1` 测试列表末尾追加新场景。
- 专项测试、相邻回归（原版底栏、背包仓库、商店、装备、顶部幻兽卡）与全量回归全部通过；完整回归现为 47 个自动化场景加主场景冒烟测试。
- 已登记证据缺口（注册表 evidence_gaps）：部分 UI 窗口的 SWF 根时间轴帧号、顶部 HUD 三段 sprite932/925/920 与四张卡的 1:1 对应关系、HUD 卡片 SWF 原始底色尚未在现有反查中单独确认，留待后续版本补证。

## v1.33：活动主场景退役旧地图/战斗面板

- 从活动主场景 `main_original.gd` 彻底移除隐藏的旧 MapPanel/BattlePanel 运行时依赖：删除 MapPanel/BattlePanel preload、map_panel/battle_panel 成员声明、_build_panels() 中两个旧面板的创建/信号连接/add_child/hide，并从 _hide_all_panels() 关闭集合中移除两个旧成员。
- 保留 `_start_battle(monster_id)` 作为旧动作路由兼容入口：只在当前地图 interactive_actors 中查找怪物并调用 _engage_world_monster 走场景内战斗，找不到时显示“当前场景中找不到该怪物。”，不创建/加载/显示任何战斗面板。NativeExitButton 与 _travel_to() 行为不变。
- 迁移引用 `main.battle_panel` 的测试/捕获脚本：test_main_scene、test_scene_battle_scene、capture_v112_scene_battle 的旧断言改为递归检查活动场景树无 map_panel.gd/battle_panel.gd 脚本节点。
- 将手工场景 visual_battle_scene 从已废弃的 `main.battle_panel._attack()` 改为使用当前场景怪物与 SceneBattleController 的 _engage_world_monster 演示，确保可实例化。
- 新增专项测试 test_no_legacy_panels_scene：递归检查活动场景树无旧面板节点；读 main_original.gd 文本确认不再 preload/声明旧成员；有效怪物 _start_battle 由 SceneBattleController 接管并锁定目标；不存在怪物 _start_battle 只更新状态提示不创建面板不替换目标；场景边缘出口仍由 NativeExitButton 完成地图切换。
- 历史脚本 main.gd/map_panel.gd/battle_panel.gd/shop_panel.gd 保留未删（运行时去耦与物理文件清理拆分为两个可独立回滚的步骤，留待后续版本）；image_0051 保持 active+legacy，注册表专项测试继续通过。
- 修复预存在脆弱性：test_scene_battle_scene 的 respawn 断言需约 5 秒（含 await 2.3 秒等待 scene_battle_controller 的 2.2 秒复活），headless 高帧率下原 --quit-after 600 帧不足 5 秒会提前终止测试导致 respawn 断言未真正执行。将 run_tests.ps1 场景测试 --quit-after 由 600 提到 1200 作为超时上限，并要求每个自动化场景日志必须出现 `^PASS ` 完成标记，缺少 PASS 即判失败并保留日志诊断；1200 帧仅作超时上限，不替代 PASS 校验。
- 不修改玩法、UI 布局、数值、掉落、剧情或存档（仍 v21）；未重写 SceneBattleController。
- 专项、相邻回归（test_scene_battle_scene/test_main_scene/test_native_map_exits_scene/test_original_ui_scene/test_complete_game_flow_scene/test_swf_evidence_registry_scene）与全量回归通过；完整回归现为 48 个自动化场景加主场景冒烟测试。
- 活动运行时已退役旧面板，历史脚本尚保留。
- v1.33 整改 01：run_tests.ps1 增加 PASS 完成标记校验（每个自动化场景日志必须出现 `^PASS `，失败区分退出码/脚本错误/缺PASS并保留日志诊断，成功才删日志）；注册表 56 处易漂移的 `main_original:<数字行号>` 等行号改为 81 项 `source_refs`（path+token）稳定定位，并清理 known_uses/evidence_source 中的全部数字行号；注册表专项测试新增 source_refs 的 path 存在、token 命中、active 资源至少一个非legacy来源校验；开发进度.md 顶部测试数 47 统一为 48。

## v1.34：剩余系统入口的原版流程校正

- 区分“原版NPC对话入口”与“VIP便捷聚合窗”：元帅战功查询、首相金币捐献、公主玫瑰赠礼、日常官宝石交付六处原错误路由到 progression 聚合窗的入口，改为各自专属NPC对话action；ProgressionPanel 保留为底栏VIP便捷入口，文档明确称“便捷聚合窗”。
- 元帅：新增 military_status 专属军功查询对话（显示当前军功/军衔/下一军衔差额/军衔战斗力），替换元帅主页与消息页两处 progression；提取 _open_marshal_dialogue；军饷沿用 claim_military_salary（周日/有军衔/未领取）。
- 首相：新增 prime_donate 专属捐献对话，档位 750,000金币=1功勋（原版 sprite1183 证据确认），调 donate_gold_for_nobility；成功扣金币加功勋、金币不足不变、留对话链。75,000,000 档无原版证据，未纳入。
- 公主：新增 princess_gift 专属赠礼对话，档位 99朵/999朵白玫瑰（原版 sprite1206 证据确认），调 give_roses；成功消耗玫瑰加好感、玫瑰不足不变；保留聊天/知己礼物/周日礼物独立路径。
- 日常官：新增 daily_deliver 专属交付对话，魔魂晶石(500功勋)/灵魂王(2000功勋)独立action，调 complete_daily_task；成功消耗材料加功勋、重复/材料不足不扣；周三/四幻兽入口、周五/六/日任务入口保留。
- 研究所：初次仅核对sprite827窗口证据；整改02进一步确认sprite1174为NPC对话并补齐四个原版选项，sprite827继续作为专属窗口；资助/产量任务/购买机制不变。
- 新增 docs/npc_progression_flow_registry.json，登记五组NPC（元帅/首相/公主/日常官/研究所）选项的 action映射/状态差/SWF证据/evidence_tokens，evidence_source 用稳定 res:// 路径，无数字行号。
- 新增 test_native_npc_progression_routes_scene 专项测试：从NPC入口验证军功查询/捐献/赠礼/交付/研究所专属流程，全程断言 progression_panel 未打开，并验证注册表结构+evidence_source/token 可验证。
- NPC action不再用 progression 代替四类原生流程；代码注释与文档区分“便捷聚合窗”与原版NPC窗口。
- 不改数值/用户覆盖/地图/战斗/掉落/UI大框架/存档（仍v21）；未删除legacy脚本；未开始v1.35。
- 专项、相邻回归与全量回归通过；完整回归现为 49 个自动化场景加主场景冒烟测试。
- 证据缺口（待PM决策）：首相 75,000,000 档无原版证据；日常官原版“灵魂晶石”+500功勋与当前“魔魂晶石”+灵魂王2000功勋冲突，按任务书4.4.6不改数值，列待PM决策。
- v1.34 整改 01：补齐NPC端到端测试（研究所从NPC入口进入后资助/产量任务/购买成功+无库存失败、公主周日礼物成功+重复不重复、周一/周二分别按钮交付魔魂晶石/灵魂王）；新增 docs/evidence/npc_progression_actions_v103_v9.txt 独立SWF证据文件，注册表19项 evidence_source 改为指向该txt（不再循环自证实现代码）并加 classification（17 native_confirmed/1 evidence_gap/1 intentional_divergence）；补登元帅“军情查询”+“关于军衔”两个证据缺口；75,000,000档标 convenience_override、日常官数值差异标 intentional_divergence（PM决定DEC-134-01/02，不改数值）；开发进度顶部测试数48统一为49。
- v1.34 整改 02：区分研究所NPC sprite1174（原版四选项：进入/研究所的当前信息/关于2008奥运使者/提高产量任务）与研究所窗口 sprite827；补齐四个NPC选项实现（_open_research_dialogue 迁移为专属函数，新增 research_info/research_olympic_info/research_production_task 三个action，只读分支不改状态，产量任务调 complete_research_production_task）；证据文件加 sprite1174 章节、sprite827 改“专属窗口”、元帅“少尉至上校”修正为“少尉至上尉”；注册表 research 组1->4项（总19->22项，20 native_confirmed/1 evidence_gap/1 intentional_divergence）；测试加固公主周日礼物物品数量+1/不变断言与研究所资助( funding_magic_stones_per_level)/产量任务(production_task_cost)/购买(research_pet_price)资源扣除断言。不改研究所数值/存档/UI，测试总数仍49。
- v1.34 整改 03：修复 _open_research_olympic_info 五福娃待交付状态判断（由错误的 beast_defeated 改为 found_count >= FUWA_NAMES.size() && !completion_claimed，因领取每轮福娃后 beast_defeated 恢复 false）；专项测试补 2008奥运使者未完成/待交付/已领取三状态只读断言（含显式 beast_defeated=false 证明不依赖错误字段，断言显示文本与 fuwa_event/VIP/魔石不变）；清理注册表 evidence_gaps 残留“少尉至上校1000战功”为“少尉至上尉1000战功”并加注册表 JSON 军衔文本断言；修正 v1.34 正文旧研究所结论（sprite827 路由一致）为当前事实（初次仅核对窗口，整改02确认 sprite1174 NPC）。不改数值/存档，测试总数49。

## v1.35：全地图演员与交互覆盖审计

- 对28张地图进行结构化演员与交互覆盖审计，建立“SWF证据→注册表→运行时实体→参数化测试”闭环。
- 新增 docs/world_interaction_registry.json 世界交互注册表：28图（与 maps.json 一一对应），107个实体（NPC/怪物/交互物/装饰），48个出口，每实体登记 entity_id/kind/asset/position/size/action_id/appearance_condition/classification/evidence_source。
- 新增 docs/evidence/world_interactions_v103_v9.txt SWF证据文件：28图分节记录背景/SWF根帧/出口/encounters + 反查工具命令 + 证据缺口。
- 运行时审计metadata：_add_actor 加 world_entity_id/world_entity_kind，_add_decoration 加 world_entity_kind=decoration，NativeExitButton 加 world_entity_kind=exit/entity_id。
- 新增 test_world_interaction_registry_scene 专项测试：注册表结构（28图+maps.json一致+背景/资源存在+entity_id/action_id唯一+出口目标存在）+ 运行时逐图实例化检查实体边界在700×550画布内且具有审计metadata + 动态矩阵（palace国王未救/已救）。
- 审计发现 grass_reward 无交互实体（仅装饰），属正常；thunder_boss_10 等Boss坐标底部在512-550底栏区域，按SWF原版坐标保留不移动。
- 不改数值/用户覆盖/战斗/掉落/存档（仍v21）；未删除legacy脚本；未开始v1.36。
- 专项、相邻回归与全量回归通过；完整回归现为50个自动化场景加主场景冒烟测试。
- 证据缺口：28图出口 direction/position 待逐图用 inspect_swf_exit_matrices 确认；部分地图SWF根帧号为版本历史值，未逐图二次确认；怪物/装饰逐图scope对照待补。
- v1.35 整改 01：修复六项阻断问题——(1)28图精确运行时比较（实体集合/资源/坐标/尺寸/action双向匹配+出口+背景+装饰mouse_filter）；(2)填充dynamic_scenarios至7项（皇宫国王/沙漠探险家/战魂谜宫/草原2空图）并参数化测试expected_present/absent；(3)证据token链修复（116实体token全部命中证据文件，evidence_source!=implementation_source）；(4)分类修正（实体分类由全部native_confirmed改为evidence_gap，268处UNCONFIRMED登记为结构化evidence_gaps）；(5)装饰稳定entity_id（_add_decoration加entity_id参数+9调用点+world_entity_id meta）；(6)action可达性验证（norm在main_original.gd出现）+边界700×512+例外。六项负向验证全部确认失败。不改数值/存档/拓扑，测试总数50。
- v1.35 整改 02：完成四个检查点全部要求--A)28图双向精确比较(entity/kind/action/asset/position/size+装饰mouse_filter+出口direction/target/position/size/disabled/action_route+背景)；4Boss越界native_overlap_exception(runtime_unconfirmed)+assert(has_exception)；gap映射用(map_id,object_id)复合键；token验证覆盖实体+出口+overlap+impl_source。48出口含UNCONFIRMED改evidence_gap。B)43动态场景(皇宫2+五福娃16+PK4+战魂3+沙漠2+最终战7+草原/树心3+领地6)数据驱动循环+fixture隔离+完整期望集合比较+组覆盖断言。C)12类真实action路径(NPC/商店/仓库/研究所/怪物/Boss/领地/矿点/抽奖/战魂箱/出口/传送)每类执行真实dispatcher验证面板/session/状态差/地图变化。D)9项内存负向验证全部命中目标断言。war_soul_explorer kind统一npc。不改数值/存档/拓扑，测试总数50。
- v1.35 整改 03：修复整改02全部不实交付--(1)61个动态场景均登记独立expected_dynamic_present/absent，测试不通过_condition_active自动推导而是与注册表独立期望比较；PK 9场景(3组x inactive/active/finished)、最终战13场景(6敌人alive/dead+game_won)、领地12场景(6图x爵位不足/满足)、战魂4场景(含secret_completed缺失态)、五福娃16场景(absent用messenger_map=空)。(2)完整GameState隔离：snapshot/restore包含level/magic_stones/inventory/demon_campaign/owned_territory/pending_territory/last_territory_challenge_day/last_territory_reward_day/nobility_merit/last_pk_race_day/completed_daily_tasks，apply_fixture重置全部字段+inventory重新初始化48空槽。(3)12类真实action路径：矿点断言非空inventory槽位增加、抽奖断言magic_stones-28、战魂箱揭示守卫后点击守卫进入battle session、NPC断言speaker_label正确、仓库通过_handle_dialogue_action而非直接toggle。(4)新增_world_action_route()只读dispatcher分类器，全量action路由验证+未知action返回UNSUPPORTED。(5)出口metadata验证(world_entity_id/direction/target/action_route)+边界恢复x+width<=700检查。(6)9项负向验证：neg7改为传入dispatcher返回UNSUPPORTED而非源码搜索，neg5修改scenario独立期望，neg6从运行时快照删除装饰。不改数值/存档/拓扑，测试总数50。
- v1.35 整改 04（最终验收）：PM/SE 于 2026-08-01 独立复验 A-F 全部关闭；最终注册表为28图/116实体/48出口/61动态场景/76结构化gap，实体未确认字段继续标记 evidence_gap。23字段状态隔离、105项 world action、11项 dialogue、24个领地状态、出口metadata与runtime rule、9项负向及50场景+主场景冒烟全部通过；v1.35正式验收，v1.36未开始。

## v1.37 整改01（2026-08-02，已实施待复验）

- **去重修复**：dedupe key 按注册表策略（per_attack=session+sequence+event / per_battle=session+event / per_level=含 level / per_item=serial 区分同 item 双领取）；顺序状态改为仅 accepted 后提交（_sequence_transition 纯函数）；新增"不同事件共用 dedupe key"负向证明被拒事件不伪造 attack_started；三回合逐回合事件集合/dedupe key/声音请求断言。
- **技能生命周期**：skill_started/skill_failed_stamina 移到新回合建立后（修复第二回合被旧回合校验拒绝的 bug）；成功施放补 skill_finished（同 session/回合、先于 player_attack_finished）；flying_slash 2 级扣 5 / star_sword 1 级扣 10 精确断言；同种子 BattleSession 伤害状态差；体力不足无 skill_started/finished。
- **AudioService 真实记录**：play() headless 先记录再跳过播放器；test_play_history/test_play_count/test_reset_play_history；controller 幸运耗尽改 battle_retreat 语义事件（无直接 AudioService 战斗调用）。
- **dodge 确定性**：test_dodge_roll 强制 roll（不用概率循环）；断言 dodge 事件、无 player_hit、无受击声音、HP 不变、无伤害浮动文字。
- **busy 快照**：turn/体力/HP/怪物HP/事件数/声音数/调度器数全快照比较。
- **level_up**：连续两次升级不被吞（per_level 含 level）；同 level 拒绝；一次结算一次提示已在注册表说明。
- **SWF 证据链 v2**：DefineSound SoundSize→1/2 字节、SoundType→1/2 声道；覆盖 DefineButton2/DoInitAction(SpriteId)/ActionConstantPool/Push 池索引（46 码 54503 action 全覆盖）；**修正 v1 错误结论**——找到 58 处 attachSound（sprite37/561/662/731/882/932/963/1071/1074/1079-1235 等），分类按 binding/trigger 规则重新统计（v1.37 整改02）：native_confirmed 4（123/132/518/520，StartSound+生产接入）/ used_unconfirmed 16（attachSound 绑定+生产使用）/ evidence_gap 6（0003/0015/399/551/684/810，名称推断 only）/ unrelated 11（无生产用途：0009/0012 + 412/418/496/502/540/879/1145/1196/1296）；三方比对（swf_parser vs manifest vs 注册表）+ 篡改命中 FEEDBACK_SOUND_FIELD_MISMATCH + 证据路径从注册表读取 + token 段归属校验。
- **负向新增 7 项**（第二回合去重、skill_finished 缺失/错回合、同 item 双领取、per_level 错误去重、被拒事件污染顺序状态、AudioService headless 未记录、SWF 格式字段不匹配），保留原 5 项。
- 删除 tests/_diag_ref.gd/.tscn；run_tests.ps1 quit-after 1200→2000；专项 1 PASS/0 错误/0 泄漏；9 项相邻回归通过；未改数值/剧情/地图/存档 v21/用户覆盖；未开始 v1.38。

## v1.37：技能、战斗特效、掉落与声音反馈校正（2026-08-02，已实施待复验）

- 新增唯一生产反馈通道 `CombatFeedbackService`（autoload）：`emit()` 唯一事件入口（测试与生产同一流程，禁止直接 append 历史）；单调递增序号 `monotonic_index` 作为测试顺序 oracle（不依赖系统时间）；session_id（每场战斗）+ attack_sequence（每回合）双重校验；dedupe key 同一逻辑事件只接受一次；顺序规则状态机（攻击先于命中/完成、死亡后无反击、victory→reward_queued→loot、体力降级互斥等）；事件历史有界 512；headless 下仍记录事件（AudioService.play 兼容入口保留）。
- 声音证据审计（work/v137/v137_sound_audit.py + v137_sound_scopes.py + v137_sound_strings.py，SWF 魔域1.03_v9）：DefineSound tag14=37 与 manifest 完全一一对应；StartSound tag15=17 处（root 帧3/4/13/31/32/33、sprite124 帧7=普通怪攻击、sprite135 帧6=Boss 攻击、sprite523 帧12/21/27/62/63=玩家技能段、sprite541 帧3=三态按钮、sprite880 帧2=对话按钮、sprite1146 帧1/2=锻造师交互）；ExportAssets 导出 20 个原始文件名（指向怪物.wav/学习技能.wav/传送.wav/出征幻兽.wav/back.mp3 等）；FrameLabel 段名（裂地爆斩/风斩/飞天连斩/星魔剑）。
- 37 段音频逐项分类：native_confirmed 13（123/132/412/418/496/502/518/520/540/879/1145/1196/1296）、used_unconfirmed 18（0001-0008/0010-0013/0014/0015/0016-0018/0020，其中 0014=传送.wav 用作 attack、0015=出征幻兽.wav 用作 victory 为用途不符登记 gap）、evidence_gap 4（399/551/684/810）、unrelated 2（0009 其它升级.wav 未接入、0019 back.mp3 背景音乐流式）。
- 生产接入：SceneBattleController 攻击/技能/命中/反击/受击/闪避/死亡/胜利/奖励入队全部改走反馈事件；GameState 升级（level_up）、掉落领取（loot_claimed/loot_claim_failed，背包满失败不删除队列）接入；busy 期间重复输入维持拒绝（不重复扣体力/伤害/声音）；cancel_battle 与 handle_map_change 后旧 session 事件被拒。
- SWF 证据驱动的战斗声音：普通怪攻击 sound_0123、Boss 攻击 sound_0132、飞天连斩 sound_0518、星魔剑 sound_0520 加入 AudioService.SOUND_PATHS。
- 新增专项 tests/test_combat_feedback_sequence_scene（17 项正向 + 5 项负向：FEEDBACK_DUPLICATE_EVENT/FEEDBACK_SEQUENCE_MISMATCH/FEEDBACK_SOUND_UNREGISTERED/FEEDBACK_PASSIVE_SKILL_TRIGGERED/FEEDBACK_STALE_SESSION_EVENT，负向先证明 mutation 作用于目标分支、恢复后同一验证器正向对照）。
- 新增 docs/combat_feedback_registry.json（37 段登记 + 事件映射 + 顺序规则 + 错误码 + 10 项 evidence_gaps）与 docs/evidence/combat_feedback_v103_v9.txt（token 逐行独立）。
- 52 场景全量 + 主场景冒烟全部通过；未改数值/剧情/地图/存档 v21/用户覆盖；未开始 v1.38。

## v1.36 验收记录（2026-08-02）

- PM/SE 终审通过，**v1.36 正式验收（PASS）**。终审覆盖：活动 handle（untrack 清空）、finished 信号 WeakRef 绑定（消除自引用释放）、100 次自然/取消逐次三计数为 0、Scheduler 自然完成/取消/地图重建/场景退出真实 WeakRef 释放断言、场景退出 main/controller/Scheduler/Tween/actor 逐项释放断言、49 条注册记录（5 时间轴/3 static/41 gap）与 44 候选全覆盖、独立 SWF 扫描（tag26=1577/tag28=320/tag39=233/tag4/5=0）、根事件状态机 149/186、专项 ExitCode 0 + 1 PASS + 0 错误、51 场景全量 + 冒烟通过、存档 v21、无 v1.37 实现记录。
- 终审勘误一项（不阻断验收）：报告所称"mutation_applied 前置断言"（启动后槽位+1）不成立——它与 untrack 开关无关；已按验收记录更正为启用/禁用 untrack 的取消后槽位差异证明。真正的负向路径（禁用 untrack → 取消后槽残留 → SCHEDULER_REFERENCE_LEAK → 恢复后通过）经确认有效。
- **v1.37 门禁解除但未开始**：必须等正式任务书下发后方可实施。

## v1.36：NPC、特殊怪物与剧情对象原生时间轴（整改12）

- 整改12（PM/SE 拒签整改11 的 Scheduler 强引用/真实释放验证阻断关闭，不修改 149/186、Boss 帧序、map_change Timer、玩法数值）：
- **承认整改11 的两个不实**：(a) `_last_handle` 是永久保留的强引用 handle（保留已结束 Scheduler/Tween 的强引用），整改11 将其误报为"活动 handle"；(b) 场景退出段的 "no crash, no dangling refs" 只是 print 无任何断言。
- (1) `_last_handle` 改为真正活动 handle：untrack（自然完成/cancel/battle_cancel/map_change）时若 handle 匹配当前 Scheduler 则清空（`_last_handle = {}`），不保留已结束对象的永久强引用。
- (2) 三个独立计数并验证：`active_scheduler_count()`（当前有效 Scheduler 数）、`tracked_scheduler_slot_count()`（注册数组实际槽位数，含失效 WeakRef 残留）、`has_active_scheduler_handle()`（活动 handle 是否存在且指向未 ended Scheduler）——互不替代。
- (3) 100 次自然完成与 100 次取消：**每次**结束后断言三计数均为 0（非仅循环结束后）。
- (4) 真实释放验证：保存 Scheduler WeakRef → 自然完成/取消后释放测试局部强引用 → 等 ≥2 process_frame → `WeakRef.get_ref()==null`；地图切换/actor 重建（_apply_current_map 真实路径）后同样断言释放。**修复真实泄漏**：tween 的 finished 连接 `bind(tween)`/`bind(sched)` 自引用循环导致 tween/sched 永不释放（实测 refcount=2、槽空但对象存活）→ 连接改为绑定 WeakRef（`_untrack_tween.bind(weakref(tween))`、`_untrack_scheduler.bind(weakref(sched))`），释放验证通过。
- (5) SCHEDULER_REFERENCE_LEAK 负向重写：禁用 untrack 后经**注册槽残留**（tracked_scheduler_slot_count）命中精确错误码（不依赖 _last_handle 强引用）；PM/SE 终审勘误：'启动后槽位+1'不是有效的 mutation 证明（与 untrack 开关无关）——已改为**启用/禁用 untrack 时取消后槽位的差异**证明 mutation 生效（启用清零回基线、禁用残留 +1）；恢复 untrack 后用生产 untrack 清理全部残留，同一验证器完全通过。
- (6) 场景退出真实断言：对 main/controller/Scheduler/Tween/临时 actor 建立 WeakRef → queue_free → 释放局部强引用 → 等待 → 逐项断言 `get_ref()==null` 或失效；删除无断言的 "no dangling refs" 打印。
- 自检修复：100 次循环的局部 handle 强引用阻止释放（改临时表达式）；负向验证器多个残留需循环清理；WeakRef 类型显式标注。
- 保持不变：149/186、Boss 帧序、map_change Timer、数值、存档 v21、地图拓扑。
- 以下为整改11 内容（已被整改12 取代的事实以整改12 为准）：
- (1) active scheduler 只保存真正活动的 Scheduler：`_active_schedulers` 存 WeakRef；自然完成（tween.finished→_untrack_scheduler）、cancel/battle_cancel/map_change（cleanup→_untrack_scheduler）后解除引用；`active_scheduler_count()` 计被跟踪的调度器。
- (2) 连续 100 次自然完成（tween.custom_step 确定性推进）与 100 次取消后计数均回到 0（实测）。
- (3) `_drive_scheduler` 返回生产 handle（{scheduler, tween, timeline_id}），`test_last_handle()` 提供最近 handle（非永久历史数组）；`test_last_scheduler()` 从 handle 取。
- (4) duration 负向必须确定性推进**真实生产 Tween**（tween.custom_step，不调用 sched.seek 制造结果），观察 actor：边界序列来自 SWF_EXPECTED 独立期望（负向篡改 dur 1→2 后，期望边界 1/12 观察 actor 仍帧0 → 命中 PRODUCTION_TIMELINE_BOUNDARY_MISMATCH:b1+）。
- (5) 负向结束后恢复进入负向前的独立人物基线（生产 cleanup 恢复快照），再运行正向对照（_verify_player_attack_cancel_restore 通过）。
- (6) 原 texture=null 时恢复精确 null（调度器 restore 总是赋值快照，含 null）。
- (7) ended/cancelled 后 seek/advance 均不再应用帧（幂等终止）；测试断言 cancel 后 seek/advance 节点状态不变。
- (8) 负向：禁用 scheduler untrack → SCHEDULER_REFERENCE_LEAK（同一生命周期验证器）；生产 driver 错误 duration → PRODUCTION_TIMELINE_BOUNDARY_MISMATCH（driver 验证器，SWF_EXPECTED 独立边界 oracle）；cancel 后 seek → 节点状态不得变化。
- (9) driver 边界验证器：真实 _play_*（player_attack 用 player_actor；其余用真实 Control 节点经 target_actor/session 驱动）+ tween.custom_step 推进，t=0/每帧边界±ε/结束点观察真实 actor 的 texture/position/size 与 once_restore（4 个战斗时间轴）。
- 自检发现并修复：Godot tween custom_step 的 interval 完成有 ~0.0005 提前量（边界观察用 ±0.002 窗口）、prev 累计精度、probe 位置基线、期望帧来源（帧表内容 + 独立期望时间边界）、active_scheduler_count 语义（计被跟踪而非未 ended）、负向残留用生产 untrack 清理。
- 保持不变：人物/目标取消、149/186、Boss 帧序、map_change Timer、12 项注册表负向。
- 以下为整改10 内容（已被整改11 取代的事实以整改11 为准）：
- (1) TimelineScheduler 幂等 cancel()/restore()：构造时保存原状态快照（texture/size，position 由 base 表示）；restore 精确恢复 position/size/texture 后执行 on_restore 回调（生产逻辑副作用，幂等重复恢复无害）；ended/cancelled 后再次调用直接返回（自然结束与取消只能 restore 一次）。
- (2) _drive_scheduler 注册 tracked Tween 时传入 Scheduler 取消 cleanup（cleanup=sched.cancel）——Tween 被 battle_cancel/map_change kill 时执行幂等 restore；新增 _active_schedulers 注册表 + test_last_scheduler()（返回与真实 _play_* 同一调度对象）+ test_set_scheduler_cleanup_enabled()（取消负向开关）。
- (3) 人物攻击真实取消测试：保存原 texture/position/size；调真实 _play_player_attack；分别在 t=0、帧1（0505 出现后）、末帧前（0509 出现后）取消；每次精确恢复原状态且 active Tween=0；测试不手工修改人物状态（仅真实生产函数 + cancel_battle + 事件驱动轮询）。
- (4) 目标攻击/受击取消幂等测试：真实 _play_target_attack/_play_target_hit（通过生产成员 target_actor/session 驱动）+ cancel_battle——_force_restore_target_attack/hit 与 Scheduler cleanup 连续发生不二次破坏状态；Boss 与普通怪均恢复原 texture/position/size；连续两次 cancel 幂等。
- (5) 生产集成测试：真实 _play_player_attack 链路（_drive_scheduler + tracked tween + cleanup）启动后经 test_last_scheduler() 取得同一调度对象，同帧内对其 seek(1/12) 观察帧1（tween 未推进，无竞争）；duration 负向（override 帧表 dur 1->2）进入真实 _drive_scheduler 生产链，旧边界 1/12 观察 actor 仍帧0、新边界 2/12 帧1——错误排程在真实链路可被捕获。
- (6) 确定性推进 API 仅为 seek/advance，代码注释、PASS 文案与文档中无额外命名。
- (7) anchor 测试直接检查真实 player_actor、普通怪（spider）、Boss（thunder_boss_10）actor 的 left/top/right/bottom 四字段（各在其地图上检查，避免重建释放）；不再用默认 probe 值代替。
- (8) 取消负向：test_set_scheduler_cleanup_enabled(false) 临时移除 Scheduler cleanup 后，同一验证器 _verify_player_attack_cancel_restore 命中 TIMELINE_CANCEL_RESTORE_MISSING（texture/position/size/tween 精确错误码）；重新启用后同一验证器通过。
- (9) player_attack 的 on_restore 回调改为空（调度器快照负责精确恢复原状态，任务书 7.4；注册表 fallback_frame 保留为证据字段）；删除 _restore_player_frame。
- 保持不变：149/186 根事件结果、map_change Timer 测试、Boss 帧序、12 项注册表负向、自然结束清除、defeat 淡出不悬挂。
- 以下为整改09 内容（已被整改10 取代的事实以整改10 为准）：
- (1) 统一生产时间轴调度器 scripts/timeline_scheduler.gd（TimelineScheduler）：生产 _play_player_attack/_play_target_attack/_play_target_hit 与测试确定性推进（seek/advance）启动**同一个生产调度对象**；测试不再直接调用 _apply_timeline_frame_state 逐帧写入（该函数已删除）。
- (2) 离散帧排程（唯一语义，无额外 duration 段）：t=0 立即应用 frame0 -> 等待 frame0.duration -> 应用 frame1 -> ... -> 等待末帧 duration -> once_restore。player_attack：frame1 在 1/12、frame2 在 2/12、frame3 在 3/12、restore 在 4/12（删除导致 frame1 推迟到 2/12 的多余 property/interval 排程）。
- (3) normal_monster_attack：SWF 证据为离散位置变化（sprite124 frame7 Move+HasMatrix translate=-5px 单帧位移，无补间证据）-> 按帧边界离散应用 offset；总时长 = 2 帧 × 1/12（两帧数据两段时长，不再三段补间）。
- (4) boss_hit：重新核对 sprite134 帧序（frame1 放置 char98 站立、frame3 放置 shape133 受击 -> 帧1-2 站立、帧3-4 受击）；注册表、TIMELINE_FRAMES、生产播放、SWF_EXPECTED 统一为 [站立 image_1072, 受击 boss_hit_native]（t=0 站立、2/12 受击、4/12 once_restore）；专项断言真实生产 t=0 及 2/12 对应图像。
- (5) 真正生产边界测试：验证器基于真实 TimelineScheduler 实例（与 _play_* 同一类）执行 seek/advance，在真实节点观察 t=0/边界-ε/边界/边界+ε/end 的 texture/position/size 及 once_restore；duration 负向篡改（dur 1->2）启动生产调度器，在旧边界 1/12（帧0）与新边界 2/12（帧1）观察实际 actor 状态变化。
- (6) anchor 语义：调度器不写入 anchor（验证器断言 seek 前后节点 anchor 字段不变）；anchor 为 actor 静态属性（注册表 top_left 的 position 语义），生产场景真实 actor（player_actor）anchor 字段 (0,0,0,0) 静态断言。
- (7) 修正 scene_battle_controller.gd 顶部注释"五个 TIMELINE_FRAMES"残留错误（实际 4 个战斗时间轴，player_motion 由 main_original 覆盖）。
- (8) 相邻测试同步：test_monster_attack_animation_scene 改为事件驱动轮询（离散帧0 偏移出现/回位/结束），删除猜测 timer 断言；test_monster_hit_scene 天然适配新帧序（0.20s 时受击帧保持）。
- 保持不变：根事件状态机 149/186 结果、map_change Timer 测试、12 项注册表负向、自然结束清除、defeat 淡出不悬挂。
- 以下为整改08 内容（已被整改09 取代的事实以整改09 为准）：
- A. 根事件状态机：root_events 记录 flags/has_character；HasCharacter=false 的 Move 更新**不覆盖**当前 character（旧实现把 Move 覆盖为 -1 导致漏判 3 个角色——这正是整改07 的 146 与 PM 独立模拟 149 的差异根源，已删除"146/149 口径差异"说明）；新增 move-only 固定夹具（8 用例 10 断言）；PM 点名 787/925/1228 断言为 true（Move 修复正确性固化）；全量统计 removed=149、unique=186，与 PM 独立模拟完全一致；18 个"替换后删除不算旧角色"字符断言保留。
- B. 真正生产确定性时间轴：删除 test_timeline_state_at/test_apply_timeline_state_at（测试辅助器自证来源）；生产状态转换唯一函数 _apply_timeline_frame_state()（texture/position/size 写入节点），生产动画播放（_play_player_attack/_play_target_attack/_play_target_hit）与测试确定性验证共用；生产所有时长改从 _frame_duration()（duration_frames/NATIVE_FPS）获取，删除 0.08/0.055/0.11/0.17 独立硬编码；边界验证器 _verify_timeline_boundaries() 以 SWF_EXPECTED 为独立 oracle（帧表 res/dur/offset/size/anchor 逐项对比 + t=0/边界±ε/结束点 + 经生产函数写入真实 Control 节点断言 texture/position/size/真实 anchor 字段 0,0,0,0）；负向：篡改生产 duration（dur 1→2）被同一验证器捕获（dur0/dur1 报错）；player_motion 五剪辑（idle8/down8/up8/left10/right10）完整帧序列逐帧断言 + 循环回绕，删除整改07"idle 未循环"错误缺口（生产待机确实循环播放 player_idle_frames）。
- 覆盖范围如实说明：TIMELINE_FRAMES 覆盖 4 个战斗时间轴（player_attack/normal_monster_attack/boss_attack/boss_hit）；player_motion 由 main_original.gd 生产帧表（player_idle_frames/player_walk_frames，8/8/8/10/10 帧）覆盖并在 main 侧逐帧验证——不声称 TIMELINE_FRAMES 含五项。
- 以下为整改07 内容（已被整改08 取代的事实以整改08 为准）：

## v1.36：NPC、特殊怪物与剧情对象原生时间轴（整改06）

- 整改06（PM/SE 拒签整改05 的六项阻断关闭）：(1) 自然结束清除断言——保留段不再手工 kill/clear，改为等待生产回调解除跟踪（tween await finished->_untrack；timer await 生产 _cancellable_wait 完成后跟踪数组为空）；(2) 路径验证去空值绕过——implementation_path/test_scene 缺失或空串一律 TIMELINE_IMPLEMENTATION_MISSING / TIMELINE_TEST_SCENE_MISSING，新增删除字段与空串两项负向（负向总数 12）；(3) 根移除判定消除循环自证——depth 匹配重构为生产纯函数，测试用固定输入/固定预期夹具直接调用，并将整改05 修复的 10 个误判字符（1302-1311）固化为自动化断言；(4) 真实 map_change 生命周期——scene_battle_controller.handle_map_change() 由 main_original._apply_current_map() 调用，真实地图切换 kill/stop 并解除跟踪 on_map_change 活动资源；(5) 确定性逐帧边界矩阵（整改07 中升级为完整时间边界矩阵）；(6) 文档事实修正——swf_parser 注释 320→285、"每帧清空"改为"分布在帧3-38 的部分帧（帧7/37 无 RemoveObject2）"。
- 以下为整改05 内容（已被整改06/07 取代的事实以整改06/07 为准）：

## v1.36：NPC、特殊怪物与剧情对象原生时间轴（整改05）

- 引用链完整化：swf_parser.gd 解析 PlaceObject2 完整 matrix（translate+scale+rotate）与 ColorTransform 实际参数（CXFORM add/mult 四通道，非仅标志位；sprite1159 frame2/3 实测 add=[77,77,77,0]/[38,38,38,0] 悬停变亮），解析 RemoveObject2（深度删除，帧序状态机：对象"放置后被移除"计为显示列表变化），find_placers_of 含根时间轴，is_bitmap_visually_static 递归到根。视觉变化判定：char_swap / translate / scale_rotate / color / remove 任一触发即非静态。
- 标签分布修正口径（整改05）：全文件递归统计（递归进入全部 233 个 DefineSprite）——PlaceObject(tag4)=0、RemoveObject(tag5)=0、PlaceObject2(tag26)=1577、RemoveObject2(tag28)=320、DefineSprite(tag39)=233，与 PM/SE 独立递归扫描数字一致；根时间轴口径 tag26=327/tag28=285 单独保留；swf_parser.gd 与 work/_v136_tagscan.py 均递归统计。
- 根 RemoveObject2 查询修复（整改05）：has_root_remove_after_placement() 改为按 (frame, depth) 匹配（原实现只比帧号，会把其他 depth 删除误判）；测试内独立参考实现（纯数据模拟）对根时间轴全部 219 个 character 逐一对比，零不一致。
- cancel_policy 门禁补全（整改05）：validate_registry 增加删除 cancel_policy 字段 -> TIMELINE_CANCEL_POLICY_MISSING 分支与真实负向（任务书 7 必测项）；合法但错误值仍命中 TIMELINE_CANCEL_POLICY_MISMATCH。
- 注册表路径与帧边界验证（整改05）：validate_registry 校验 implementation_path / test_scene 文件存在；frames 数量必须与 SWF_EXPECTED 完全一致，缺帧/多帧均命中 TIMELINE_FRAME_COUNT_MISMATCH（新增两项负向）。
- 策略取消丢跟踪修复（整改05）：_kill_active_tweens/_clear_active_timers 仅移除实际 kill/stop 的条目，不匹配当前事件策略的资源保留跟踪；测试用生产同款登记结构手动登记 on_map_change 资源，battle_cancel 事件后断言其存活且仍被跟踪（修复前会被无条件 clear 丢跟踪）。
- 根时间轴 RemoveObject2 事实如实记录：285 个分布在帧3-38 的部分帧（帧13=29个、帧38=58个清场；帧7、帧37 无 RemoveObject2，并非每帧清空），属场景状态机切换（如 marshal 帧13放置帧14移除），不构成对象自身动画证据；swf_parser.has_root_remove_after_placement() 按 (frame, depth) 匹配查询；static_native 分类仅基于对象自身动画证据，根 remove 与 gotoAndStop 常驻帧列为证据缺口。
- 专项用例 image_1156->shape1157->sprite1158->sprite1159：sprite1159(3帧) frame2-3 有 ColorTransform（悬停色变，实际参数 add=77/38）-> "视觉不变"不成立 -> grocery 为 evidence_gap。
- 引用链分类结果（完整判定后）：3 实体链路无任何视觉变化（marshal/research/mine_nodes）-> static_native；41 实体 wrapper 存在视觉变化（17 NPC color 悬停色变、15 怪物 sprite118/1074 选择器 char_swap+translate+scale_rotate+内部RemoveObject2、princess/chest 真动画、福娃选择器）-> evidence_gap。research/mine_nodes 在 SWF 任何时间轴无放置记录（疑动态附加）已如实注明。
- 修改 scripts/scene_battle_controller.gd：cancel_policy 生产真实接入——启动时 _load_timeline_policies() 读注册表，每个活动 Tween/Timer 登记 timeline_id，事件站点按 timeline_cancelled_by(timeline_id, event) 查询策略后决定取消（battle_cancel 事件=on_battle_cancel 时间轴；map_change 事件=on_map_change 时间轴）。全部 5 处生产 Tween 带 timeline_id；Timer 用可 stop 的 Timer 节点（_cancellable_wait 轮询）。
- Tween/Label 清理：_create_tracked_tween(actor, timeline_id, cleanup) 登记 cleanup callable，_kill_active_tweens kill 时执行（浮动文字 label.queue_free 不再残留；_finish_defeat 淡出被 kill 时 cleanup 恢复 modulate）；_finish_defeat 用 is_running() 轮询替代 await fade.finished（kill 后协程退出不悬挂，attack_sequence 检查防续跑）。
- 修改 scripts/main_original.gd：_apply_current_map 按策略重置 player_motion（on_map_change 事件取消移动动画状态，回 idle 首帧）；_rebuild_map_actors->cancel_battle()（battle_cancel 事件）保留。
- 证据文件整改：全部 token 独立成行（image_0503/0505/0507/0509、frame=6、image_0127/0129 及 3 static + 41 gap 实体逐实体 image_XXXX/evidence_gap 行），禁止压缩写法 image_0503/0505/0507/0509；validate_registry 三分类（timelines/static/gap）token 全量校验。
- test_native_timeline_registry_scene 专项测试：实例化 main.tscn 触发真实事件；生产策略断言（5 timeline 生产读取值==SWF_EXPECTED，语义 on_battle_cancel≠map_change 事件）；Timer 保存真实节点引用，cancel_battle/地图切换后逐个断言 is_stopped()==true 且节点数帧内释放（等待协程退出可观察，无悬挂）；浮动文字 Label 取消前后计数==0 无残留；defeat 淡出期间 cancel_battle -> modulate 由 cleanup 恢复、流程中断（不切地图）；单一 validate_registry() 正向0错误，7项负向用合法但错误值（resource 换合法文件、duration 1->2、offset [-33,-42]->[-32,-42]、cancel_policy on_map_change->on_battle_cancel、evidence token、anchor top_left->top_right、fps 12->11）命中精确错误码；1156 引用链专项；文档统计按解析 character ID 去重（实体记录49=5+3+41/唯一bitmap 33/验证44/解析失败0）。
- 不改玩法数值/地图拓扑/存档（仍v21）；未开始v1.37。
- 专项、全量回归通过；完整回归为51个自动化场景加主场景冒烟测试（全部 RUN + PASS + 无 FAIL/SCRIPT ERROR）。